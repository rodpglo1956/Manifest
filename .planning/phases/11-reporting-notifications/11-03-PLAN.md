---
phase: 11-reporting-notifications
plan: 03
type: execute
wave: 2
depends_on: [11-02]
files_modified:
  - src/app/(app)/settings/notifications/page.tsx
  - src/app/(app)/settings/notifications/actions.ts
  - src/app/(app)/settings/notifications/notification-preferences-form.tsx
autonomous: true
requirements: [NOTF-03, NOTF-04]

must_haves:
  truths:
    - "User can toggle which channels (in-app, push, email, SMS) are enabled per notification category"
    - "User can set quiet hours start/end time and timezone"
    - "Preferences are saved and respected by notification dispatcher"
  artifacts:
    - path: "src/app/(app)/settings/notifications/notification-preferences-form.tsx"
      provides: "Category x channel toggle matrix with quiet hours"
      min_lines: 80
    - path: "src/app/(app)/settings/notifications/actions.ts"
      provides: "Server actions for preferences CRUD"
      exports: ["getNotificationPreferences", "updateNotificationPreferences"]
  key_links:
    - from: "src/app/(app)/settings/notifications/notification-preferences-form.tsx"
      to: "src/app/(app)/settings/notifications/actions.ts"
      via: "Server actions for save/load"
      pattern: "updateNotificationPreferences"
---

<objective>
Replace the existing simple boolean notification preferences (Phase 6) with the full category-by-channel toggle matrix. Users can enable/disable specific channels (in-app, push, email, SMS) per notification category (compliance, maintenance, load, billing, crm, driver, system, marie), and configure quiet hours.

Purpose: Users control exactly how they receive notifications to reduce noise while staying informed on critical items.
Output: Updated notification preferences page with matrix UI and quiet hours config.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-reporting-notifications/11-02-SUMMARY.md
@src/app/(app)/settings/notifications/page.tsx
@src/app/(app)/settings/notifications/notification-preferences-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification preferences matrix UI</name>
  <files>src/app/(app)/settings/notifications/notification-preferences-form.tsx, src/app/(app)/settings/notifications/actions.ts, src/app/(app)/settings/notifications/page.tsx</files>
  <action>
1. Update src/app/(app)/settings/notifications/actions.ts:
   - Replace existing simple boolean preference actions with new ones targeting notification_preferences table
   - `getNotificationPreferencesV2()`: fetches from notification_preferences table for current user. If no record, returns defaults. Returns typed NotificationPreferencesV2
   - `updateNotificationPreferencesV2(prefs: Partial<NotificationPreferencesV2>)`: upserts notification_preferences record
   - Keep backward compat: existing simple preferences still work for Phase 6 push toggle

2. Rewrite src/app/(app)/settings/notifications/notification-preferences-form.tsx:
   - Render a matrix table/grid:
     - Rows: 8 categories (Compliance, Maintenance, Loads, Billing, CRM, Drivers, System, Marie)
     - Columns: 4 channels (In-App, Push, Email, SMS)
     - Each cell: toggle switch (small checkbox or switch component)
     - In-app column is always enabled and non-toggleable (notifications always stored)
   - Below the matrix: Quiet Hours section
     - Enable quiet hours toggle
     - Start time input (type="time")
     - End time input (type="time")
     - Timezone select dropdown (common US timezones: Eastern, Central, Mountain, Pacific, plus UTC)
     - Help text: "Push, email, and SMS notifications will be held during quiet hours. In-app notifications are still stored."
   - Save button at bottom, uses optimistic toggle pattern (per Phase 6 convention: update UI immediately, revert on server error)
   - Load preferences on mount via server action

3. Update src/app/(app)/settings/notifications/page.tsx:
   - Keep existing page structure but render the new preferences form component
   - Page title: "Notification Preferences"
   - Section heading explaining the matrix
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Category x channel toggle matrix renders with all 8 categories and 4 channels. Quiet hours section has time inputs and timezone select. Preferences save and load correctly. No type errors.</done>
</task>

</tasks>

<verification>
- Preferences page shows 8x4 toggle matrix
- Quiet hours can be configured with timezone
- Changes persist across page refresh
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- User can independently control each category's channels
- Quiet hours respected by dispatcher (tested via dispatcher logic from Plan 02)
- In-app always enabled, non-toggleable
</success_criteria>

<output>
After completion, create `.planning/phases/11-reporting-notifications/11-03-SUMMARY.md`
</output>
