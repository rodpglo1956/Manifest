---
phase: 11-reporting-notifications
plan: 05
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/(app)/analytics/fleet/page.tsx
  - src/app/(app)/analytics/drivers/page.tsx
  - src/app/(app)/analytics/customers/page.tsx
  - src/app/(app)/analytics/components/fleet-charts.tsx
  - src/app/(app)/analytics/components/driver-scorecard.tsx
autonomous: true
requirements: [REPT-05, REPT-02, REPT-06]

must_haves:
  truths:
    - "Fleet analytics shows utilization, MPG trending, maintenance cost by vehicle, and vehicle TCO ranking"
    - "Driver scorecards rank drivers by loads, miles, revenue, on-time %, fuel efficiency, safety, compliance"
    - "Customer/broker profitability ranking shows revenue, loads, avg rate, days to pay"
  artifacts:
    - path: "src/app/(app)/analytics/fleet/page.tsx"
      provides: "Fleet analytics dashboard"
      min_lines: 40
    - path: "src/app/(app)/analytics/drivers/page.tsx"
      provides: "Driver performance scorecards table"
      min_lines: 40
    - path: "src/app/(app)/analytics/customers/page.tsx"
      provides: "Customer/broker profitability ranking"
      min_lines: 40
  key_links:
    - from: "src/app/(app)/analytics/drivers/page.tsx"
      to: "src/lib/analytics/actions.ts"
      via: "Server action data fetch"
      pattern: "getDriverPerformance"
    - from: "src/app/(app)/analytics/customers/page.tsx"
      to: "src/lib/analytics/actions.ts"
      via: "Server action data fetch"
      pattern: "getCustomerAnalytics"
---

<objective>
Build fleet analytics, driver scorecards, and customer/broker profitability pages. Fleet shows utilization charts, MPG trending, maintenance costs, and vehicle TCO ranking. Drivers page has a sortable scorecard table. Customers page ranks profitability with pay speed metrics.

Purpose: Operators identify their best/worst performing assets, drivers, and customers to make data-driven decisions.
Output: Three analytics sub-pages with charts and sortable tables.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-reporting-notifications/11-01-SUMMARY.md
@src/lib/analytics/actions.ts

<interfaces>
From src/lib/analytics/actions.ts (created in Plan 01):
```typescript
export async function getFleetAnalytics(period: 'week' | 'month' | 'quarter')
export async function getDriverPerformance(period: 'month' | 'quarter' | 'year')
export async function getCustomerAnalytics()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fleet analytics and driver scorecards</name>
  <files>src/app/(app)/analytics/fleet/page.tsx, src/app/(app)/analytics/components/fleet-charts.tsx, src/app/(app)/analytics/drivers/page.tsx, src/app/(app)/analytics/components/driver-scorecard.tsx</files>
  <action>
1. Create src/app/(app)/analytics/components/fleet-charts.tsx:
   - FleetUtilizationChart: Recharts AreaChart showing fleet_utilization_pct over time (blue fill)
   - MpgTrendChart: Recharts LineChart showing avg_mpg trending per period
   - MaintenanceCostChart: Recharts BarChart showing maintenance cost by vehicle (horizontal bars, top 10 vehicles)
   - FuelCostChart: Recharts LineChart showing total_fuel_cost trending
   - All follow existing chart patterns (ResponsiveContainer, rounded border, gray grid)

2. Create src/app/(app)/analytics/fleet/page.tsx:
   - Server component, reads period from searchParams
   - Calls getFleetAnalytics(period)
   - Renders PeriodSelector at top
   - Grid layout: FleetUtilizationChart and MpgTrendChart on first row, MaintenanceCostChart and FuelCostChart on second row
   - Below charts: "Vehicle TCO Ranking" table with columns: Unit #, Total Maintenance, Total Fuel, Total Cost, Cost/Mile -- sorted by total cost desc
   - Top 10 vehicles shown

3. Create src/app/(app)/analytics/components/driver-scorecard.tsx:
   - Client component for sortable driver table
   - Columns: Driver Name, Loads, Miles, Revenue, On-Time %, MPG, Safety Incidents, Compliance Score
   - Each column header is clickable for sort toggle (asc/desc)
   - Default sort: revenue desc
   - Row colors: on_time_pct > 90% green subtle bg, < 70% red subtle bg
   - Compliance score shows colored badge (green > 80, yellow 50-80, red < 50)
   - Uses useState for sort state (client-side sorting since data set is small per org)

4. Create src/app/(app)/analytics/drivers/page.tsx:
   - Server component, reads period from searchParams (default 'month')
   - Calls getDriverPerformance(period)
   - Period selector with month/quarter/year options
   - Renders DriverScorecard table with fetched data
   - Summary stats at top: total drivers, avg on-time %, avg compliance score
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Fleet page shows 4 charts and vehicle TCO table. Drivers page shows sortable scorecard table with color-coded metrics. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Customer/broker profitability page</name>
  <files>src/app/(app)/analytics/customers/page.tsx</files>
  <action>
1. Create src/app/(app)/analytics/customers/page.tsx:
   - Server component
   - Calls getCustomerAnalytics()
   - Two sections:

   Section 1: "Customer Profitability" table
   - Columns: Company Name, Type (badge: customer/broker), Total Revenue, Loads, Avg Rate/Mile, Avg Days to Pay
   - Sorted by total revenue desc
   - Days to Pay column: green < 30, yellow 30-45, red > 45

   Section 2: "Broker Reliability" table (filtered to type='broker' from same data)
   - Columns: Broker Name, Loads, Revenue, Avg Days to Pay, Payment Rating
   - Payment Rating: computed from avg_days_to_pay (Excellent < 15, Good 15-30, Fair 30-45, Poor > 45)
   - Rating shown as colored badge

   - Empty state for each section: "No customer data yet. Complete loads to see profitability rankings."
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Customer profitability and broker reliability tables render with color-coded pay speed metrics. No type errors.</done>
</task>

</tasks>

<verification>
- /analytics/fleet shows 4 charts and vehicle TCO ranking
- /analytics/drivers shows sortable scorecard table
- /analytics/customers shows customer profitability and broker reliability tables
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Fleet metrics visualized with utilization, MPG, costs, TCO
- Driver performance sortable by any metric
- Customer/broker rankings highlight pay speed and profitability
</success_criteria>

<output>
After completion, create `.planning/phases/11-reporting-notifications/11-05-SUMMARY.md`
</output>
