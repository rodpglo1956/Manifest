---
phase: 11-reporting-notifications
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/app/(app)/analytics/page.tsx
  - src/app/(app)/analytics/layout.tsx
  - src/app/(app)/analytics/operations/page.tsx
  - src/app/(app)/analytics/components/kpi-card.tsx
  - src/app/(app)/analytics/components/period-selector.tsx
  - src/app/(app)/analytics/components/revenue-profit-chart.tsx
  - src/app/(app)/analytics/components/operations-charts.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [REPT-03, REPT-04]

must_haves:
  truths:
    - "Analytics main dashboard shows revenue/expenses/profit chart with KPI cards and period comparison"
    - "Operations page shows load volume, miles, on-time trending, and top lanes table"
    - "Period selector allows switching between week, month, quarter views"
  artifacts:
    - path: "src/app/(app)/analytics/page.tsx"
      provides: "Main analytics dashboard with KPI cards and revenue/profit chart"
      min_lines: 40
    - path: "src/app/(app)/analytics/operations/page.tsx"
      provides: "Operations analytics with load volume and performance charts"
      min_lines: 40
    - path: "src/app/(app)/analytics/components/kpi-card.tsx"
      provides: "Reusable KPI card with value, label, change indicator"
      exports: ["KPICard"]
  key_links:
    - from: "src/app/(app)/analytics/page.tsx"
      to: "src/lib/analytics/actions.ts"
      via: "Server action data fetch"
      pattern: "getAnalyticsDashboard"
    - from: "src/app/(app)/analytics/operations/page.tsx"
      to: "src/lib/analytics/actions.ts"
      via: "Server action data fetch"
      pattern: "getOperationsAnalytics"
---

<objective>
Build the main analytics dashboard and operations analytics pages. The main dashboard shows revenue/expenses/profit chart, KPI cards (RPM, CPM, profit/mile, fleet utilization, on-time %, compliance score) with period-over-period comparison badges. The operations page shows load volume, miles trending, on-time delivery trending, and top performing lanes.

Purpose: Operators see actionable financial and operational metrics with trend comparison at a glance.
Output: Analytics main page, operations page, shared components (KPI card, period selector), sidebar nav.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-reporting-notifications/11-01-SUMMARY.md
@src/app/(app)/dashboard/charts/revenue-trend-chart.tsx
@src/lib/analytics/actions.ts

<interfaces>
From src/lib/analytics/actions.ts (created in Plan 01):
```typescript
export async function getAnalyticsDashboard(period: 'week' | 'month' | 'quarter')
export async function getOperationsAnalytics(period: 'week' | 'month' | 'quarter')
```

From src/lib/analytics/snapshot-helpers.ts:
```typescript
export function formatCurrency(value: number): string
export function formatPercent(value: number): string
export function calculatePeriodComparison(current, previous, metric): { value, change, changePercent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analytics shared components and main dashboard</name>
  <files>src/app/(app)/analytics/layout.tsx, src/app/(app)/analytics/page.tsx, src/app/(app)/analytics/components/kpi-card.tsx, src/app/(app)/analytics/components/period-selector.tsx, src/app/(app)/analytics/components/revenue-profit-chart.tsx, src/components/layout/app-sidebar.tsx</files>
  <action>
1. Create src/app/(app)/analytics/components/kpi-card.tsx:
   - Props: { label: string, value: string, change?: number, changePercent?: number, icon?: ReactNode }
   - Renders a card with: value (text-2xl font-bold), label (text-sm text-gray-500), change badge (green arrow up or red arrow down with percentage)
   - If change > 0: green text with ArrowUp icon; if < 0: red text with ArrowDown icon; if 0 or undefined: gray "—"

2. Create src/app/(app)/analytics/components/period-selector.tsx:
   - Client component with 3 buttons: Week, Month, Quarter
   - Uses URL searchParams (?period=week|month|quarter) for server-side data loading
   - Active button has primary background color, others have outline style
   - Default: month

3. Create src/app/(app)/analytics/components/revenue-profit-chart.tsx:
   - Recharts LineChart with 3 lines: Revenue (brand pink #EC008C), Expenses (red #EF4444), Profit (green #22C55E)
   - Uses same styling patterns as existing revenue-trend-chart.tsx (CartesianGrid, rounded container, etc.)
   - Props: data array with { date, revenue, expenses, profit }
   - Empty state message matching existing pattern

4. Create src/app/(app)/analytics/layout.tsx:
   - Sub-navigation tabs: Overview, Operations, Fleet, Drivers, Customers, Reports
   - Links to /analytics, /analytics/operations, /analytics/fleet, /analytics/drivers, /analytics/customers, /analytics/reports
   - Active tab highlighted based on pathname
   - Layout wraps children with heading "Analytics" and the tab nav below

5. Create src/app/(app)/analytics/page.tsx (main dashboard):
   - Server component, reads period from searchParams (default 'month')
   - Calls getAnalyticsDashboard(period) to get data
   - Renders PeriodSelector at top
   - KPI grid (3 cols on lg, 2 on md, 1 on sm): revenue/mile, cost/mile, profit/mile, fleet utilization, on-time %, compliance score -- each with period comparison change
   - Revenue/Expenses/Profit chart below KPIs
   - Deadhead percentage as a small gauge or stat card

6. Update src/components/layout/app-sidebar.tsx:
   - Add "Analytics" section in sidebar nav with BarChart3 icon (lucide-react)
   - Link to /analytics
   - Use collapsible group pattern (per Phase 8 convention) with auto-expand on /analytics/* pages
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Analytics main dashboard renders KPI cards with period comparison, revenue/expenses/profit chart, period selector. Sidebar has Analytics link. No type errors.</done>
</task>

<task type="auto">
  <name>Task 2: Operations analytics page</name>
  <files>src/app/(app)/analytics/operations/page.tsx, src/app/(app)/analytics/components/operations-charts.tsx</files>
  <action>
1. Create src/app/(app)/analytics/components/operations-charts.tsx:
   - LoadVolumeChart: Recharts BarChart with two bars (completed=green, canceled=red) per period
   - MilesTrendChart: Recharts LineChart showing total_miles trending
   - OnTimeTrendChart: Recharts LineChart showing on_time_delivery_pct trending with 90% target reference line (ReferenceLine)
   - RatePerMileChart: Recharts LineChart showing avg_rate_per_mile trending
   - All follow existing chart styling patterns (rounded container, gray grid, brand colors)

2. Create src/app/(app)/analytics/operations/page.tsx:
   - Server component, reads period from searchParams
   - Calls getOperationsAnalytics(period) to get data
   - Renders PeriodSelector at top
   - Grid layout: LoadVolumeChart and MilesTrendChart on first row, OnTimeTrendChart and RatePerMileChart on second row
   - Below charts: "Top Lanes" table with columns: Origin, Destination, Loads, Revenue, Avg Rate/Mile -- top 5 lanes sorted by revenue
   - Table uses standard Tailwind table styling matching existing tables
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Operations page shows load volume, miles, on-time, rate/mile charts plus top lanes table. Period selector works. No type errors.</done>
</task>

</tasks>

<verification>
- /analytics shows KPI cards with comparison badges and revenue/profit chart
- /analytics/operations shows 4 charts and top lanes table
- Period selector switches data between week/month/quarter
- Sidebar shows Analytics link
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Revenue, expenses, profit visible with period-over-period change arrows
- Operations trending charts render with proper empty states
- Top lanes table shows ranked lanes by revenue
</success_criteria>

<output>
After completion, create `.planning/phases/11-reporting-notifications/11-04-SUMMARY.md`
</output>
