---
phase: 08-fleet-management
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/(app)/fleet/dashboard/page.tsx
  - src/app/(app)/fleet/maintenance/page.tsx
  - src/app/(app)/fleet/fuel/page.tsx
  - src/components/fleet/fleet-dashboard.tsx
  - src/components/fleet/maintenance-center.tsx
  - src/components/fleet/fuel-dashboard.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [FLET-09, FLET-03, FLET-04, FLET-06, FLET-07]

must_haves:
  truths:
    - "Fleet dashboard shows vehicles by status, maintenance due count, fleet cost per mile, top 5 expensive vehicles"
    - "Maintenance center lists upcoming/overdue maintenance with add form"
    - "Fuel dashboard shows fuel spend, MPG ranking, cost per mile trending"
    - "Sidebar has expandable Fleet section with sub-navigation"
  artifacts:
    - path: "src/app/(app)/fleet/dashboard/page.tsx"
      provides: "Fleet dashboard overview page"
      min_lines: 30
    - path: "src/components/fleet/fleet-dashboard.tsx"
      provides: "Fleet dashboard component with stat cards and charts"
      min_lines: 100
    - path: "src/components/fleet/maintenance-center.tsx"
      provides: "Maintenance list with filters and add form"
      min_lines: 100
    - path: "src/components/fleet/fuel-dashboard.tsx"
      provides: "Fuel stats, MPG ranking, transaction list"
      min_lines: 100
  key_links:
    - from: "src/components/fleet/fleet-dashboard.tsx"
      to: "src/lib/fleet/actions.ts"
      via: "data fetching"
      pattern: "getFleetCostSummary|getUpcomingMaintenance"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "/fleet/*"
      via: "sub-navigation links"
      pattern: "fleet/dashboard|fleet/maintenance|fleet/fuel"
---

<objective>
Create the fleet dashboard (status snapshot, maintenance due, cost metrics, top expensive vehicles), maintenance center page, fuel dashboard page, and update sidebar with fleet sub-navigation.

Purpose: Operational fleet visibility for dispatchers and admins.
Output: 3 fleet pages and sidebar sub-nav update.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fleet-management/08-CONTEXT.md

@src/components/layout/app-sidebar.tsx
@src/lib/fleet/actions.ts
@src/lib/fleet/fleet-helpers.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fleet dashboard and maintenance center pages</name>
  <files>src/app/(app)/fleet/dashboard/page.tsx, src/components/fleet/fleet-dashboard.tsx, src/app/(app)/fleet/maintenance/page.tsx, src/components/fleet/maintenance-center.tsx, src/app/(app)/fleet/page.tsx</files>
  <action>
    1. Update src/app/(app)/fleet/page.tsx to redirect to /fleet/dashboard (or render dashboard inline). The current vehicle list moves to the vehicle list within the dashboard or remains accessible. Decision: keep /fleet as vehicles list (existing behavior), add /fleet/dashboard as the new dashboard page. Add tab navigation at top of /fleet page: "Vehicles" (current, active), "Dashboard", "Maintenance", "Fuel" as links.

    2. Create src/app/(app)/fleet/dashboard/page.tsx (server component):
       - Fetch fleet summary data via getFleetCostSummary() and getUpcomingMaintenance()
       - Fetch vehicles grouped by status (simple count query)
       - Render FleetDashboard client component with data

    3. Create src/components/fleet/fleet-dashboard.tsx (client component):
       - Fleet Snapshot section: 4 stat cards in grid — Active Vehicles (green), In Shop (yellow), Out of Service (red), Total Fleet (gray). Each shows count, clicking navigates to /fleet?status=X.
       - Maintenance Due section: Count of maintenance items due within 30 days with severity indicator. List of top 5 upcoming items with vehicle name, type, and due date. "View All" link to /fleet/maintenance.
       - Cost Per Mile section: Fleet-wide cost per mile in large format. Breakdown bar (maintenance vs fuel vs depreciation proportions). Use formatCurrency from fleet-helpers.
       - Top 5 Expensive Vehicles section: Table with vehicle unit_number, total cost (maintenance + fuel), cost per mile, miles. Sorted by total cost desc.
       - Use Tailwind card pattern consistent with existing dashboard (stat card with icon, label, value, optional delta).

    4. Create src/app/(app)/fleet/maintenance/page.tsx (server component):
       - Fetch all maintenance records and upcoming maintenance
       - Render MaintenanceCenter component

    5. Create src/components/fleet/maintenance-center.tsx (client component):
       - Filter bar: vehicle selector, maintenance type filter, date range, status (all/upcoming/overdue/completed)
       - Maintenance list: table with columns — Vehicle, Type (with MAINTENANCE_TYPE_LABELS), Vendor, Cost, Date In, Date Out, Downtime, Status
       - Overdue items highlighted with red background
       - "Log Maintenance" button opens inline add form (collapsible, same ?addItem=true URL pattern from compliance)
       - Inline form uses maintenanceRecordSchema with vehicle selector dropdown, all fields from schema
       - Form submits via createMaintenanceRecord server action, uses useTransition for pending state
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Fleet dashboard shows 4 stat cards, maintenance due, cost per mile, top 5 vehicles. Maintenance center lists records with filters and add form.</done>
</task>

<task type="auto">
  <name>Task 2: Fuel dashboard and sidebar sub-navigation</name>
  <files>src/app/(app)/fleet/fuel/page.tsx, src/components/fleet/fuel-dashboard.tsx, src/components/layout/app-sidebar.tsx</files>
  <action>
    1. Create src/app/(app)/fleet/fuel/page.tsx (server component):
       - Fetch fuel transactions (last 90 days), fleet cost summary
       - Render FuelDashboard component

    2. Create src/components/fleet/fuel-dashboard.tsx (client component):
       - Fuel Spend section: total fuel spend (last 30 days) stat card. If Recharts available (it is from Phase 6), show fuel spend bar chart by week (last 12 weeks). Use ResponsiveContainer pattern from Phase 6 analytics.
       - MPG Ranking section: Table of vehicles sorted by avg_mpg desc. Columns: Vehicle, Avg MPG, Total Gallons, Total Miles. Color code: green for above fleet avg, red for below. Highlight worst performer.
       - Cost Per Mile Trending section: If enough data, show line chart of fleet cost per mile by month.
       - Fuel Transaction List section: Table with date, vehicle, driver, location, gallons, price/gal, total, odometer. Filterable by vehicle and date range.
       - "Log Fuel" button opens inline form (collapsible) using fuelTransactionSchema, vehicle and driver selectors, submits via createFuelTransaction.

    3. Update src/components/layout/app-sidebar.tsx:
       - Replace the single Fleet nav item with an expandable group (like Compliance if it has sub-items, or use a simple pattern):
         Fleet (parent, icon: Truck, href: /fleet) with sub-items:
           - Vehicles (/fleet) — the vehicle list
           - Dashboard (/fleet/dashboard)
           - Maintenance (/fleet/maintenance)
           - Fuel (/fleet/fuel)
       - Use a collapsible sub-nav pattern: clicking "Fleet" shows/hides sub-items. Active state based on current path.
       - Import Wrench icon for Maintenance, Fuel icon for Fuel, BarChart3 for Dashboard from lucide-react.
       - Keep sidebar clean: sub-items are indented and smaller text.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Fuel dashboard shows spend chart, MPG ranking, cost trending, and transaction list. Sidebar has expandable Fleet section with 4 sub-items.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Fleet dashboard renders with stat cards and data sections
- Maintenance center renders with table and inline form
- Fuel dashboard renders with charts and transaction list
- Sidebar shows expandable Fleet nav with sub-items
</verification>

<success_criteria>
- Fleet dashboard provides at-a-glance fleet health: status counts, maintenance due, cost per mile, expensive vehicles
- Maintenance center allows viewing/adding maintenance records with filters
- Fuel dashboard shows fuel spending, MPG ranking, and cost per mile trending
- Sidebar navigation updated with fleet sub-sections
</success_criteria>

<output>
After completion, create `.planning/phases/08-fleet-management/08-04-SUMMARY.md`
</output>
