---
phase: 08-fleet-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00023_fleet_management.sql
  - src/types/database.ts
  - src/schemas/vehicle.ts
  - src/schemas/fleet.ts
  - src/lib/fleet/fleet-helpers.ts
  - src/components/ui/status-badge.tsx
  - tests/fleet/fleet-helpers.test.ts
autonomous: true
requirements: [FLET-01, FLET-02, FLET-03, FLET-04, FLET-06, FLET-07, FLET-08]

must_haves:
  truths:
    - "Vehicles table supports class, fuel type, odometer, purchase info, photos, and expanded statuses"
    - "Maintenance records, schedules, fuel transactions, and vehicle assignments tables exist with RLS"
    - "Cost per mile can be calculated from maintenance + fuel data"
    - "Zod schemas validate all fleet entity inputs"
  artifacts:
    - path: "supabase/migrations/00023_fleet_management.sql"
      provides: "ALTER vehicles + 4 new tables with RLS"
      contains: "maintenance_records"
    - path: "src/types/database.ts"
      provides: "Fleet TypeScript types"
      contains: "MaintenanceRecord"
    - path: "src/schemas/fleet.ts"
      provides: "Zod schemas for maintenance, fuel, assignments"
      exports: ["maintenanceRecordSchema", "fuelTransactionSchema", "maintenanceScheduleSchema"]
    - path: "src/lib/fleet/fleet-helpers.ts"
      provides: "Cost per mile calculation, fleet utilities"
      exports: ["calculateCostPerMile", "calculateFleetCostPerMile"]
  key_links:
    - from: "src/schemas/fleet.ts"
      to: "src/types/database.ts"
      via: "type alignment"
      pattern: "MaintenanceType|FuelType|VehicleClass"
    - from: "src/lib/fleet/fleet-helpers.ts"
      to: "src/types/database.ts"
      via: "type imports"
      pattern: "MaintenanceRecord|FuelTransaction"
---

<objective>
Create the fleet management database schema (expand vehicles table, add maintenance_records, maintenance_schedules, fuel_transactions, vehicle_assignments), TypeScript types, Zod validation schemas, fleet helper utilities (cost-per-mile calculation), and StatusBadge extensions.

Purpose: Foundation layer that all fleet UI, server actions, and the maintenance monitor depend on.
Output: Migration file, types, schemas, helpers with tests, StatusBadge vehicle status variant.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fleet-management/08-CONTEXT.md

@src/types/database.ts
@src/schemas/vehicle.ts
@src/components/ui/status-badge.tsx
@supabase/migrations/00021_compliance_tables.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration, TypeScript types, and Zod schemas</name>
  <files>supabase/migrations/00023_fleet_management.sql, src/types/database.ts, src/schemas/vehicle.ts, src/schemas/fleet.ts</files>
  <behavior>
    - vehicleSchema accepts expanded vehicle_type enum (medical_van, sprinter, box_truck, hotshot, straight_truck, day_cab, sleeper, flatbed, reefer, tanker, dry_van_trailer, flatbed_trailer, reefer_trailer, step_deck_trailer, other)
    - vehicleSchema accepts vehicle_class enum (class_1_2, class_3_4, class_5_6, class_7, class_8, trailer, other)
    - vehicleSchema accepts expanded status enum (active, in_shop, out_of_service, parked, sold, totaled)
    - vehicleSchema accepts fuel_type (diesel, gasoline, cng, electric, hybrid)
    - vehicleSchema accepts optional fields: license_plate, license_state, registration_expiry, current_odometer, avg_mpg, purchase_date, purchase_price, current_value, insurance_policy, photo_urls, notes
    - maintenanceRecordSchema validates type enum (oil_change, tire_rotation, tire_replacement, brake_service, transmission, engine, electrical, hvac, body_work, dot_inspection, preventive, recall, roadside_repair, scheduled_service, unscheduled_repair, other)
    - maintenanceRecordSchema requires vehicle_id, maintenance_type, description, date_in; optional vendor, costs, odometer, warranty, date_out, next_service fields
    - fuelTransactionSchema requires vehicle_id, transaction_date, gallons, total_cost; optional driver_id, location, city, state, price_per_gallon, odometer_reading, receipt_url, source
    - maintenanceScheduleSchema validates interval_miles or interval_days (at least one required), priority enum
  </behavior>
  <action>
    1. Create migration 00023_fleet_management.sql:
       - ALTER TABLE vehicles: add vehicle_class TEXT NOT NULL DEFAULT 'other' with CHECK, expand vehicle_type CHECK to include all PRD types (medical_van, sprinter, box_truck, hotshot, straight_truck, day_cab, sleeper, flatbed, reefer, tanker, dry_van_trailer, flatbed_trailer, reefer_trailer, step_deck_trailer, other), expand status CHECK to (active, in_shop, out_of_service, parked, sold, totaled), add fuel_type TEXT DEFAULT 'diesel' with CHECK, add fields: license_plate, license_state, registration_expiry DATE, current_odometer INTEGER, odometer_updated_at TIMESTAMPTZ, avg_mpg NUMERIC(5,2), purchase_date DATE, purchase_price NUMERIC(12,2), current_value NUMERIC(12,2), insurance_policy TEXT, gps_device_id TEXT, eld_provider TEXT, eld_device_id TEXT, photo_urls TEXT[], notes TEXT. Use ALTER TABLE ADD COLUMN for each new column; use ALTER TABLE DROP CONSTRAINT + ADD CONSTRAINT for expanding CHECK constraints on vehicle_type and status.
       - CREATE TABLE maintenance_records per PRD 3.2 with downtime_days as GENERATED STORED column, RLS org_isolation using auth.org_id()
       - CREATE TABLE maintenance_schedules per PRD 3.2, vehicle_id nullable (null = org-wide), RLS
       - CREATE TABLE fuel_transactions per PRD 3.2, RLS
       - CREATE TABLE vehicle_assignments per PRD 3.2, RLS
       - All RLS policies follow existing pattern: USING (org_id = (select auth.org_id()))

    2. Update src/types/database.ts:
       - Replace VehicleType with expanded enum including all new types
       - Replace VehicleStatus with expanded enum: 'active' | 'in_shop' | 'out_of_service' | 'parked' | 'sold' | 'totaled'
       - Add VehicleClass type: 'class_1_2' | 'class_3_4' | 'class_5_6' | 'class_7' | 'class_8' | 'trailer' | 'other'
       - Add FuelType: 'diesel' | 'gasoline' | 'cng' | 'electric' | 'hybrid'
       - Add MaintenanceType enum with all 16 types
       - Add FuelSource: 'manual' | 'fuel_card' | 'eld' | 'receipt_scan'
       - Add MaintenancePriority: 'low' | 'normal' | 'high' | 'critical'
       - Expand Vehicle type with all new fields (vehicle_class, fuel_type, license_plate, license_state, registration_expiry, current_odometer, odometer_updated_at, avg_mpg, purchase_date, purchase_price, current_value, insurance_policy, gps_device_id, eld_provider, eld_device_id, photo_urls, notes, current_driver_id)
       - Add MaintenanceRecord, MaintenanceSchedule, FuelTransaction, VehicleAssignment types
       - Add Database table entries for all new tables with Row/Insert/Update types following existing patterns (use z.input pattern for Insert)

    3. Update src/schemas/vehicle.ts:
       - Expand vehicleSchema with new fields: vehicle_class (required, default 'other'), fuel_type (optional, default 'diesel'), license_plate (optional), license_state (optional), registration_expiry (optional date string), current_odometer (optional coerced number), avg_mpg (optional coerced number), purchase_date (optional), purchase_price (optional coerced number), current_value (optional coerced number), insurance_policy (optional), notes (optional)
       - Update vehicle_type enum to match expanded list
       - Update status enum to match expanded list
       - Use z.input for VehicleInput type (per project convention)

    4. Create src/schemas/fleet.ts:
       - maintenanceRecordSchema: vehicle_id (uuid string), maintenance_type (enum), description (min 1), vendor_name (optional), vendor_location (optional), odometer_at_service (optional coerced number), cost_parts (optional coerced number, default 0), cost_labor (optional coerced number, default 0), cost_total (optional coerced number, default 0), warranty_covered (optional boolean, default false), date_in (required date string), date_out (optional date string), document_urls (optional string array), next_service_odometer (optional coerced number), next_service_date (optional date string)
       - maintenanceScheduleSchema: vehicle_id (optional uuid), vehicle_class (optional enum), maintenance_type (required enum), interval_miles (optional coerced number), interval_days (optional coerced number), description (optional), estimated_cost (optional coerced number), priority (enum, default 'normal'), active (boolean, default true). Add refine: at least one of interval_miles or interval_days must be provided.
       - fuelTransactionSchema: vehicle_id (uuid), driver_id (optional uuid), transaction_date (required date string), location (optional), city (optional), state (optional), gallons (required coerced number > 0), price_per_gallon (optional coerced number), total_cost (required coerced number > 0), odometer_reading (optional coerced number), source (enum, default 'manual')
       - vehicleAssignmentSchema: vehicle_id (uuid), driver_id (uuid), reason (optional string)
       - Export input types: MaintenanceRecordInput, MaintenanceScheduleInput, FuelTransactionInput, VehicleAssignmentInput
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Migration file creates/alters all tables, types compile, schemas validate correct inputs and reject invalid ones</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fleet helpers (cost-per-mile, fleet utilities) and StatusBadge extension</name>
  <files>src/lib/fleet/fleet-helpers.ts, tests/fleet/fleet-helpers.test.ts, src/components/ui/status-badge.tsx</files>
  <behavior>
    - calculateCostPerMile({ maintenanceCosts, fuelCosts, insuranceCostPerMonth, purchasePrice, currentValue, monthsOwned, totalMiles }) returns cost per mile as number rounded to 2 decimal places
    - calculateCostPerMile returns 0 when totalMiles is 0
    - calculateCostPerMile includes depreciation: (purchasePrice - currentValue) / totalMiles when both values provided
    - calculateFleetCostPerMile(vehicles[]) aggregates all vehicle costs and returns fleet-wide cost per mile
    - calculateMPG(miles, gallons) returns miles per gallon, 0 when gallons is 0
    - getMaintenanceStatus(lastServiceDate, nextServiceDate, nextServiceOdometer, currentOdometer) returns 'overdue' | 'due_soon' | 'ok'
    - StatusBadge renders vehicle status variants with correct colors: active=green, in_shop=yellow, out_of_service=red, parked=gray, sold=gray, totaled=red
  </behavior>
  <action>
    1. Create src/lib/fleet/fleet-helpers.ts:
       - calculateCostPerMile({ maintenanceCosts: number, fuelCosts: number, insuranceCostPerMonth?: number, purchasePrice?: number, currentValue?: number, monthsOwned?: number, totalMiles: number }): number — returns total cost / totalMiles, includes depreciation when purchase/current value provided, includes prorated insurance, returns 0 for 0 miles
       - calculateFleetCostPerMile(vehicleCosts: Array<{ maintenanceCosts: number, fuelCosts: number, totalMiles: number }>): number — sums all costs / sums all miles
       - calculateMPG(totalMiles: number, totalGallons: number): number — simple division, 0 guard
       - getMaintenanceStatus(params: { lastServiceDate?: string, nextServiceDate?: string, nextServiceOdometer?: number, currentOdometer?: number }): 'overdue' | 'due_soon' | 'ok' — overdue if past nextServiceDate or past nextServiceOdometer, due_soon if within 30 days or 3000 miles
       - formatCurrency(amount: number): string — returns "$X,XXX.XX" format
       - VEHICLE_CLASS_LABELS: Record<VehicleClass, string> mapping class_1_2 -> "Class 1-2 (Light)" etc.
       - MAINTENANCE_TYPE_LABELS: Record<MaintenanceType, string> for human-readable labels

    2. Create tests/fleet/fleet-helpers.test.ts:
       - Test calculateCostPerMile with known inputs
       - Test 0-mile guard returns 0
       - Test with and without depreciation/insurance
       - Test calculateFleetCostPerMile aggregation
       - Test calculateMPG with normal and 0 gallons
       - Test getMaintenanceStatus for each status outcome

    3. Update src/components/ui/status-badge.tsx:
       - Add 'vehicle' variant to StatusBadge alongside existing load/driver/dispatch/compliance variants
       - Colors: active=green, in_shop=yellow, out_of_service=red, parked=gray, sold=gray, totaled=red
       - Labels: active="Active", in_shop="In Shop", out_of_service="Out of Service", parked="Parked", sold="Sold", totaled="Totaled"
  </action>
  <verify>
    <automated>npx vitest run tests/fleet/fleet-helpers.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Cost-per-mile calculations are accurate, MPG helper works, maintenance status detection is correct, StatusBadge renders vehicle statuses with appropriate colors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx vitest run tests/fleet/` passes all fleet helper tests
- Migration file contains ALTER TABLE vehicles and CREATE TABLE for 4 new tables
- Database.ts contains MaintenanceRecord, FuelTransaction, MaintenanceSchedule, VehicleAssignment types
</verification>

<success_criteria>
- All fleet tables defined in migration with RLS policies
- TypeScript types compile cleanly
- Zod schemas validate/reject correct inputs
- Cost-per-mile and MPG helpers tested and passing
- StatusBadge supports vehicle status variant
</success_criteria>

<output>
After completion, create `.planning/phases/08-fleet-management/08-01-SUMMARY.md`
</output>
