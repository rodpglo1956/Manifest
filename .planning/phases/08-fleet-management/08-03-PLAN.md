---
phase: 08-fleet-management
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/vehicles/vehicle-form.tsx
  - src/components/vehicles/vehicle-list.tsx
  - src/app/(app)/fleet/page.tsx
  - src/app/(app)/fleet/new/page.tsx
  - src/app/(app)/fleet/[id]/page.tsx
  - src/app/(app)/fleet/[id]/edit/page.tsx
autonomous: true
requirements: [FLET-01, FLET-02]

must_haves:
  truths:
    - "Vehicle form captures class, fuel type, odometer, purchase info, registration, and notes"
    - "Vehicle list shows expanded status badges and key info (class, driver, next service)"
    - "Vehicle detail page shows all info with tabs for maintenance history, fuel stats, cost breakdown, and assignment history"
    - "Vehicle status can be changed from detail page"
  artifacts:
    - path: "src/components/vehicles/vehicle-form.tsx"
      provides: "Expanded vehicle form with all fleet fields"
      min_lines: 200
    - path: "src/app/(app)/fleet/[id]/page.tsx"
      provides: "Vehicle detail page with tabs"
      min_lines: 100
    - path: "src/components/vehicles/vehicle-list.tsx"
      provides: "Updated vehicle list with expanded columns"
      min_lines: 50
  key_links:
    - from: "src/components/vehicles/vehicle-form.tsx"
      to: "src/schemas/vehicle.ts"
      via: "zodResolver import"
      pattern: "vehicleSchema"
    - from: "src/app/(app)/fleet/[id]/page.tsx"
      to: "src/lib/fleet/actions.ts"
      via: "server action calls"
      pattern: "getMaintenanceRecords|getFuelTransactions|getVehicleCostPerMile"
---

<objective>
Expand the vehicle form with all new fields (class, fuel type, odometer, registration, purchase info), update the vehicle list to show expanded status and info, and create a vehicle detail page with maintenance/fuel/cost/assignment tabs.

Purpose: Full vehicle lifecycle UI for Command mode fleet management.
Output: Expanded form, updated list, new detail page with tabbed sections.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fleet-management/08-CONTEXT.md

@src/components/vehicles/vehicle-form.tsx
@src/components/vehicles/vehicle-list.tsx
@src/app/(app)/fleet/page.tsx
@src/app/(app)/fleet/new/page.tsx
@src/schemas/vehicle.ts
@src/types/database.ts
</context>

<interfaces>
From src/schemas/vehicle.ts (updated in 08-01):
```typescript
export const vehicleSchema // expanded with vehicle_class, fuel_type, license_plate, etc.
export type VehicleInput = z.input<typeof vehicleSchema>
```

From src/lib/fleet/fleet-helpers.ts (created in 08-01):
```typescript
export const VEHICLE_CLASS_LABELS: Record<VehicleClass, string>
export const MAINTENANCE_TYPE_LABELS: Record<MaintenanceType, string>
export function formatCurrency(amount: number): string
```

From src/lib/fleet/actions.ts (created in 08-02):
```typescript
export async function getMaintenanceRecords(vehicleId?: string)
export async function getFuelTransactions(vehicleId?: string)
export async function getVehicleCostPerMile(vehicleId: string)
export async function getVehicleAssignmentHistory(vehicleId: string)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand vehicle form and update vehicle list</name>
  <files>src/components/vehicles/vehicle-form.tsx, src/components/vehicles/vehicle-list.tsx, src/app/(app)/fleet/new/page.tsx, src/app/(app)/fleet/[id]/edit/page.tsx</files>
  <action>
    1. Rewrite src/components/vehicles/vehicle-form.tsx:
       - Keep existing react-hook-form + zodResolver pattern
       - Organize fields in sections with headers:
         Section "Vehicle Info": unit_number, vin, year, make, model (keep existing layout)
         Section "Classification": vehicle_class (select from VEHICLE_CLASS_LABELS), vehicle_type (expanded select with all PRD types), fuel_type (select)
         Section "Registration": license_plate, license_state (select US states), registration_expiry (date input)
         Section "Odometer & Performance": current_odometer (number), avg_mpg (number, readonly display if available)
         Section "Purchase & Value" (collapsible, open in edit mode): purchase_date, purchase_price (currency input), current_value (currency input), insurance_policy
         Section "Notes": notes textarea
       - Status field: show in edit mode with expanded options (active, in_shop, out_of_service, parked, sold, totaled) using StatusBadge color preview
       - VEHICLE_TYPES constant expanded to match all PRD types with human-readable labels
       - VEHICLE_CLASSES constant from fleet-helpers VEHICLE_CLASS_LABELS
       - Use grid layouts: 2-col for classification row, 3-col for year/make/model, 2-col for license plate/state
       - Update new page and edit page to pass expanded defaultValues

    2. Update src/components/vehicles/vehicle-list.tsx:
       - Add columns: vehicle_class (use VEHICLE_CLASS_LABELS), status (use StatusBadge variant='vehicle'), current_driver (show driver name if assigned)
       - Add status filter buttons at top (All, Active, In Shop, Out of Service, etc.) using searchParams pattern
       - Each row links to /fleet/[id] (detail page, not edit)
       - Show quick action buttons: "Edit" link, "Change Status" dropdown
       - Mobile-friendly: hide class and driver columns on small screens

    3. Update src/app/(app)/fleet/new/page.tsx to use expanded form (should already work if form is updated)

    4. Update src/app/(app)/fleet/[id]/edit/page.tsx to fetch all expanded vehicle fields and pass as defaultValues
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Vehicle form has all expanded fields organized in sections, vehicle list shows class/status/driver with filters, edit page pre-fills all fields</done>
</task>

<task type="auto">
  <name>Task 2: Vehicle detail page with tabbed maintenance/fuel/cost/assignment sections</name>
  <files>src/app/(app)/fleet/[id]/page.tsx, src/app/(app)/fleet/page.tsx</files>
  <action>
    1. Create src/app/(app)/fleet/[id]/page.tsx as a server component:
       - Fetch vehicle by id with all expanded fields
       - Fetch maintenance records, fuel transactions (last 20), cost-per-mile data, assignment history via server actions from src/lib/fleet/actions.ts
       - Layout:
         Top section: Vehicle info card with photo placeholder, unit number, year/make/model, class badge, status badge, current driver (link to driver detail), VIN, license plate, registration expiry
         Tab navigation (using URL searchParams ?tab=maintenance|fuel|costs|assignments, default 'maintenance'):
           "Maintenance" tab: List of maintenance records (type, vendor, cost, dates, downtime), "Add Maintenance" button linking to inline form
           "Fuel" tab: List of fuel transactions (date, location, gallons, cost, odometer), MPG display, "Add Fuel" button
           "Costs" tab: Cost-per-mile breakdown card (maintenance, fuel, depreciation, insurance, total), cost trending if data available
           "History" tab: Assignment history timeline (driver name, dates, reason)
         Quick actions row: Edit Vehicle, Change Status, Assign Driver, Log Maintenance, Log Fuel
       - Use Tailwind card styling consistent with existing detail pages (load-detail, driver-detail patterns)

    2. Update src/app/(app)/fleet/page.tsx:
       - Change to serve as fleet dashboard (will be fully built in 08-04, but prepare the redirect/layout):
         Keep current vehicle list as the default view
         Update the select query to include all expanded vehicle fields
         Update heading from "Fleet" to "Fleet" with subtitle showing active/in_shop/out_of_service counts
         Add links to /fleet/maintenance and /fleet/fuel (pages created in 08-04)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Vehicle detail page renders with all info and 4 tabs, fleet page shows expanded vehicle data with status counts</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Vehicle form renders all sections with correct input types
- Vehicle list shows status badges and class labels
- Vehicle detail page compiles with tabbed layout
</verification>

<success_criteria>
- Full vehicle CRUD UI with all PRD fields including class, fuel type, registration, odometer, purchase info
- Vehicle detail page shows maintenance history, fuel stats, cost breakdown, and assignment history in tabs
- Status filter on vehicle list works
- All pages follow existing Tailwind/component patterns
</success_criteria>

<output>
After completion, create `.planning/phases/08-fleet-management/08-03-SUMMARY.md`
</output>
