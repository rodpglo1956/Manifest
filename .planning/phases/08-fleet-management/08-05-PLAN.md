---
phase: 08-fleet-management
plan: 05
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/app/driver/vehicle/page.tsx
  - src/components/fleet/driver-vehicle-panel.tsx
  - src/components/fleet/driver-fuel-form.tsx
  - src/components/fleet/driver-issue-report.tsx
  - src/app/oo/vehicle/page.tsx
  - src/components/fleet/oo-vehicle-dashboard.tsx
autonomous: true
requirements: [FLET-10]

must_haves:
  truths:
    - "Driver PWA shows assigned vehicle info with unit number, year, make, model, status"
    - "Driver can report a vehicle issue from PWA (creates maintenance request)"
    - "Driver can log a fuel transaction from PWA"
    - "Owner-Operator sees single vehicle dashboard with maintenance log, fuel tracking, cost of ownership"
  artifacts:
    - path: "src/app/driver/vehicle/page.tsx"
      provides: "Driver PWA vehicle page"
      min_lines: 30
    - path: "src/components/fleet/driver-vehicle-panel.tsx"
      provides: "Driver's assigned vehicle info display"
      min_lines: 50
    - path: "src/components/fleet/driver-fuel-form.tsx"
      provides: "Simplified fuel log form for drivers"
      min_lines: 50
    - path: "src/components/fleet/driver-issue-report.tsx"
      provides: "Issue report form creating maintenance request"
      min_lines: 50
    - path: "src/app/oo/vehicle/page.tsx"
      provides: "Owner-Operator vehicle dashboard"
      min_lines: 30
  key_links:
    - from: "src/app/driver/vehicle/page.tsx"
      to: "src/lib/fleet/actions.ts"
      via: "server action imports"
      pattern: "createFuelTransaction|createMaintenanceRecord"
    - from: "src/app/oo/vehicle/page.tsx"
      to: "src/lib/fleet/actions.ts"
      via: "server action imports"
      pattern: "getVehicleCostPerMile|getMaintenanceRecords|getFuelTransactions"
---

<objective>
Create the Driver PWA vehicle page (assigned vehicle info, report issue button, fuel log entry) and Owner-Operator vehicle dashboard (single vehicle with maintenance, fuel, cost of ownership).

Purpose: Mobile-first vehicle tools for drivers and single-vehicle view for owner-operators.
Output: Driver vehicle page with issue reporting and fuel logging, OO vehicle dashboard.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fleet-management/08-CONTEXT.md

@src/app/driver/layout.tsx
@src/lib/fleet/actions.ts
@src/lib/fleet/fleet-helpers.ts
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver PWA vehicle page with issue reporting and fuel logging</name>
  <files>src/app/driver/vehicle/page.tsx, src/components/fleet/driver-vehicle-panel.tsx, src/components/fleet/driver-fuel-form.tsx, src/components/fleet/driver-issue-report.tsx</files>
  <action>
    1. Create src/app/driver/vehicle/page.tsx (server component):
       - Authenticate user, get profile, find driver record by user_id
       - Fetch vehicle by driver.current_vehicle_id. If no assigned vehicle, show "No vehicle assigned" message with explanation.
       - Fetch recent fuel transactions for this vehicle by this driver (last 10)
       - Render DriverVehiclePanel with vehicle data, fuel form, and issue report form
       - Add to driver PWA bottom nav if not already present (check driver layout.tsx for nav pattern)

    2. Create src/components/fleet/driver-vehicle-panel.tsx (client component):
       - Vehicle info card (mobile-optimized, full-width):
         Large unit number header
         Year Make Model subtitle
         Status badge (StatusBadge variant='vehicle')
         Vehicle class label
         Key details: VIN (last 6 chars shown), license plate, fuel type, current odometer
         Registration expiry with warning if within 30 days
       - Two action buttons below card:
         "Report Issue" (red/warning style) — toggles DriverIssueReport form
         "Log Fuel" (blue/primary style) — toggles DriverFuelForm
       - Recent fuel logs section: last 5 entries with date, gallons, cost, odometer. Simple list, mobile-friendly cards.
       - Style: mobile-first, touch-friendly buttons (min 44px tap targets), large text for in-cab use

    3. Create src/components/fleet/driver-fuel-form.tsx (client component):
       - Simplified fuel form for drivers (fewer fields than full fleet form):
         gallons (required, number input with step=0.001)
         total_cost (required, currency input)
         price_per_gallon (auto-calculated from total_cost/gallons, shown as display)
         odometer_reading (required for accurate MPG)
         location (optional, text input with placeholder "Station name or address")
         city and state (optional, for IFTA integration)
       - Hidden fields: vehicle_id (from parent), driver_id (from parent), source='manual', transaction_date=now()
       - Submit via createFuelTransaction server action
       - useTransition for pending state, success message on complete, form resets
       - Large touch-friendly inputs for mobile use

    4. Create src/components/fleet/driver-issue-report.tsx (client component):
       - Simple issue report form that creates a maintenance_record with type='unscheduled_repair':
         description (required, textarea, "Describe the issue")
         maintenance_type (select from common issues: tire_replacement, brake_service, engine, electrical, hvac, other)
         Severity selector: "Can still drive" / "Need to stop" (maps to maintenance_schedules priority)
       - Hidden fields: vehicle_id, odometer_at_service (current vehicle odometer), date_in=today, cost fields left at 0 (admin fills in later)
       - Also creates a proactive_alert (alert_type='maintenance_due', severity='warning' or 'critical' based on severity selection, title='Driver reported issue on [unit_number]') so dispatchers/Marie see it
       - Submit via createMaintenanceRecord + separate alert creation
       - Success: "Issue reported. Your dispatcher has been notified."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Driver PWA vehicle page shows assigned vehicle info, fuel log form works with simplified fields, issue report creates maintenance record and alert</done>
</task>

<task type="auto">
  <name>Task 2: Owner-Operator vehicle dashboard</name>
  <files>src/app/oo/vehicle/page.tsx, src/components/fleet/oo-vehicle-dashboard.tsx</files>
  <action>
    1. Create src/app/oo/vehicle/page.tsx (server component):
       - Authenticate user, get profile, find driver record by user_id (OO pattern: org_members count === 1)
       - Fetch vehicle by driver.current_vehicle_id. If no vehicle, show prompt to add one via /fleet/new.
       - Fetch maintenance records, fuel transactions (last 30), cost-per-mile data, assignment history
       - Render OOVehicleDashboard component

    2. Create src/components/fleet/oo-vehicle-dashboard.tsx (client component):
       - Vehicle info card at top (same as driver panel but desktop-friendly layout)
       - Stat cards row: Cost Per Mile, Avg MPG, Total Maintenance (last 12 months), Total Fuel (last 12 months)
       - Maintenance Log section:
         List of maintenance records (date, type label, vendor, cost, downtime)
         "Add Maintenance" inline form (uses full maintenanceRecordSchema since OO manages their own vehicle)
         Next scheduled maintenance highlighted if exists
       - Fuel Tracking section:
         Recent fuel transactions list
         MPG display with trend indicator (up/down arrow vs previous month)
         "Log Fuel" inline form (same as driver fuel form but with more fields)
       - Total Cost of Ownership section:
         Breakdown card: purchase price, depreciation, total maintenance, total fuel, insurance, total cost
         Cost per mile in large format
         Monthly average cost
       - Use consistent card styling with other OO pages (oo/compliance pattern)
       - Link to DVIR/pre-trip inspection at /driver/compliance (already exists from Phase 7)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>OO vehicle dashboard shows single vehicle with stats, maintenance log, fuel tracking, and total cost of ownership breakdown</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Driver PWA vehicle page compiles with all 3 sub-components
- OO vehicle dashboard compiles with stat cards and inline forms
- Issue report creates both maintenance record and proactive alert
</verification>

<success_criteria>
- Driver sees assigned vehicle with key info in mobile-friendly layout
- Driver can report issues (creates maintenance request + alert for dispatcher)
- Driver can log fuel with simplified form (gallons, cost, odometer, location)
- Owner-Operator sees comprehensive single-vehicle dashboard with maintenance, fuel, and TCO
- All forms use server actions with proper validation
</success_criteria>

<output>
After completion, create `.planning/phases/08-fleet-management/08-05-SUMMARY.md`
</output>
