---
phase: 12-onboarding-pwa-security-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00032_onboarding_progress.sql
  - src/types/database.ts
  - src/lib/onboarding/schemas.ts
  - src/lib/onboarding/actions.ts
  - src/app/(auth)/onboarding/page.tsx
  - src/app/(auth)/onboarding/layout.tsx
  - src/components/onboarding/wizard.tsx
  - src/components/onboarding/steps/business-profile.tsx
  - src/components/onboarding/steps/first-vehicle.tsx
  - src/components/onboarding/steps/first-driver.tsx
  - src/components/onboarding/steps/integrations.tsx
  - src/components/onboarding/steps/plan-selection.tsx
autonomous: true
requirements:
  - ONBD-01
  - ONBD-04

must_haves:
  truths:
    - "New user after signup lands on 5-step onboarding wizard instead of empty dashboard"
    - "Wizard collects business profile, first vehicle, first driver, integrations, and plan selection"
    - "Onboarding progress is tracked per org in onboarding_progress table"
    - "Owner-operator skips the first-driver step"
  artifacts:
    - path: "supabase/migrations/00032_onboarding_progress.sql"
      provides: "onboarding_progress table with RLS"
      contains: "onboarding_progress"
    - path: "src/components/onboarding/wizard.tsx"
      provides: "Multi-step wizard container with progress bar"
      min_lines: 60
    - path: "src/lib/onboarding/actions.ts"
      provides: "Server actions for saving each wizard step"
      exports: ["saveBusinessProfile", "saveFirstVehicle", "saveFirstDriver", "saveIntegrations", "savePlanSelection", "getOnboardingProgress"]
  key_links:
    - from: "src/components/onboarding/wizard.tsx"
      to: "src/lib/onboarding/actions.ts"
      via: "server action calls per step"
      pattern: "saveBusinessProfile|saveFirstVehicle|saveFirstDriver"
    - from: "src/lib/onboarding/actions.ts"
      to: "supabase"
      via: "database inserts for org, vehicle, driver, onboarding_progress"
      pattern: "supabase.*from.*onboarding_progress"
---

<objective>
Create the onboarding wizard that guides new users through a 5-step setup after signup: business profile, first vehicle, first driver (skip for OO), integration check, and plan selection. Track progress in a new onboarding_progress table.

Purpose: New users get a guided setup experience instead of an empty dashboard, reducing time-to-value and drop-off.
Output: Database migration, Zod schemas, server actions, and full wizard UI with 5 steps.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-pwa-security-polish/12-CONTEXT.md
@src/app/(auth)/onboarding/page.tsx
@src/types/database.ts
@src/lib/billing/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding database migration, types, and server actions</name>
  <files>
    supabase/migrations/00032_onboarding_progress.sql,
    src/types/database.ts,
    src/lib/onboarding/schemas.ts,
    src/lib/onboarding/actions.ts
  </files>
  <action>
    1. Create migration 00032_onboarding_progress.sql:
       - onboarding_progress table per PRD Section 5.3: id, org_id (UNIQUE FK to organizations), step_completed INTEGER DEFAULT 0, business_profile_done BOOLEAN DEFAULT false, first_vehicle_done BOOLEAN DEFAULT false, first_driver_done BOOLEAN DEFAULT false, integrations_done BOOLEAN DEFAULT false, plan_selected BOOLEAN DEFAULT false, checklist_dismissed BOOLEAN DEFAULT false, completed_at TIMESTAMPTZ, created_at/updated_at defaults
       - Enable RLS with org_isolation policy using auth.org_id() function (established pattern from Phase 1)
       - INSERT trigger: auto-create onboarding_progress row when organization is created (similar to billing_accounts pattern from Phase 10)

    2. Update src/types/database.ts: Add OnboardingProgress type with Row/Insert/Update variants following existing pattern

    3. Create src/lib/onboarding/schemas.ts:
       - businessProfileSchema: company name (required), address fields, carrier_type enum, dot_number (optional), fleet_size_range enum (1-5, 6-20, 21-50, 51+)
       - firstVehicleSchema: year, make, model, vin (optional), unit_number
       - firstDriverSchema: first_name, last_name, cdl_number (optional), phone, email
       - Use z.input for form types (per project convention)

    4. Create src/lib/onboarding/actions.ts with server actions:
       - getOnboardingProgress(): fetch current org's onboarding state
       - saveBusinessProfile(data): update organization fields + set business_profile_done=true, step_completed=1
       - saveFirstVehicle(data): insert vehicle record + set first_vehicle_done=true, step_completed=2
       - saveFirstDriver(data): insert driver record + optionally assign to vehicle + set first_driver_done=true, step_completed=3
       - skipFirstDriver(): set first_driver_done=true, step_completed=3 (for OO)
       - saveIntegrations(data): set integrations_done=true, step_completed=4 (placeholder, just marks done)
       - savePlanSelection(data): set plan_selected=true, step_completed=5, completed_at=now()
       - completeOnboarding(): set completed_at, redirect to /dashboard
       - Use getAuthContext() pattern from Phase 8 for auth + org check
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Migration file exists with onboarding_progress table + RLS, types updated, schemas validate wizard input, server actions handle all 5 steps</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding wizard UI with 5-step flow</name>
  <files>
    src/app/(auth)/onboarding/page.tsx,
    src/app/(auth)/onboarding/layout.tsx,
    src/components/onboarding/wizard.tsx,
    src/components/onboarding/steps/business-profile.tsx,
    src/components/onboarding/steps/first-vehicle.tsx,
    src/components/onboarding/steps/first-driver.tsx,
    src/components/onboarding/steps/integrations.tsx,
    src/components/onboarding/steps/plan-selection.tsx
  </files>
  <action>
    1. Create src/app/(auth)/onboarding/layout.tsx: Clean centered layout with Manifest logo, no sidebar. Similar to existing auth layout but wider (max-w-2xl) for wizard content.

    2. Replace src/app/(auth)/onboarding/page.tsx: Server component that fetches onboarding progress via getOnboardingProgress(). If completed_at is set, redirect to /dashboard. Otherwise render Wizard with current step.

    3. Create src/components/onboarding/wizard.tsx:
       - Client component managing current step state (1-5)
       - Progress bar at top showing step names and completion: Business Profile, First Vehicle, First Driver, Integrations, Plan Selection
       - Step navigation: Next/Back buttons, skip button where appropriate
       - Detects OO mode (org_members count === 1 or carrier_type check) to skip step 3
       - Each step is a separate component rendered conditionally
       - Calls server actions on step completion, advances to next step on success

    4. Create step components (all client components with react-hook-form + zodResolver):
       - business-profile.tsx: Company name, address (street, city, state, zip), carrier_type dropdown (medical_transport, trucking, mixed_fleet, courier, other), DOT number (optional), fleet size range radio buttons. Pre-populate from existing org data if available.
       - first-vehicle.tsx: Year (number), make, model, VIN (optional), unit_number. Simple single-vehicle form. Success shows confirmation with vehicle summary.
       - first-driver.tsx: First name, last name, CDL number (optional), phone, email. Checkbox to assign to vehicle from step 2. Skip button for OO.
       - integrations.tsx: Three cards (ELD, Fuel Card, Accounting) each with provider dropdown and "Skip for now" link. All skippable. This is a placeholder step marking future integration points.
       - plan-selection.tsx: Reuse plan comparison data from src/lib/billing/config.ts. Show plan cards with features. Highlight recommended plan based on fleet size from step 1. "Start Free Trial" (14-day Professional) as primary CTA, "Start with Free" as secondary.

    5. Style all with Tailwind: clean white cards, subtle shadows, step indicators with checkmarks for completed steps. Mobile responsive.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>5-step onboarding wizard renders at /onboarding, each step collects data via form, server actions save to DB, OO users skip driver step, completion redirects to dashboard</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Onboarding wizard renders with 5 steps and progress bar
- Each step form validates with Zod schemas
- Server actions save data to correct tables
- OO users see 4 steps (driver step skipped)
- Completion sets completed_at and redirects to /dashboard
</verification>

<success_criteria>
- New user after signup sees guided wizard instead of empty dashboard
- All 5 wizard steps function: business profile, vehicle, driver, integrations, plan
- Onboarding progress tracked in onboarding_progress table
- Owner-operators skip the first-driver step
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-pwa-security-polish/12-01-SUMMARY.md`
</output>
