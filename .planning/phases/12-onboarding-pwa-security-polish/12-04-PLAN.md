---
phase: 12-onboarding-pwa-security-polish
plan: 04
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/onboarding/getting-started-checklist.tsx
  - src/app/(command)/dashboard/page.tsx
  - src/lib/onboarding/actions.ts
  - src/app/(auth)/onboarding/driver/page.tsx
  - src/components/onboarding/driver-onboarding.tsx
  - src/lib/middleware/routing.ts
autonomous: true
requirements:
  - ONBD-02
  - ONBD-03

must_haves:
  truths:
    - "Getting Started checklist widget appears on dashboard until dismissed"
    - "Checklist tracks: add vehicle, add driver, create first load, set up compliance, invite team member"
    - "Driver onboarding flow covers invite, PWA install prompt, account creation, first pre-trip tutorial"
    - "Post-signup routing directs to onboarding wizard if not completed"
  artifacts:
    - path: "src/components/onboarding/getting-started-checklist.tsx"
      provides: "Dashboard widget showing setup completion tasks"
      min_lines: 40
    - path: "src/app/(auth)/onboarding/driver/page.tsx"
      provides: "Driver-specific onboarding page"
      min_lines: 20
  key_links:
    - from: "src/app/(command)/dashboard/page.tsx"
      to: "src/components/onboarding/getting-started-checklist.tsx"
      via: "conditionally rendered when checklist not dismissed"
      pattern: "GettingStartedChecklist"
    - from: "src/lib/middleware/routing.ts"
      to: "onboarding_progress"
      via: "redirect to /onboarding if not completed"
      pattern: "onboarding|redirect"
---

<objective>
Add the Getting Started checklist widget to the dashboard and create the driver onboarding flow. Update routing to direct new users to the onboarding wizard.

Purpose: Users see progress toward full setup, drivers get a smooth first-time experience with PWA install and tutorial.
Output: Dashboard checklist widget, driver onboarding page, routing updates.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-pwa-security-polish/12-CONTEXT.md
@.planning/phases/12-onboarding-pwa-security-polish/12-01-SUMMARY.md
@src/app/(command)/dashboard/page.tsx
@src/lib/middleware/routing.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Getting Started checklist widget on dashboard</name>
  <files>
    src/components/onboarding/getting-started-checklist.tsx,
    src/app/(command)/dashboard/page.tsx,
    src/lib/onboarding/actions.ts
  </files>
  <action>
    1. Add to src/lib/onboarding/actions.ts (append to existing file from Plan 01):
       - getChecklistStatus(): queries org data to determine completion of each checklist item:
         * "Add a vehicle" — check vehicles count > 0
         * "Add a driver" — check drivers count > 0 (skip for OO)
         * "Create first load" — check loads count > 0
         * "Set up compliance profile" — check compliance_profiles exists
         * "Invite a team member" — check org_members count > 1 (skip for OO)
       - Returns array of {label, completed, href} items
       - dismissChecklist(): set checklist_dismissed=true on onboarding_progress

    2. Create src/components/onboarding/getting-started-checklist.tsx:
       - Client component receiving checklist items and dismissed state
       - Card with "Getting Started" header, progress indicator (e.g., "3 of 5 complete")
       - List of items with checkmark icons for completed, arrow links for incomplete
       - Each incomplete item links to the relevant page (e.g., /fleet for "Add a vehicle")
       - Dismiss button (X or "Dismiss") that calls dismissChecklist server action
       - Celebratory state when all items complete ("You're all set!" with dismiss)
       - Tailwind: white card, shadow-sm, rounded-lg, consistent with dashboard stat cards

    3. Update src/app/(command)/dashboard/page.tsx:
       - Import and conditionally render GettingStartedChecklist at the top of the dashboard
       - Only show if onboarding_progress exists AND checklist_dismissed is false
       - Fetch checklist data server-side via getChecklistStatus()
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Checklist widget shows on dashboard with real-time completion tracking, links to relevant pages, dismissible, hidden when all tasks done or dismissed</done>
</task>

<task type="auto">
  <name>Task 2: Driver onboarding flow and routing updates</name>
  <files>
    src/app/(auth)/onboarding/driver/page.tsx,
    src/components/onboarding/driver-onboarding.tsx,
    src/lib/middleware/routing.ts
  </files>
  <action>
    1. Create src/components/onboarding/driver-onboarding.tsx:
       - Multi-step flow for drivers (simpler than org wizard):
         * Step 1: Welcome screen with assigned vehicle info and company name
         * Step 2: PWA install prompt — detect if running as PWA (display-mode: standalone media query), if not show "Add to Home Screen" instructions with iOS/Android detection
         * Step 3: Account confirmation — show name, phone, verify details are correct
         * Step 4: First pre-trip tutorial — brief walkthrough of DVIR form with numbered steps and screenshots/icons, link to start first inspection at /driver/fleet (existing DVIR page)
       - Progress dots at top
       - "Next" / "Get Started" buttons
       - Completion marks driver as onboarded (update profile or driver record)

    2. Create src/app/(auth)/onboarding/driver/page.tsx:
       - Server component that fetches driver record for current user
       - If driver already onboarded, redirect to /driver
       - Otherwise render DriverOnboarding with driver and vehicle data

    3. Update src/lib/middleware/routing.ts:
       - In determineRoute(), for authenticated users with org_id:
         * If user has NOT completed onboarding (check onboarding_progress.completed_at is null) AND pathname is not /onboarding/*, redirect to /onboarding
         * For drivers: if not onboarded, redirect to /onboarding/driver
       - Do NOT query onboarding_progress in middleware (too expensive). Instead, check a flag on the profile record. Add a is_onboarded boolean field to profiles or use existing data.
       - Alternative approach: only redirect on /dashboard route, not every route, to avoid middleware complexity. Use a simpler check: if profile.org_id exists but organization has no vehicles (first-time indicator), redirect.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Driver onboarding page works at /onboarding/driver with PWA install prompt and DVIR tutorial. New org users redirected to onboarding wizard. Routing handles first-time detection.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Getting Started checklist renders on dashboard for new orgs
- Checklist items reflect real data (vehicles, drivers, loads exist)
- Dismiss button hides checklist permanently
- Driver onboarding flow shows PWA install prompt and DVIR tutorial
- New users redirected to onboarding before dashboard access
</verification>

<success_criteria>
- Getting Started checklist widget visible on dashboard until dismissed
- Checklist tracks 5 setup milestones with real-time completion
- Driver onboarding covers invite acceptance, PWA install, account setup, first pre-trip tutorial
- Routing directs new users to appropriate onboarding flow
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-pwa-security-polish/12-04-SUMMARY.md`
</output>
