---
phase: 12-onboarding-pwa-security-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00034_white_label_config.sql
  - src/types/database.ts
  - src/lib/white-label/config.ts
  - src/lib/white-label/actions.ts
  - src/lib/white-label/provider.tsx
  - src/app/(command)/settings/white-label/page.tsx
  - src/components/white-label/brand-settings.tsx
autonomous: true
requirements:
  - WLBL-01
  - WLBL-02
  - WLBL-03
  - WLBL-04

must_haves:
  truths:
    - "White-label config table stores brand name, logo, colors, custom domain per org"
    - "CSS custom properties are applied from white-label config at app init"
    - "Brand name and logo are loaded from config, not hardcoded"
    - "White-label settings page is accessible only to enterprise tier orgs"
  artifacts:
    - path: "supabase/migrations/00034_white_label_config.sql"
      provides: "white_label_config table with RLS"
      contains: "white_label_config"
    - path: "src/lib/white-label/provider.tsx"
      provides: "React context provider that injects CSS custom properties"
      exports: ["WhiteLabelProvider", "useWhiteLabel"]
    - path: "src/app/(command)/settings/white-label/page.tsx"
      provides: "White-label settings page for enterprise orgs"
      min_lines: 20
  key_links:
    - from: "src/lib/white-label/provider.tsx"
      to: "src/lib/white-label/actions.ts"
      via: "fetches config and sets CSS variables"
      pattern: "getWhiteLabelConfig"
    - from: "src/app/(command)/settings/white-label/page.tsx"
      to: "src/lib/white-label/actions.ts"
      via: "saves brand settings"
      pattern: "saveWhiteLabelConfig"
---

<objective>
Build white-label infrastructure so enterprise-tier orgs can customize branding: brand name, logo, primary/secondary colors, and custom domain. CSS custom properties system for all brand colors.

Purpose: Enterprise customers can rebrand Manifest as their own product, enabling reseller and agency business models.
Output: Database table, config provider, CSS custom properties, settings page.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding-pwa-security-polish/12-CONTEXT.md
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: White-label database, types, and server actions</name>
  <files>
    supabase/migrations/00034_white_label_config.sql,
    src/types/database.ts,
    src/lib/white-label/config.ts,
    src/lib/white-label/actions.ts
  </files>
  <action>
    1. Create migration 00034_white_label_config.sql per PRD Section 7.2:
       - white_label_config table: id UUID PK, org_id UUID NOT NULL UNIQUE FK organizations, enabled BOOLEAN DEFAULT false, brand_name TEXT, logo_url TEXT, favicon_url TEXT, primary_color TEXT DEFAULT '#EC008C', secondary_color TEXT, custom_domain TEXT, support_email TEXT, support_phone TEXT, created_at/updated_at TIMESTAMPTZ defaults
       - Enable RLS with org_isolation policy using auth.org_id()
       - Do NOT auto-create row on org insert (only enterprise orgs use this)

    2. Update src/types/database.ts: Add WhiteLabelConfig type with Row/Insert/Update variants

    3. Create src/lib/white-label/config.ts:
       - DEFAULT_BRAND constant: { brand_name: 'Manifest', logo_url: '/logo.svg', primary_color: '#EC008C', secondary_color: '#1e3a5f' }
       - CSS_VAR_MAP mapping config fields to CSS custom property names: primary_color -> --brand-primary, secondary_color -> --brand-secondary
       - isEnterprisePlan(plan: string) helper

    4. Create src/lib/white-label/actions.ts:
       - getWhiteLabelConfig(): fetch config for current org, return DEFAULT_BRAND if not enterprise or no config
       - saveWhiteLabelConfig(data): validate enterprise tier via billing_accounts.plan check, upsert white_label_config
       - uploadLogo(formData): upload to Supabase Storage white-label bucket, return URL
       - Use getAuthContext() for auth
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>White-label table exists with RLS, types updated, config defaults defined, server actions handle CRUD with enterprise-tier gating</done>
</task>

<task type="auto">
  <name>Task 2: White-label provider and settings page</name>
  <files>
    src/lib/white-label/provider.tsx,
    src/app/(command)/settings/white-label/page.tsx,
    src/components/white-label/brand-settings.tsx
  </files>
  <action>
    1. Create src/lib/white-label/provider.tsx:
       - WhiteLabelContext with brand_name, logo_url, primary_color, secondary_color
       - WhiteLabelProvider: fetches config via getWhiteLabelConfig() on mount, applies CSS custom properties to document.documentElement.style.setProperty() for --brand-primary, --brand-secondary
       - useWhiteLabel() hook to access brand values in components
       - Falls back to DEFAULT_BRAND if no config
       - Export BrandLogo component that renders logo_url with brand_name alt text
       - Export BrandName component that renders brand_name text

    2. Create src/components/white-label/brand-settings.tsx:
       - Client component with react-hook-form
       - Fields: brand_name (text), logo upload (file input with preview), primary_color (color picker input type="color"), secondary_color (color picker), custom_domain (text), support_email, support_phone
       - Live preview section showing how brand looks (mini header with logo + name + colors)
       - Save button calls saveWhiteLabelConfig
       - Success toast via sonner

    3. Create src/app/(command)/settings/white-label/page.tsx:
       - Server component that checks billing plan
       - If not enterprise: show upgrade prompt card ("White-label branding is available on the Enterprise plan") with link to /settings/billing
       - If enterprise: render BrandSettings component with current config
       - Page title: "White Label Settings"
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>WhiteLabelProvider injects CSS custom properties, BrandLogo and BrandName components replace hardcoded values, settings page allows enterprise orgs to configure branding</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- White-label settings page renders for enterprise orgs, shows upgrade prompt for others
- CSS custom properties applied from config
- BrandLogo and BrandName components work with defaults and custom config
- Logo upload saves to Supabase Storage
</verification>

<success_criteria>
- white_label_config table stores brand customization per org
- CSS custom properties system applies brand colors globally
- Brand name and logo loaded from config (not hardcoded)
- White-label settings restricted to enterprise tier
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-pwa-security-polish/12-03-SUMMARY.md`
</output>
