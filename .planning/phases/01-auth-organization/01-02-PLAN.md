---
phase: 01-auth-organization
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/signup/actions.ts
  - src/app/(auth)/login/actions.ts
  - src/app/(auth)/callback/route.ts
  - src/app/(auth)/onboarding/page.tsx
  - src/app/(auth)/onboarding/actions.ts
  - src/schemas/auth.ts
  - src/schemas/organization.ts
  - src/components/auth/signup-form.tsx
  - src/components/auth/login-form.tsx
  - src/components/auth/magic-link-form.tsx
  - src/components/auth/org-setup-form.tsx
  - tests/auth/signup.test.ts
  - tests/auth/session.test.ts
  - tests/auth/magic-link.test.ts
  - tests/org/create.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-05]

must_haves:
  truths:
    - "User can sign up with email and password and a profile record is created"
    - "User can log in with email and password and session persists across browser refreshes"
    - "User can request a magic link and log in via the emailed link"
    - "After signup, user is redirected to org creation/onboarding"
    - "User can create an organization with company name, address, DOT/MC numbers, and company type"
    - "After org creation, user is redirected to dashboard with admin role"
  artifacts:
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Signup page with email/password form"
      min_lines: 20
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Login page with email/password and magic link option"
      min_lines: 20
    - path: "src/app/(auth)/callback/route.ts"
      provides: "Auth callback handler for magic link and email confirmation"
      exports: ["GET"]
    - path: "src/app/(auth)/onboarding/page.tsx"
      provides: "Org creation form with carrier-specific fields"
      min_lines: 30
    - path: "src/schemas/auth.ts"
      provides: "Zod schemas for signup/login validation"
      exports: ["signupSchema", "loginSchema", "magicLinkSchema"]
    - path: "src/schemas/organization.ts"
      provides: "Zod schema for organization creation"
      exports: ["organizationSchema"]
  key_links:
    - from: "src/app/(auth)/signup/actions.ts"
      to: "src/lib/supabase/server.ts"
      via: "server action uses createClient for signUp"
      pattern: "supabase\\.auth\\.signUp"
    - from: "src/app/(auth)/callback/route.ts"
      to: "src/lib/supabase/server.ts"
      via: "exchanges auth code for session"
      pattern: "exchangeCodeForSession"
    - from: "src/app/(auth)/onboarding/actions.ts"
      to: "supabase/migrations/00001_organizations.sql"
      via: "inserts into organizations table, updates profiles.org_id, inserts org_members"
      pattern: "\\.from\\('organizations'\\)"
---

<objective>
Build the complete auth UI (signup, login, magic link) and organization creation flow so users can create accounts, stay logged in, and set up their carrier organization.

Purpose: These are the primary user-facing auth flows. Without them, no one can enter the application.
Output: Working signup, login (password + magic link), auth callback, and org creation with proper form validation.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-organization/01-CONTEXT.md
@.planning/phases/01-auth-organization/01-RESEARCH.md
@.planning/phases/01-auth-organization/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- these utilities exist and should be imported directly -->

From src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From src/types/database.ts:
```typescript
export interface Organization { id: string; name: string; dot_number: string | null; mc_number: string | null; address_line1: string | null; address_city: string | null; address_state: string | null; address_zip: string | null; phone: string | null; email: string | null; company_type: string; created_at: string; updated_at: string; }
export interface Profile { id: string; org_id: string | null; full_name: string | null; role: string; phone: string | null; avatar_url: string | null; created_at: string; updated_at: string; }
export interface OrgMember { id: string; org_id: string; user_id: string; role: string; joined_at: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth forms -- signup, login, and magic link with Zod validation</name>
  <files>
    src/schemas/auth.ts, src/app/(auth)/layout.tsx,
    src/app/(auth)/signup/page.tsx, src/app/(auth)/signup/actions.ts,
    src/app/(auth)/login/page.tsx, src/app/(auth)/login/actions.ts,
    src/app/(auth)/callback/route.ts,
    src/components/auth/signup-form.tsx, src/components/auth/login-form.tsx,
    src/components/auth/magic-link-form.tsx,
    tests/auth/signup.test.ts, tests/auth/session.test.ts, tests/auth/magic-link.test.ts
  </files>
  <behavior>
    - signupSchema validates: email (required, valid email), password (required, min 8 chars), fullName (required, min 1 char)
    - loginSchema validates: email (required, valid email), password (required)
    - magicLinkSchema validates: email (required, valid email)
    - Signup action calls supabase.auth.signUp with email, password, and metadata {full_name}
    - Login action calls supabase.auth.signInWithPassword
    - Magic link action calls supabase.auth.signInWithOtp with email
    - Auth callback route exchanges code for session via exchangeCodeForSession
    - Invalid form data returns field-level errors
  </behavior>
  <action>
    1. Create `src/schemas/auth.ts` with Zod schemas:
       - `signupSchema`: email (string, email), password (string, min 8, message "Password must be at least 8 characters"), fullName (string, min 1, message "Name is required")
       - `loginSchema`: email (string, email), password (string, min 1)
       - `magicLinkSchema`: email (string, email)

    2. Create `src/app/(auth)/layout.tsx`:
       - Centered layout with max-w-md container
       - Manifest logo/name at top (text "Manifest" in primary color, capitalized)
       - White background, clean minimal design
       - No sidebar, no header -- auth pages are standalone

    3. Create `src/components/auth/signup-form.tsx` (Client Component):
       - react-hook-form with zodResolver using signupSchema
       - Fields: Full Name, Email, Password
       - Submit calls server action
       - Show field-level validation errors
       - Loading state on submit button
       - Link to login page at bottom

    4. Create `src/app/(auth)/signup/page.tsx`:
       - Renders SignupForm component
       - Page title "Create your account"

    5. Create `src/app/(auth)/signup/actions.ts` (server action):
       - Parse form data with signupSchema
       - Call `supabase.auth.signUp()` with email, password, options.data.full_name
       - On success: redirect to `/onboarding`
       - On error: return `{ error: { form: [error.message] } }`

    6. Create `src/components/auth/login-form.tsx` (Client Component):
       - Two-tab or toggle UI: "Password" and "Magic Link"
       - Password tab: email + password fields with loginSchema
       - Magic Link tab: email field only with magicLinkSchema
       - Submit calls respective server action
       - Link to signup page at bottom

    7. Create `src/app/(auth)/login/page.tsx`:
       - Renders LoginForm component
       - Page title "Sign in to Manifest"

    8. Create `src/app/(auth)/login/actions.ts` (server actions):
       - `login()`: Parse with loginSchema, call `supabase.auth.signInWithPassword()`, redirect to `/dashboard` on success
       - `sendMagicLink()`: Parse with magicLinkSchema, call `supabase.auth.signInWithOtp({ email })`, return success message "Check your email for a login link"

    9. Create `src/app/(auth)/callback/route.ts` per Research Pattern 5:
       - GET handler extracts `code` from searchParams
       - Calls `supabase.auth.exchangeCodeForSession(code)`
       - On success: redirect to `next` param or `/dashboard`
       - On error: redirect to `/login?error=auth_callback_failed`

    10. Update test stubs in tests/auth/:
        - `signup.test.ts`: Test signupSchema validation (valid input passes, missing email fails, short password fails). Mock Supabase signUp call.
        - `session.test.ts`: Test that server client creation works (mock cookies).
        - `magic-link.test.ts`: Test magicLinkSchema validation, mock signInWithOtp call.

    DESIGN: Use design system tokens -- primary color buttons, Inter font, 6px button radius, 10px card radius, max shadow 0 2px 8px rgba(0,0,0,0.06). No gradients. JetBrains Mono NOT used here (no DOT/MC numbers on auth forms).
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx vitest run tests/auth/signup.test.ts tests/auth/magic-link.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Signup form creates account via Supabase Auth and redirects to onboarding. Login form supports both password and magic link. Auth callback route exchanges code for session. Zod schemas validate all form inputs. Tests verify schema validation logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Organization creation flow with carrier-specific fields</name>
  <files>
    src/schemas/organization.ts,
    src/app/(auth)/onboarding/page.tsx, src/app/(auth)/onboarding/actions.ts,
    src/components/auth/org-setup-form.tsx,
    tests/org/create.test.ts
  </files>
  <behavior>
    - organizationSchema validates: name (required), company_type (enum: dot_carrier, non_dot_carrier, both), all other fields optional
    - Org creation inserts into organizations table, updates profiles.org_id, inserts org_members with admin role
    - DOT number and MC number fields use JetBrains Mono font
    - After org creation, user is redirected to /dashboard
  </behavior>
  <action>
    1. Create `src/schemas/organization.ts` per Research code example:
       - name: string, min 1 "Company name is required", max 255
       - address_line1, address_city: string, optional
       - address_state: string, max 2, optional
       - address_zip: string, max 10, optional
       - phone: string, optional
       - email: string, email or empty string, optional
       - dot_number: string, max 20, optional or empty
       - mc_number: string, max 20, optional or empty
       - company_type: enum ['dot_carrier', 'non_dot_carrier', 'both']

    2. Create `src/components/auth/org-setup-form.tsx` (Client Component):
       - react-hook-form with zodResolver using organizationSchema
       - Sections: Company Info, Address, Carrier Details
       - Company Info: Company Name (required), Phone, Email
       - Address: Street, City, State (2-char), ZIP
       - Carrier Details: Company Type (radio/select: "DOT Carrier", "Non-DOT Carrier", "Both"), DOT Number, MC Number
       - DOT Number and MC Number inputs use `font-mono` class (JetBrains Mono)
       - MC Number field has helper text: "Optional -- FMCSA is phasing out MC numbers"
       - company_type defaults to 'dot_carrier'
       - Submit calls server action
       - Loading state, field-level errors

    3. Create `src/app/(auth)/onboarding/page.tsx`:
       - Page title "Set up your company"
       - Subtitle "Tell us about your operation"
       - Renders OrgSetupForm
       - Only accessible to authenticated users without an org_id (middleware will enforce in Plan 03)

    4. Create `src/app/(auth)/onboarding/actions.ts` per Research code example:
       - Server action `createOrganization(formData: FormData)`
       - Verify user is authenticated via `supabase.auth.getUser()`
       - Parse with organizationSchema
       - INSERT into organizations, get back org.id
       - UPDATE profiles SET org_id = org.id, role = 'admin' WHERE id = user.id
       - INSERT into org_members (org_id, user_id, role = 'admin')
       - On success: redirect to `/dashboard`
       - On error: return field errors
       - CRITICAL: Per Research Pitfall 3, the users_own_profile RLS policy ensures the user can update their own profile even before org_id is set.

    5. Update `tests/org/create.test.ts`:
       - Test organizationSchema: valid full input passes, missing name fails, invalid company_type fails, valid with only required fields passes
       - Test that DOT/MC numbers accept alphanumeric input
       - Mock the createOrganization server action flow

    DESIGN: Same clean auth layout. Primary color submit button. 8px spacing grid. Max shadow. No gradients. DOT/MC fields in JetBrains Mono.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx vitest run tests/org/create.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Organization creation form collects carrier-specific details. DOT/MC numbers display in JetBrains Mono. Creating an org sets the user as admin, links their profile, and creates org_members record. User redirected to dashboard after org creation. Schema validation tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with all new pages and components
2. `npx vitest run tests/auth/ tests/org/ --reporter=verbose` -- all implemented tests pass, stubs are todo
3. Signup page renders at /signup with email, password, name fields
4. Login page renders at /login with password and magic link options
5. Onboarding page renders at /onboarding with org creation form
6. Auth callback route exists at /callback
7. All form validation works (try submitting empty forms, short passwords, etc.)
</verification>

<success_criteria>
- User can sign up with email/password (AUTH-01)
- User session persists via Supabase SSR cookie handling (AUTH-02)
- Magic link login option available (AUTH-03)
- Organization creation captures: company name, address, phone, email, DOT number, MC number, company type (AUTH-05)
- After signup + org creation, user has admin role and is linked to their org
- All Zod validation schemas enforce correct constraints
- Forms follow design system: Inter font, #EC008C primary, no gradients, max 12px radius
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-organization/01-02-SUMMARY.md`
</output>
