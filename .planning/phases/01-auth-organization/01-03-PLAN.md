---
phase: 01-auth-organization
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/middleware.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/app/(driver)/layout.tsx
  - src/app/(driver)/dashboard/page.tsx
  - src/app/(app)/settings/team/page.tsx
  - src/app/(app)/settings/team/actions.ts
  - src/components/auth/invite-form.tsx
  - src/components/layout/app-header.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/driver-header.tsx
  - src/schemas/invite.ts
  - tests/auth/invite.test.ts
  - tests/middleware/routing.test.ts
autonomous: false
requirements: [AUTH-06, AUTH-07, AUTH-08, AUTH-09]

must_haves:
  truths:
    - "Admin can invite a user by email with a specific role assignment"
    - "Invited user receives email, clicks link, creates account, and is automatically linked to the inviting org with assigned role"
    - "Driver role users are redirected to /driver/dashboard when accessing Command mode routes"
    - "Non-driver role users are redirected to /dashboard when accessing Driver PWA routes"
    - "Unauthenticated users are redirected to /login on protected routes"
    - "Users without an org are redirected to /onboarding"
    - "Role-based access is enforced -- drivers cannot access admin features"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Auth session check + role-based routing middleware"
      min_lines: 40
    - path: "src/app/(app)/layout.tsx"
      provides: "Command mode layout with sidebar and header"
      min_lines: 20
    - path: "src/app/(driver)/layout.tsx"
      provides: "Driver PWA layout with mobile-first header"
      min_lines: 15
    - path: "src/app/(app)/settings/team/page.tsx"
      provides: "Team management page with member list and invitation form"
      min_lines: 30
    - path: "src/schemas/invite.ts"
      provides: "Zod schema for invitation form"
      exports: ["inviteSchema"]
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/supabase/middleware.ts"
      via: "calls updateSession for JWT validation, then queries profile for role"
      pattern: "updateSession"
    - from: "src/app/(app)/settings/team/actions.ts"
      to: "src/lib/supabase/admin.ts"
      via: "uses supabaseAdmin to call auth.admin.inviteUserByEmail"
      pattern: "inviteUserByEmail"
    - from: "src/middleware.ts"
      to: "supabase/migrations/00002_profiles.sql"
      via: "queries profiles table for role and org_id"
      pattern: "from\\('profiles'\\)"
---

<objective>
Build the invitation flow for team management, implement role-based middleware routing across all three modes (Command, Driver PWA, Owner-Operator), and create the route group layouts.

Purpose: Without middleware routing, users land on the wrong mode. Without invitations, organizations can only have one user. These features complete the multi-user, multi-role foundation.
Output: Working invitation flow, role-based routing, Command and Driver PWA layouts with placeholder dashboards.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-organization/01-CONTEXT.md
@.planning/phases/01-auth-organization/01-RESEARCH.md
@.planning/phases/01-auth-organization/01-01-SUMMARY.md
@.planning/phases/01-auth-organization/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
From src/lib/supabase/middleware.ts:
```typescript
export async function updateSession(request: NextRequest): Promise<{
  supabase: SupabaseClient;
  supabaseResponse: NextResponse;
  claims: JWTClaims | null;
  error: Error | null;
}>
```

From src/lib/supabase/admin.ts:
```typescript
export const supabaseAdmin: SupabaseClient  // uses SUPABASE_SERVICE_ROLE_KEY
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

<!-- From Plan 02 -->
From src/app/(auth)/callback/route.ts:
```typescript
// Handles auth code exchange -- invitation links redirect here too
export async function GET(request: Request): Promise<NextResponse>
```

From src/types/database.ts:
```typescript
export interface Profile { id: string; org_id: string | null; full_name: string | null; role: string; phone: string | null; avatar_url: string | null; }
export interface OrgMember { id: string; org_id: string; user_id: string; role: string; joined_at: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Role-based middleware routing and route group layouts</name>
  <files>
    src/middleware.ts,
    src/app/(app)/layout.tsx, src/app/(app)/dashboard/page.tsx,
    src/app/(driver)/layout.tsx, src/app/(driver)/dashboard/page.tsx,
    src/components/layout/app-header.tsx, src/components/layout/app-sidebar.tsx,
    src/components/layout/driver-header.tsx,
    tests/middleware/routing.test.ts
  </files>
  <behavior>
    - Unauthenticated request to /dashboard -> redirect to /login
    - Authenticated user with no org_id -> redirect to /onboarding
    - Authenticated driver accessing /dashboard -> redirect to /driver/dashboard
    - Authenticated admin accessing /driver/dashboard -> redirect to /dashboard
    - Authenticated admin accessing /dashboard -> allowed (no redirect)
    - Auth routes (/login, /signup) with valid session -> redirect to appropriate dashboard
    - Static assets and public routes -> no auth check
  </behavior>
  <action>
    1. Create `src/middleware.ts` following Research Pattern 4 EXACTLY:
       - Call `updateSession(request)` to refresh JWT and validate via getClaims()
       - Public routes (/, /login, /signup, /callback, marketing) -- pass through, no auth
       - Auth routes with valid session -- redirect to appropriate dashboard based on role
       - Protected routes without session -- redirect to /login
       - For protected routes with session, query profiles table for role and org_id:
         ```
         const { data: profile } = await supabase
           .from('profiles')
           .select('role, org_id')
           .eq('id', claims.sub)
           .single()
         ```
       - No org_id -> redirect to /onboarding
       - Driver accessing /dashboard or /(app)/* routes -> redirect to /driver/dashboard
       - Non-driver accessing /driver/* routes -> redirect to /dashboard
       - Owner-Operator detection: role === 'admin' AND check org member count === 1 (per Research Open Question 3). For now, owner-operators use the same Command mode layout -- differentiation is scoping, not a different layout.
       - Matcher config: skip _next/static, _next/image, favicon, image files

    2. Create `src/components/layout/app-header.tsx`:
       - Top bar for Command mode
       - Left: "Manifest" text in primary color (#EC008C)
       - Right: user name/email, sign out button
       - Sign out calls supabase.auth.signOut() and redirects to /login
       - Clean, minimal -- white background, bottom border, max shadow-sm

    3. Create `src/components/layout/app-sidebar.tsx`:
       - Left sidebar for Command mode navigation
       - Navigation items with Lucide icons: Dashboard (LayoutDashboard), Loads (Truck -- placeholder for Phase 2), Dispatch (Navigation -- placeholder), Invoices (FileText -- placeholder), Settings (Settings)
       - Only Dashboard and Settings are active links in Phase 1
       - Other items shown but grayed out with "(coming soon)" tooltip or text
       - Active item highlighted with primary color
       - Compact design: 240px width, white background, border-right

    4. Create `src/app/(app)/layout.tsx`:
       - Server Component that checks auth (redirect to /login if not authenticated)
       - Renders AppHeader + AppSidebar + main content area
       - Main content has padding (16px or 24px)
       - Responsive: sidebar collapses on mobile (hamburger menu or overlay)

    5. Create `src/app/(app)/dashboard/page.tsx`:
       - Placeholder Command mode dashboard
       - Title "Dashboard" with subtitle "Welcome to Manifest"
       - Display user name and org name (fetch from Supabase)
       - Placeholder stat cards (empty state): "Active Loads: 0", "Booked Today: 0", "Drivers on Duty: 0", "Revenue MTD: $0"
       - Cards follow design system: 10px radius, shadow-md max, 8px spacing grid

    6. Create `src/components/layout/driver-header.tsx`:
       - Mobile-first header for Driver PWA
       - "Manifest" centered, menu button left, profile icon right
       - Simpler than Command header -- optimized for mobile

    7. Create `src/app/(driver)/layout.tsx`:
       - Mobile-first layout for Driver PWA
       - DriverHeader at top
       - Content area with bottom navigation (Dashboard, Loads, Settings -- placeholders for future phases)
       - No sidebar -- mobile bottom nav pattern

    8. Create `src/app/(driver)/dashboard/page.tsx`:
       - Placeholder Driver PWA dashboard
       - "No active loads" empty state card
       - Simple, clean, mobile-optimized

    9. Update `tests/middleware/routing.test.ts`:
       - Test routing logic (unit test the decision function, not the full middleware):
         - No claims -> returns redirect to /login
         - Claims but no profile.org_id -> returns redirect to /onboarding
         - Driver role + /dashboard path -> returns redirect to /driver/dashboard
         - Admin role + /driver/dashboard path -> returns redirect to /dashboard
         - Admin role + /dashboard path -> no redirect (allowed)
         - Login path with valid claims -> redirect to dashboard

    DESIGN: White backgrounds, Inter font, #EC008C for brand and active states. No gradients in app UI. Shadows max 0 2px 8px rgba(0,0,0,0.06). Cards 10px radius, buttons 6px radius. 8px spacing grid. Tables scroll horizontally on mobile.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx vitest run tests/middleware/routing.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Middleware correctly routes users by role: drivers to Driver PWA, admins/dispatchers to Command mode. Unauthenticated users redirected to login. Users without org redirected to onboarding. Command mode has sidebar + header layout. Driver PWA has mobile-first layout. Both have placeholder dashboards.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Team invitation flow -- admin invites users with role assignment</name>
  <files>
    src/schemas/invite.ts,
    src/app/(app)/settings/team/page.tsx, src/app/(app)/settings/team/actions.ts,
    src/components/auth/invite-form.tsx,
    tests/auth/invite.test.ts
  </files>
  <behavior>
    - inviteSchema validates: email (required, valid email), role (required, one of admin/dispatcher/driver/viewer)
    - Admin can submit invitation form with email and role
    - Server action uses supabaseAdmin (service role) to call auth.admin.inviteUserByEmail with org_id and role in metadata
    - Team page shows list of current org members with role badges
    - Non-admin users cannot access the invite functionality
    - Invitation callback handled by existing /callback route (from Plan 02) -- trigger auto-creates profile with org_id and role from metadata
  </behavior>
  <action>
    1. Create `src/schemas/invite.ts`:
       - `inviteSchema`: email (string, email), role (enum: 'admin', 'dispatcher', 'driver', 'viewer')

    2. Create `src/components/auth/invite-form.tsx` (Client Component):
       - react-hook-form with zodResolver using inviteSchema
       - Email input field
       - Role select dropdown with four options: Admin, Dispatcher, Driver, Viewer
       - Each role has a one-line description (from PRD-01 Section 2.2 roles table)
       - Submit button "Send Invitation"
       - Success state: "Invitation sent to {email}"
       - Error state: display error message
       - Loading state on submit

    3. Create `src/app/(app)/settings/team/page.tsx`:
       - Server Component
       - Check current user role -- only admin can access (if not admin, show "You don't have permission to manage team members")
       - Fetch org members: query org_members joined with profiles to get name, email, role, joined_at
       - Display member list in a table:
         - Columns: Name, Email, Role (with colored badge), Joined
         - Role badges: admin (pink/primary), dispatcher (blue), driver (green), viewer (gray)
       - Below table: InviteForm component for sending new invitations
       - Title "Team Members" with subtitle showing member count

    4. Create `src/app/(app)/settings/team/actions.ts`:
       - Server action `inviteTeamMember(formData: FormData)`
       - Verify current user is admin:
         ```
         const supabase = await createClient()
         const { data: { user } } = await supabase.auth.getUser()
         const { data: profile } = await supabase.from('profiles').select('role, org_id').eq('id', user.id).single()
         if (profile.role !== 'admin') return { error: 'Only admins can invite users' }
         ```
       - Parse formData with inviteSchema
       - Use `supabaseAdmin` (from src/lib/supabase/admin.ts) to call:
         ```
         supabaseAdmin.auth.admin.inviteUserByEmail(email, {
           data: { org_id: profile.org_id, role: parsed.data.role, invited: true },
           redirectTo: `${process.env.NEXT_PUBLIC_APP_URL}/callback?next=/dashboard`
         })
         ```
       - IMPORTANT: The admin client uses SUPABASE_SERVICE_ROLE_KEY to bypass RLS for the invitation API. This is server-only code.
       - On success: revalidate the team page path
       - On error: return error message
       - Note: The `handle_new_user` trigger (from Plan 01, migration 00005) already handles extracting org_id and role from raw_user_meta_data, so when the invited user clicks the link and their auth.users record is created, the trigger will automatically create their profile with the correct org_id and role, and insert the org_members record.

    5. Update `tests/auth/invite.test.ts`:
       - Test inviteSchema: valid input passes, missing email fails, invalid role fails
       - Test that admin check is enforced (mock profile query returning non-admin role, expect error)
       - Test invitation call structure (mock supabaseAdmin, verify inviteUserByEmail is called with correct metadata)

    DESIGN: Team page uses standard data table styling. Role badges are small colored pills. Invite form is below the table, same card styling as auth forms.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx vitest run tests/auth/invite.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    Admin can invite users from Settings > Team page. Invitation form captures email and role. Server action calls Supabase admin API with org metadata. Team page displays current members with role badges. Non-admin users see permission error. Invited users auto-linked to org via auth trigger.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth and organization flow end-to-end</name>
  <files>none -- verification only</files>
  <action>
    Human verifies the complete auth and organization flow built across Plans 01, 02, and 03:
    - Signup, login (password + magic link), org creation, team invitation, and role-based routing across Command mode and Driver PWA.

    Steps to verify:
    1. Visit http://localhost:3000/signup -- create a new account with email and password
    2. After signup, you should be redirected to /onboarding -- fill in company details (name, DOT number, company type)
    3. After org creation, you should land on /dashboard (Command mode) with sidebar and header showing "Manifest"
    4. Navigate to Settings > Team -- you should see yourself as admin
    5. Try the invitation form -- enter an email and select "driver" role, submit (will need configured SMTP to actually send)
    6. Log out, then log back in -- verify you go directly to /dashboard (session persistence)
    7. Try the magic link option on the login page
    8. Verify design system: primary color is #EC008C (pink), fonts are Inter, no gradients, clean minimal UI
  </action>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>User has approved the complete auth and organization flow</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npx vitest run --reporter=verbose` -- all implemented tests pass
3. Middleware correctly redirects: unauthenticated -> /login, no org -> /onboarding, driver -> /driver/dashboard, admin -> /dashboard
4. Team page shows member list and invite form (admin only)
5. Route groups work: /(app) routes have sidebar layout, /(driver) routes have mobile layout, /(auth) routes have centered layout
6. Human verification of complete auth flow
</verification>

<success_criteria>
- Admin can invite users with role assignment via Settings > Team (AUTH-06)
- Invited users join org with assigned role via email link (AUTH-07)
- Role-based access enforced via RLS policies AND middleware routing (AUTH-08)
- Middleware redirects to correct mode: admin/dispatcher -> Command, driver -> Driver PWA (AUTH-09)
- Complete signup -> org creation -> dashboard flow works end-to-end
- Design system applied consistently across all auth and layout pages
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-organization/01-03-SUMMARY.md`
</output>
