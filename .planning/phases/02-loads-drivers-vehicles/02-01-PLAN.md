---
phase: 02-loads-drivers-vehicles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00007_vehicles.sql
  - supabase/migrations/00008_drivers.sql
  - supabase/migrations/00009_loads.sql
  - supabase/migrations/00010_load_status_history.sql
  - supabase/migrations/00011_load_number_trigger.sql
  - supabase/migrations/00012_load_status_trigger.sql
  - supabase/migrations/00013_storage_bucket.sql
  - src/types/database.ts
  - src/schemas/driver.ts
  - src/schemas/vehicle.ts
  - src/schemas/load.ts
  - src/lib/load-status.ts
  - tests/drivers/schema.test.ts
  - tests/vehicles/schema.test.ts
  - tests/loads/schema.test.ts
  - tests/loads/status.test.ts
autonomous: true
requirements:
  - DRVR-01
  - VEHI-01
  - VEHI-02
  - LOAD-01
  - LOAD-02
  - LOAD-03
  - LOAD-04
  - LOAD-05
  - LOAD-06
  - LOAD-07
  - LOAD-08
  - LOAD-09

must_haves:
  truths:
    - "Database tables for vehicles, drivers, loads, and load_status_history exist with correct columns"
    - "RLS policies enforce org_id isolation on all new tables"
    - "Load number auto-generates via database trigger in ORG-PREFIX-SEQUENCE format"
    - "Load status changes automatically log to load_status_history via trigger"
    - "Storage bucket exists for load documents with org-scoped RLS"
    - "TypeScript types and Zod schemas exist for all entities"
    - "Load status transition rules are defined and validated"
  artifacts:
    - path: "supabase/migrations/00007_vehicles.sql"
      provides: "Vehicles table with RLS"
      contains: "create table vehicles"
    - path: "supabase/migrations/00009_loads.sql"
      provides: "Loads table with all PRD-01 columns"
      contains: "create table loads"
    - path: "supabase/migrations/00011_load_number_trigger.sql"
      provides: "Auto-generation trigger for load numbers"
      contains: "generate_load_number"
    - path: "src/types/database.ts"
      provides: "TypeScript types for Driver, Vehicle, Load, LoadStatusHistory"
      exports: ["Driver", "Vehicle", "Load", "LoadStatusHistory", "LoadStatus", "DriverStatus", "VehicleType", "EquipmentType"]
    - path: "src/schemas/load.ts"
      provides: "Zod schemas for multi-step load form"
      exports: ["pickupSchema", "deliverySchema", "freightSchema", "rateSchema", "brokerSchema", "assignmentSchema", "loadSchema"]
    - path: "src/lib/load-status.ts"
      provides: "Status transition logic and metadata"
      exports: ["LOAD_STATUSES", "VALID_TRANSITIONS", "canTransition", "getStatusColor", "getStatusLabel"]
  key_links:
    - from: "src/types/database.ts"
      to: "supabase/migrations/*.sql"
      via: "types mirror migration columns"
      pattern: "export type (Driver|Vehicle|Load|LoadStatusHistory)"
    - from: "src/schemas/load.ts"
      to: "src/types/database.ts"
      via: "schema fields match type properties"
      pattern: "z\\.object"
---

<objective>
Create the database foundation for Phase 2: migrations for vehicles, drivers, loads, load_status_history tables with RLS policies, database triggers for load number auto-generation and status history logging, Supabase Storage bucket for documents, TypeScript types extending the Database interface, Zod validation schemas for all entities, and load status transition logic.

Purpose: Every subsequent plan depends on these tables, types, and schemas existing. This is the data layer that drivers, vehicles, and loads build on.
Output: 7 SQL migrations, extended database types, 3 Zod schema files, load status utility, and test stubs.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-loads-drivers-vehicles/02-CONTEXT.md
@.planning/phases/02-loads-drivers-vehicles/02-RESEARCH.md

<interfaces>
<!-- Existing types to extend -->
From src/types/database.ts:
```typescript
export type CompanyType = 'dot_carrier' | 'non_dot_carrier' | 'both'
export type UserRole = 'admin' | 'dispatcher' | 'driver' | 'viewer'
export type Organization = { id: string; name: string; /* ... */ }
export type Profile = { id: string; org_id: string | null; role: UserRole; /* ... */ }
export type OrgMember = { id: string; org_id: string; user_id: string; role: UserRole; joined_at: string }
export type Database = { public: { Tables: { organizations: {...}; profiles: {...}; org_members: {...} }; /* ... */ } }
```

From src/schemas/auth.ts (Zod pattern reference):
```typescript
export const signupSchema = z.object({
  email: z.string().email('Please enter a valid email'),
  password: z.string().min(8, 'Password must be at least 8 characters'),
  fullName: z.string().min(1, 'Name is required'),
})
export type SignupInput = z.infer<typeof signupSchema>
```

Existing migrations (00001-00006):
- 00001_organizations.sql, 00002_profiles.sql, 00003_org_members.sql
- 00004_rls_policies.sql (includes auth.org_id() helper)
- 00005_auth_trigger.sql, 00006_auth_helpers.sql
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create database migrations for vehicles, drivers, loads, triggers, and storage</name>
  <files>
    supabase/migrations/00007_vehicles.sql
    supabase/migrations/00008_drivers.sql
    supabase/migrations/00009_loads.sql
    supabase/migrations/00010_load_status_history.sql
    supabase/migrations/00011_load_number_trigger.sql
    supabase/migrations/00012_load_status_trigger.sql
    supabase/migrations/00013_storage_bucket.sql
  </files>
  <behavior>
    - 00007_vehicles.sql: creates vehicles table with id (uuid PK), org_id (FK to organizations), unit_number, vin, year (integer), make, model, vehicle_type (text: dry_van/reefer/flatbed/sprinter/box_truck/other), status (text: active/inactive), created_at, updated_at; RLS enabled with org_id policy using (select auth.org_id()); index on org_id
    - 00008_drivers.sql: creates drivers table per PRD-01 Section 3.2 schema exactly -- id, org_id, user_id (nullable FK to auth.users), first_name, last_name, email, phone, license_number, license_state, license_class, license_expiration (date), hire_date, status (active/inactive/terminated), current_vehicle_id (FK to vehicles), home_terminal, notes, emergency_contact_name, emergency_contact_phone, created_at, updated_at; RLS with org_id policy; index on org_id
    - 00009_loads.sql: creates loads table per PRD-01 Section 4.2 exactly -- all pickup, delivery, freight, rate, assignment, broker, document URL, and meta columns; RLS with org_id policy; indexes on org_id, status, driver_id
    - 00010_load_status_history.sql: creates load_status_history table with load_id FK, old_status, new_status, changed_by, location_lat, location_lng, notes, created_at; RLS allowing select for org members via subquery through loads table; index on load_id
    - 00011_load_number_trigger.sql: creates load_number_sequences table (org_id PK, last_number integer default 0) with RLS; creates generate_load_number() trigger function (security definer, set search_path = '') that auto-generates ORG-PREFIX-SEQUENCE format load numbers using org name first 3 chars + zero-padded 6-digit sequence; BEFORE INSERT trigger on loads
    - 00012_load_status_trigger.sql: creates log_load_status_change() trigger function (security definer, set search_path = '') that inserts into load_status_history when status changes; AFTER UPDATE OF status trigger on loads
    - 00013_storage_bucket.sql: creates 'load-documents' private storage bucket; creates RLS policies for insert/select/delete on storage.objects scoped by org_id in folder path using (storage.foldername(name))[1] = (select auth.org_id())::text
  </behavior>
  <action>
    Create 7 SQL migration files following the exact patterns from Phase 1 migrations (00001-00006). Use `(select auth.org_id())` for RLS policies (initPlan caching pattern established in 00004_rls_policies.sql). Use `security definer` and `set search_path = ''` for trigger functions (pattern from 00005_auth_trigger.sql). All table/column names in snake_case. All PKs are uuid with gen_random_uuid(). All timestamps are timestamptz. Status columns are text type with documented enum values in SQL comments.

    For the load_number_trigger: use INSERT ... ON CONFLICT for atomic sequence increment. Format is uppercase first 3 chars of org name + hyphen + 6-digit zero-padded number (e.g., ACM-000001).

    For the storage bucket: use `insert into storage.buckets` not an edge function. RLS policies use storage.foldername() to extract org_id from file path structure `{org_id}/{load_id}/{doc_type}.{ext}`.
  </action>
  <verify>
    <automated>ls -la supabase/migrations/0000{7,8,9}*.sql supabase/migrations/0001{0,1,2,3}*.sql && echo "All 7 migration files exist"</automated>
  </verify>
  <done>7 migration files exist with correct table definitions, RLS policies, triggers, and storage bucket matching PRD-01 schemas</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create TypeScript types, Zod schemas, load status utility, and test stubs</name>
  <files>
    src/types/database.ts
    src/schemas/driver.ts
    src/schemas/vehicle.ts
    src/schemas/load.ts
    src/lib/load-status.ts
    tests/drivers/schema.test.ts
    tests/vehicles/schema.test.ts
    tests/loads/schema.test.ts
    tests/loads/status.test.ts
  </files>
  <behavior>
    - database.ts: adds DriverStatus ('active'|'inactive'|'terminated'), VehicleType ('dry_van'|'reefer'|'flatbed'|'sprinter'|'box_truck'|'other'), VehicleStatus ('active'|'inactive'), EquipmentType (same as VehicleType), LoadStatus (10 values: booked through canceled), RateType ('flat'|'per_mile'|'hourly'|'per_stop'), WeightUnit ('lbs'|'kg'), LicenseClass ('A'|'B'|'C'|'standard') types; adds Driver, Vehicle, Load, LoadStatusHistory row types; extends Database type with drivers, vehicles, loads, load_status_history, load_number_sequences table definitions
    - driver.ts: driverSchema with first_name (required), last_name (required), email (optional email), phone (required), license_number (optional), license_state (optional 2-char), license_class (optional enum), license_expiration (optional), hire_date (optional), status (enum default 'active'), home_terminal (optional), notes (optional), emergency_contact_name (optional), emergency_contact_phone (optional); driverUpdateSchema as partial; DriverInput type
    - vehicle.ts: vehicleSchema with unit_number (required), vin (optional, 17 chars when provided), year (number, 1900-2100), make (required), model (required), vehicle_type (enum required), status (enum default 'active'); VehicleInput type
    - load.ts: per-step schemas (pickupSchema, deliverySchema, freightSchema, rateSchema, brokerSchema, assignmentSchema) plus combined loadSchema merging all with load_number (optional) and notes (optional); STEP_FIELDS arrays for wizard trigger() validation; LoadInput type using z.input (not z.infer per Phase 1 decision for zodResolver compatibility)
    - load-status.ts: LOAD_STATUSES array of all 10 statuses; VALID_TRANSITIONS map defining which statuses can transition to which (booked->dispatched/canceled, dispatched->in_transit/canceled, etc.); canTransition(from, to) boolean function; getStatusColor(status) returning Tailwind color class; getStatusLabel(status) returning display label; STATUS_ORDER for sorting
    - Test stubs: tests/drivers/schema.test.ts with todos for DRVR-01 validation, tests/vehicles/schema.test.ts with todos for VEHI-01/02, tests/loads/schema.test.ts with todos for LOAD-01 through LOAD-05, tests/loads/status.test.ts with tests for canTransition (valid transitions pass, invalid transitions rejected, canceled reachable from any status)
  </behavior>
  <action>
    Extend src/types/database.ts by adding new type aliases and extending the Database type's Tables object with drivers, vehicles, loads, load_status_history, and load_number_sequences entries. Follow the existing pattern: Row type uses the entity type, Insert type uses Omit for auto-generated fields with optional overrides, Update type uses Partial. Use `type` aliases not `interface` (per Phase 1 decision for Supabase postgrest compatibility).

    Create Zod schemas following the pattern in src/schemas/auth.ts. For load.ts, create per-step schemas that can be used independently by the wizard's trigger() validation, plus a merged loadSchema for full validation on submit. Export STEP_FIELDS arrays mapping step names to their field name arrays.

    For load-status.ts: define the complete status lifecycle. Valid transitions follow the PRD-01 lifecycle: booked -> dispatched -> in_transit -> at_pickup -> loaded -> at_delivery -> delivered -> invoiced -> paid. Canceled is reachable from any status except paid/invoiced. Status colors: green for delivered/paid, blue for in_transit/at_pickup/loaded/at_delivery, yellow for booked/dispatched, red for canceled, gray for invoiced.

    Create test files with real passing tests for load status transitions (canTransition) and todo stubs for schema validation tests.
  </action>
  <verify>
    <automated>npx vitest run tests/loads/status.test.ts tests/drivers/schema.test.ts tests/vehicles/schema.test.ts tests/loads/schema.test.ts --reporter=verbose</automated>
  </verify>
  <done>TypeScript types cover all 5 new tables, Zod schemas validate all entity forms, load status utility defines valid transitions with passing tests, and test stubs exist for all schema files</done>
</task>

</tasks>

<verification>
- All 7 migration files exist in supabase/migrations/ (00007-00013)
- src/types/database.ts exports Driver, Vehicle, Load, LoadStatusHistory types and extended Database type
- src/schemas/driver.ts, vehicle.ts, load.ts export validation schemas
- src/lib/load-status.ts exports canTransition, VALID_TRANSITIONS, getStatusColor, getStatusLabel
- `npx vitest run` passes with status transition tests green
- `npm run build` succeeds (no TypeScript errors)
</verification>

<success_criteria>
Database foundation complete: all tables, triggers, storage, types, schemas, and status logic ready for Plans 02-06 to build CRUD and UI on top of.
</success_criteria>

<output>
After completion, create `.planning/phases/02-loads-drivers-vehicles/02-01-SUMMARY.md`
</output>
