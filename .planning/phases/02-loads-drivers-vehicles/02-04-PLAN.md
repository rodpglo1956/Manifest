---
phase: 02-loads-drivers-vehicles
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(app)/loads/status-actions.ts
  - src/hooks/use-realtime-loads.ts
  - src/components/ui/file-upload.tsx
  - src/components/loads/load-documents.tsx
  - src/lib/storage.ts
  - tests/loads/status.test.ts
  - tests/loads/documents.test.ts
autonomous: true
requirements:
  - LOAD-07
  - LOAD-08
  - LOAD-09
  - LOAD-10

must_haves:
  truths:
    - "Load status can be updated through the complete lifecycle (booked through delivered)"
    - "Every status change is logged to load_status_history via database trigger"
    - "Load status changes are broadcast via Supabase Realtime and reflected in real-time"
    - "Users can upload BOL, rate confirmation, and POD documents to a load"
    - "Invalid status transitions are rejected"
  artifacts:
    - path: "src/app/(app)/loads/status-actions.ts"
      provides: "updateLoadStatus server action with transition validation"
      exports: ["updateLoadStatus", "uploadDocumentUrl"]
    - path: "src/hooks/use-realtime-loads.ts"
      provides: "Supabase Realtime subscription hook"
      exports: ["useRealtimeLoads"]
    - path: "src/components/ui/file-upload.tsx"
      provides: "File upload component for desktop and mobile"
      exports: ["FileUpload"]
    - path: "src/components/loads/load-documents.tsx"
      provides: "Document upload and display section for load detail"
      exports: ["LoadDocuments"]
    - path: "src/lib/storage.ts"
      provides: "Storage upload utility for load documents"
      exports: ["uploadLoadDocument", "getDocumentUrl"]
  key_links:
    - from: "src/app/(app)/loads/status-actions.ts"
      to: "src/lib/load-status.ts"
      via: "canTransition validation before status update"
      pattern: "canTransition"
    - from: "src/hooks/use-realtime-loads.ts"
      to: "src/lib/supabase/client.ts"
      via: "Browser client for Realtime channel subscription"
      pattern: "supabase\\.channel"
    - from: "src/lib/storage.ts"
      to: "supabase/migrations/00013_storage_bucket.sql"
      via: "Upload to load-documents bucket with org_id path prefix"
      pattern: "from\\('load-documents'\\)"
---

<objective>
Implement load status lifecycle management (transition validation, status update server action), Supabase Realtime subscription hook for live updates, and document upload system (Supabase Storage with org-scoped RLS, file upload component for desktop and mobile camera).

Purpose: Status lifecycle is the core operational flow -- loads move from booked through delivered. Realtime keeps all users in sync. Documents (BOL, POD) are the legal proof of delivery.
Output: Status update server action in status-actions.ts (separate from createLoad in actions.ts to avoid parallel write conflict with Plan 02-03), Realtime hook, file upload component, document display component, storage utility.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-loads-drivers-vehicles/02-CONTEXT.md
@.planning/phases/02-loads-drivers-vehicles/02-RESEARCH.md
@.planning/phases/02-loads-drivers-vehicles/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 -->
From src/lib/load-status.ts:
```typescript
export const LOAD_STATUSES: LoadStatus[]
export const VALID_TRANSITIONS: Record<LoadStatus, LoadStatus[]>
export function canTransition(from: LoadStatus, to: LoadStatus): boolean
export function getStatusColor(status: LoadStatus): string
export function getStatusLabel(status: LoadStatus): string
```

From src/types/database.ts:
```typescript
export type LoadStatus = 'booked' | 'dispatched' | 'in_transit' | 'at_pickup' | 'loaded' | 'at_delivery' | 'delivered' | 'invoiced' | 'paid' | 'canceled'
export type Load = { id: string; org_id: string; load_number: string; status: LoadStatus; bol_url: string | null; rate_confirmation_url: string | null; pod_url: string | null; ... }
```

From src/lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient<Database>
// Browser client for Realtime subscriptions
```

From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient<Database>>
// Server client for server actions
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement load status update action with transition validation and Realtime hook</name>
  <files>
    src/app/(app)/loads/status-actions.ts
    src/hooks/use-realtime-loads.ts
    tests/loads/status.test.ts
  </files>
  <behavior>
    - updateLoadStatus(loadId, newStatus, notes?) validates transition using canTransition, returns error if invalid
    - updateLoadStatus updates loads.status via Supabase (trigger handles history logging)
    - useRealtimeLoads(orgId) subscribes to postgres_changes on loads table filtered by org_id
    - useRealtimeLoads calls router.refresh() on change events to re-render server components
    - useRealtimeLoads cleans up channel on unmount
    - Test: canTransition('booked', 'dispatched') returns true
    - Test: canTransition('booked', 'delivered') returns false (can't skip steps)
    - Test: canTransition('in_transit', 'canceled') returns true (cancel from any active status)
  </behavior>
  <action>
    Create src/app/(app)/loads/status-actions.ts (NEW FILE -- separate from actions.ts which is owned by Plan 02-03 for createLoad):
    - 'use server' at top
    - updateLoadStatus(loadId: string, newStatus: LoadStatus, notes?: string): Fetches current load status, calls canTransition(currentStatus, newStatus), returns { error: 'Invalid status transition' } if false. On valid: updates loads table set status = newStatus, updated_at = now(). The database trigger (00012) automatically writes to load_status_history. Revalidates /loads path.
    - Also add updateLoad(id: string, formData: FormData) for editing load details (not status).

    NOTE: This file is intentionally separate from src/app/(app)/loads/actions.ts to allow Plans 02-03 and 02-04 to execute in parallel (Wave 2) without file write conflicts. Plan 02-03 owns actions.ts (createLoad). This plan owns status-actions.ts (updateLoadStatus, uploadDocumentUrl).

    Create src/hooks/use-realtime-loads.ts following research Pattern 2:
    - 'use client' hook
    - Creates browser Supabase client
    - Subscribes to channel `org:${orgId}:loads` with postgres_changes on loads table
    - On any change event (INSERT, UPDATE, DELETE), calls router.refresh()
    - Returns cleanup function via useEffect return
    - NOTE: Do NOT include supabase in the useEffect dependency array -- create it once outside useEffect or use useRef to avoid infinite re-subscription loops

    Expand tests/loads/status.test.ts with real tests (not just todos) for canTransition covering:
    - All valid forward transitions in the lifecycle
    - Invalid skip transitions (booked -> delivered)
    - Cancel reachable from all active statuses
    - Cancel NOT reachable from paid
  </action>
  <verify>
    <automated>npx vitest run tests/loads/status.test.ts --reporter=verbose</automated>
  </verify>
  <done>Status update validates transitions, Realtime hook triggers server component refresh on load changes, status transition tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Build document upload system with Supabase Storage and file upload component</name>
  <files>
    src/lib/storage.ts
    src/components/ui/file-upload.tsx
    src/components/loads/load-documents.tsx
    src/app/(app)/loads/status-actions.ts
    tests/loads/documents.test.ts
  </files>
  <action>
    Create src/lib/storage.ts:
    - uploadLoadDocument(file: File, orgId: string, loadId: string, docType: 'bol' | 'rate_confirmation' | 'pod'): uploads file to Supabase Storage bucket 'load-documents' at path `${orgId}/${loadId}/${docType}.${ext}`. Uses upsert: true to allow re-upload. Returns the storage path.
    - getDocumentUrl(path: string): creates signed URL for document download (private bucket requires signed URLs, not public URLs). Uses supabase.storage.from('load-documents').createSignedUrl(path, 3600) for 1-hour expiry.
    - Uses browser Supabase client since uploads happen client-side.

    Create src/components/ui/file-upload.tsx ('use client'):
    - Props: onFileSelected (callback with File), accept (string, default 'image/*,application/pdf'), label (string), isMobile (boolean, optional)
    - Renders a styled label wrapping a hidden file input
    - When isMobile is true, adds capture="environment" attribute for rear camera
    - Shows upload icon (lucide-react Upload or Camera icon), label text
    - After file selected, shows filename preview
    - Design: dashed border, hover border changes to primary color, rounded-lg (but <= 12px per design system)

    Create src/components/loads/load-documents.tsx ('use client'):
    - Props: loadId, orgId, bolUrl, rateConfirmationUrl, podUrl (existing URLs from load record)
    - For each document type (BOL, Rate Confirmation, POD):
      - If URL exists: show document link with download icon, file name
      - If URL missing: show FileUpload component
    - On upload: calls uploadLoadDocument, then calls uploadDocumentUrl server action to update the load record's document URL field (bol_url, rate_confirmation_url, or pod_url)

    Add uploadDocumentUrl server action to src/app/(app)/loads/status-actions.ts (append to file created in Task 1) that updates the specific URL field on the load record. This action lives in status-actions.ts (NOT actions.ts) to maintain file ownership separation.

    Create tests/loads/documents.test.ts with todo stubs for LOAD-10 (desktop upload), LOAD-11 (mobile camera upload).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>File upload component works for desktop file picker and mobile camera, documents upload to Supabase Storage with org-scoped paths, document URLs stored on load record, existing documents shown as download links</done>
</task>

</tasks>

<verification>
- updateLoadStatus rejects invalid transitions and accepts valid ones
- Realtime hook subscribes to correct channel and triggers refresh
- FileUpload component renders with capture attribute on mobile
- Documents upload to load-documents bucket at correct path
- `npm run build` passes
- `npx vitest run tests/loads/` passes
</verification>

<success_criteria>
Load status lifecycle operational with transition validation and automatic history logging. Realtime updates broadcast status changes. Document upload works for BOL, rate confirmation, and POD via desktop file picker and mobile camera.
</success_criteria>

<output>
After completion, create `.planning/phases/02-loads-drivers-vehicles/02-04-SUMMARY.md`
</output>
