---
phase: 07-compliance
plan: 05
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/app/driver/compliance/page.tsx
  - src/components/compliance/dvir-form.tsx
  - src/components/compliance/driver-compliance-view.tsx
  - src/app/oo/compliance/page.tsx
  - src/components/compliance/oo-compliance-dashboard.tsx
autonomous: true
requirements:
  - COMP-08
  - COMP-12

must_haves:
  truths:
    - "Driver PWA shows personal compliance items and CDL/medical expiry countdowns"
    - "DVIR form has 11 pass/fail toggles that can be completed in under 60 seconds"
    - "DVIR form shows defect description field only when item marked fail"
    - "DVIR submission creates inspection record and revalidates page"
    - "Owner-Operator sees simplified compliance dashboard scoped to own vehicles/driver record"
  artifacts:
    - path: "src/app/driver/compliance/page.tsx"
      provides: "Driver PWA compliance page with DVIR form"
      min_lines: 15
    - path: "src/components/compliance/dvir-form.tsx"
      provides: "Digital DVIR pre/post-trip inspection form"
      min_lines: 80
    - path: "src/components/compliance/driver-compliance-view.tsx"
      provides: "Driver's personal compliance items and DQ status"
      min_lines: 40
    - path: "src/app/oo/compliance/page.tsx"
      provides: "Owner-Operator compliance page"
      min_lines: 15
  key_links:
    - from: "src/components/compliance/dvir-form.tsx"
      to: "src/lib/compliance/actions.ts"
      via: "calls submitDVIR"
      pattern: "submitDVIR"
    - from: "src/components/compliance/dvir-form.tsx"
      to: "src/lib/compliance/dvir-schema.ts"
      via: "imports DVIR_INSPECTION_ITEMS, dvirSchema"
      pattern: "DVIR_INSPECTION_ITEMS"
    - from: "src/components/compliance/driver-compliance-view.tsx"
      to: "src/lib/compliance/actions.ts"
      via: "calls getComplianceItems, getDriverQualification"
      pattern: "getComplianceItems|getDriverQualification"
---

<objective>
Driver PWA compliance page with DVIR digital inspection form and Owner-Operator simplified compliance dashboard. These complete the compliance module across all three user modes.

Purpose: Drivers need fast DVIR forms (used twice daily) and visibility into their own compliance status. Owner-Operators need a simplified single-carrier compliance view.
Output: Driver PWA compliance page with DVIR form, OO compliance dashboard.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compliance/07-CONTEXT.md
@.planning/phases/07-compliance/07-01-SUMMARY.md
@.planning/phases/07-compliance/07-02-SUMMARY.md
@src/app/driver/loads/page.tsx
@src/app/oo/dashboard/page.tsx

<interfaces>
<!-- From Plan 07-01 and 07-02 outputs -->

From src/lib/compliance/dvir-schema.ts:
```typescript
export const DVIR_INSPECTION_ITEMS = [
  { id: 'service_brakes', label: 'Service Brakes (incl. trailer connections)', category: 'safety' },
  { id: 'parking_brake', label: 'Parking (Hand) Brake', category: 'safety' },
  // ... 11 items total
] as const;

export const dvirSchema: z.ZodSchema;
```

From src/lib/compliance/actions.ts:
```typescript
export async function submitDVIR(formData: FormData): Promise<{ error?: string }>;
export async function getComplianceItems(filters?): Promise<ComplianceItem[]>;
export async function getDriverQualification(driverId: string): Promise<...>;
export async function getComplianceDashboard(): Promise<...>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver PWA compliance page with DVIR form</name>
  <files>
    src/app/driver/compliance/page.tsx,
    src/components/compliance/dvir-form.tsx,
    src/components/compliance/driver-compliance-view.tsx
  </files>
  <action>
1. Create `src/components/compliance/dvir-form.tsx` as client component -- OPTIMIZED FOR SPEED:
   - Props: vehicleId (pre-populated from driver's assigned vehicle), onComplete callback
   - Layout: full-width mobile-first card
   - Top section: inspection type toggle (Pre-Trip / Post-Trip) as large tab buttons
   - Vehicle info display: unit number, year/make/model (read-only, from assigned vehicle)
   - Odometer input: numeric field (optional)
   - Main section: 11 FMCSA inspection items from DVIR_INSPECTION_ITEMS
     - Each item renders as a row with: label text + two large toggle buttons (PASS green / FAIL red)
     - Default all items to 'pass' (most common case -- driver taps through quickly)
     - Group items by category (safety, visibility, mechanical, emergency) with subtle dividers
   - When an item is marked FAIL: slide-down text field for defect description (required)
   - Bottom: optional notes textarea, camera upload button (reuse FileUpload for photo evidence)
   - Submit button: large, prominent, disabled until all items have a selection
   - On submit: build FormData from form state, call submitDVIR via useTransition
   - Show success toast on completion, call onComplete
   - Use react-hook-form with zodResolver(dvirSchema)
   - Design goal: driver can complete a no-defect inspection in < 30 seconds (11 taps + submit)

2. Create `src/components/compliance/driver-compliance-view.tsx` as client component:
   - Show driver's personal compliance items (assigned_to = current driver or driver_id = current driver)
   - CDL expiry countdown card: days remaining, color-coded (green > 90 days, yellow 30-90, red < 30)
   - Medical card expiry countdown card: same pattern
   - List of assigned compliance items with status badges
   - Document upload button for each item (reuse FileUpload)

3. Create `src/app/driver/compliance/page.tsx` as server component:
   - Get current user, find linked driver record
   - If driver has assigned vehicle: show DVIR form at top
   - If no assigned vehicle: show message "No vehicle assigned -- contact your dispatcher"
   - Below DVIR: DriverComplianceView with personal items and DQ status
   - Add compliance link to driver PWA bottom navigation (update driver layout if needed)

4. Update driver PWA navigation to include compliance link:
   - Check if driver nav already has compliance link, if not add it
   - Use ClipboardCheck icon from lucide-react
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 type errors"</automated>
  </verify>
  <done>Driver PWA shows DVIR form with 11 pass/fail toggles optimized for speed. Defect fields appear only on fail. Personal compliance items and CDL/medical countdowns visible. DVIR submission creates inspection record.</done>
</task>

<task type="auto">
  <name>Task 2: Owner-Operator simplified compliance dashboard</name>
  <files>
    src/app/oo/compliance/page.tsx,
    src/components/compliance/oo-compliance-dashboard.tsx
  </files>
  <action>
1. Create `src/components/compliance/oo-compliance-dashboard.tsx` as client component:
   - Simplified single-carrier compliance view
   - Section 1: Personal DQ file status (use DQChecklist component from Plan 04 or inline simplified version)
     - Show completeness percentage bar
     - List missing/expired documents
   - Section 2: Vehicle inspection tracker
     - Show assigned vehicle(s) with last inspection date and next due
     - Quick link to log inspection
   - Section 3: Compliance items assigned to this owner-operator
     - List with status badges, due dates, mark complete action
   - Section 4: IFTA log (if carrier has IFTA license)
     - Simple manual entry form for adding miles/gallons by jurisdiction
     - Current quarter summary table
   - Adapt based on is_dot_regulated: non-DOT OO sees only insurance, registration, vehicle inspection sections
   - Mobile-friendly card layout

2. Create `src/app/oo/compliance/page.tsx` as server component:
   - Get current user, find their org's compliance profile
   - If no profile: show setup prompt (same pattern as Command mode)
   - Detect if owner-operator via org_members count === 1 pattern (per Phase 4 convention)
   - Fetch compliance dashboard data scoped to OO's vehicles and driver record
   - Render OOComplianceDashboard
   - Add compliance link to OO navigation if not already present
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 type errors"</automated>
  </verify>
  <done>Owner-Operator has simplified compliance dashboard with personal DQ status, vehicle inspection tracker, assigned compliance items, and IFTA log. View adapts for DOT vs non-DOT carrier type.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
Visit /driver/compliance -- DVIR form and personal compliance items render
Visit /oo/compliance -- simplified compliance dashboard renders
</verification>

<success_criteria>
- DVIR form has 11 pass/fail toggle buttons optimized for fast completion
- All items default to pass for quick no-defect inspections
- Fail items show defect description field
- DVIR submission creates inspection record
- Driver sees personal compliance items and CDL/medical countdowns
- Owner-Operator sees simplified single-carrier compliance dashboard
- DOT vs non-DOT adaptation works in both modes
- Driver PWA navigation includes compliance link
</success_criteria>

<output>
After completion, create `.planning/phases/07-compliance/07-05-SUMMARY.md`
</output>
