---
phase: 07-compliance
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/app/(app)/compliance/page.tsx
  - src/app/(app)/compliance/items/page.tsx
  - src/components/compliance/compliance-dashboard.tsx
  - src/components/compliance/health-score-gauge.tsx
  - src/components/compliance/compliance-items-table.tsx
  - src/components/compliance/upcoming-deadlines.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements:
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-12
  - COMP-13

must_haves:
  truths:
    - "Compliance dashboard shows health score 0-100 with visual gauge"
    - "Dashboard shows active compliance alerts sorted by urgency"
    - "Dashboard shows upcoming deadlines for next 90 days"
    - "Compliance items page lists all items with status, category, due date filters"
    - "Items page supports mark complete action with document upload"
    - "Sidebar shows Compliance link in navigation"
    - "DOT vs non-DOT filtering hides irrelevant categories"
  artifacts:
    - path: "src/app/(app)/compliance/page.tsx"
      provides: "Compliance dashboard page"
      min_lines: 20
    - path: "src/components/compliance/compliance-dashboard.tsx"
      provides: "Dashboard with health score, alerts, upcoming deadlines"
      min_lines: 50
    - path: "src/components/compliance/compliance-items-table.tsx"
      provides: "Filterable compliance items table with actions"
      min_lines: 60
    - path: "src/components/compliance/upcoming-deadlines.tsx"
      provides: "Next 90 days deadline list/calendar"
      min_lines: 30
  key_links:
    - from: "src/components/compliance/compliance-dashboard.tsx"
      to: "src/lib/compliance/actions.ts"
      via: "calls getComplianceDashboard"
      pattern: "getComplianceDashboard"
    - from: "src/components/compliance/compliance-items-table.tsx"
      to: "src/lib/compliance/actions.ts"
      via: "calls getComplianceItems, completeComplianceItem"
      pattern: "getComplianceItems|completeComplianceItem"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "/compliance"
      via: "navigation link"
      pattern: "compliance"
---

<objective>
Compliance dashboard and items management pages in Command mode. The dashboard shows health score, active alerts, and upcoming deadlines. The items page provides a filterable table for managing all compliance obligations.

Purpose: Primary compliance interface for dispatchers and admins to monitor regulatory status and manage obligations.
Output: Dashboard page, items list page, sidebar navigation update.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-compliance/07-CONTEXT.md
@.planning/phases/07-compliance/07-01-SUMMARY.md
@.planning/phases/07-compliance/07-02-SUMMARY.md
@src/components/layout/app-sidebar.tsx
@src/app/(app)/dashboard/page.tsx

<interfaces>
<!-- From Plan 07-01 and 07-02 outputs -->

From src/lib/compliance/actions.ts:
```typescript
export async function getComplianceDashboard(): Promise<{
  healthScore: number;
  metrics: HealthScoreMetrics;
  recentAlerts: ComplianceAlert[];
  upcomingItems: ComplianceItem[];
  profile: ComplianceProfile | null;
}>;
export async function getComplianceItems(filters?): Promise<ComplianceItem[]>;
export async function completeComplianceItem(itemId: string, documentUrls?: string[]): Promise<{ error?: string }>;
export async function createComplianceItem(formData: FormData): Promise<{ error?: string }>;
export async function getComplianceProfile(): Promise<ComplianceProfile | null>;
export async function upsertComplianceProfile(formData: FormData): Promise<{ error?: string }>;
```

From src/components/ui/status-badge.tsx:
```typescript
// 'compliance' and 'inspection' variants available
<StatusBadge status="overdue" variant="compliance" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compliance dashboard page with health score, alerts, and upcoming deadlines</name>
  <files>
    src/app/(app)/compliance/page.tsx,
    src/components/compliance/compliance-dashboard.tsx,
    src/components/compliance/health-score-gauge.tsx,
    src/components/compliance/upcoming-deadlines.tsx,
    src/components/layout/app-sidebar.tsx
  </files>
  <action>
1. Create `src/app/(app)/compliance/page.tsx` as server component:
   - Call getComplianceDashboard() to fetch all dashboard data
   - If no compliance profile exists, render a setup prompt that pre-fills from org data (DOT, MC, company_type) with a "Set Up Compliance" button that calls upsertComplianceProfile
   - Otherwise render ComplianceDashboard component

2. Create `src/components/compliance/health-score-gauge.tsx`:
   - Visual 0-100 gauge using Recharts RadialBarChart (existing dependency) or simple CSS ring
   - Color coded: green >= 80, yellow 50-79, red < 50
   - Show score number prominently in center
   - Below gauge: show metric breakdown (overdue count, due soon count, DQ %, inspection %)

3. Create `src/components/compliance/upcoming-deadlines.tsx`:
   - List view of compliance items due in next 90 days, ordered by due_date
   - Each row: title, category badge, due date, days remaining, assigned entity (driver name or vehicle unit), StatusBadge
   - Color-code rows by urgency: red for overdue, yellow for due within 14 days, default for rest
   - Link each item to /compliance/items with filter

4. Create `src/components/compliance/compliance-dashboard.tsx`:
   - Three-section layout:
     a) Top row: HealthScoreGauge + stat cards (total items, overdue, due soon, completed this month)
     b) Middle: Recent compliance alerts list (last 10 unacknowledged) with acknowledge button using existing acknowledgeAlert pattern. Show severity badge (use alert-helpers getSeverityColor pattern).
     c) Bottom: UpcomingDeadlines component (next 90 days)
   - Adapt layout based on is_dot_regulated: hide DOT-specific sections for non-DOT carriers
   - Quick action buttons: "Add Compliance Item", "View All Items", "DQ File Tracker"

5. Update `src/components/layout/app-sidebar.tsx`:
   - Add Compliance section to navItems: { href: '/compliance', label: 'Compliance', icon: Shield }
   - Import Shield icon from lucide-react
   - Position after existing nav items (after Dashboard, Loads, Drivers, Fleet, Dispatch, Invoices)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 type errors"</automated>
  </verify>
  <done>Compliance dashboard renders health score gauge, stat cards, recent alerts with acknowledge, upcoming deadlines. Sidebar has Compliance link. Setup prompt shown when no profile exists.</done>
</task>

<task type="auto">
  <name>Task 2: Compliance items list page with filters and actions</name>
  <files>
    src/app/(app)/compliance/items/page.tsx,
    src/components/compliance/compliance-items-table.tsx
  </files>
  <action>
1. Create `src/components/compliance/compliance-items-table.tsx` as client component:
   - Props: items (ComplianceItem[]), isDotRegulated (boolean)
   - Filterable table with client-side filters:
     a) Status dropdown: all, upcoming, due_soon, overdue, completed, waived
     b) Category dropdown: all categories appropriate for DOT/non-DOT (use isComplianceModuleVisible)
     c) Search by title
   - Table columns: Title, Category, Status (StatusBadge variant="compliance"), Due Date, Days Remaining, Assigned To, Actions
   - Actions column: "Mark Complete" button (calls completeComplianceItem via useTransition), "Edit" link
   - Mark Complete: opens inline form for optional document URLs (reuse FileUpload pattern), then confirms
   - Sort by due_date ascending by default, with sortable columns
   - Empty state: "No compliance items found" with "Add Item" CTA
   - Use useTransition for all server action calls (per project convention from Phase 3)

2. Create `src/app/(app)/compliance/items/page.tsx` as server component:
   - Accept searchParams for initial filters
   - Call getComplianceItems() with filters from searchParams
   - Call getComplianceProfile() to get is_dot_regulated
   - Render page header with "Add Compliance Item" button
   - Render ComplianceItemsTable with data
   - Add Item button links to inline form or modal for createComplianceItem
   - Include the add-item form inline at top (collapsible) with fields: title, category, due_date, recurrence_rule, description, assigned_to (driver/vehicle select), alert_days_before (preset checkboxes for 90,60,30,14,7,1)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 type errors"</automated>
  </verify>
  <done>Items page shows filterable table of all compliance obligations. Users can filter by status/category, mark items complete with document upload, and add new items inline. DOT/non-DOT filtering works.</done>
</task>

</tasks>

<verification>
npx tsc --noEmit
Visit /compliance -- dashboard renders with health score, alerts, deadlines
Visit /compliance/items -- table renders with filters and actions
</verification>

<success_criteria>
- Compliance dashboard shows health score gauge (0-100) with color coding
- Dashboard shows recent alerts with acknowledge action
- Dashboard shows upcoming deadlines for next 90 days
- Compliance items page has working filters (status, category, search)
- Mark complete action works and triggers recurrence generation
- Add item form creates new compliance items
- Sidebar has Compliance link
- Non-DOT carriers see filtered category list
</success_criteria>

<output>
After completion, create `.planning/phases/07-compliance/07-03-SUMMARY.md`
</output>
