---
phase: 03-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00015_dispatches.sql
  - src/types/database.ts
  - src/lib/dispatch-status.ts
  - src/schemas/dispatch.ts
  - src/hooks/use-realtime-dispatches.ts
  - src/app/(app)/dispatch/actions.ts
  - src/app/driver/dispatch/actions.ts
  - tests/dispatch/status.test.ts
  - tests/dispatch/create.test.ts
  - tests/dispatch/accept-reject.test.ts
  - tests/dispatch/notes.test.ts
  - tests/dispatch/availability.test.ts
autonomous: true
requirements: [DISP-01, DISP-06, DISP-07, DISP-08]

must_haves:
  truths:
    - "Dispatches table exists with RLS, indexes, and partial unique constraint on active load"
    - "Dispatch status transitions are validated (assigned->accepted->en_route_pickup->...->completed, assigned->rejected)"
    - "Creating a dispatch sets load status to dispatched and assigns driver_id/vehicle_id on the load"
    - "Rejecting a dispatch reverts load to booked and clears driver_id/vehicle_id"
    - "Accepting a dispatch sets accepted_at timestamp"
    - "Driver can send notes to dispatcher via updateDriverNotes action"
    - "Dispatch changes broadcast via Supabase Realtime on dispatches table"
  artifacts:
    - path: "supabase/migrations/00015_dispatches.sql"
      provides: "Dispatches table with RLS, indexes, partial unique index, Realtime publication"
      contains: "create table dispatches"
    - path: "src/types/database.ts"
      provides: "DispatchStatus type, Dispatch type, Database.dispatches table entry"
      contains: "DispatchStatus"
    - path: "src/lib/dispatch-status.ts"
      provides: "Dispatch status transitions, labels, colors"
      exports: ["canDispatchTransition", "VALID_DISPATCH_TRANSITIONS", "DISPATCH_STATUSES"]
    - path: "src/schemas/dispatch.ts"
      provides: "Zod schemas for dispatch creation and driver notes"
      exports: ["createDispatchSchema", "driverNotesSchema"]
    - path: "src/hooks/use-realtime-dispatches.ts"
      provides: "Realtime hook for dispatch table changes"
      exports: ["useRealtimeDispatches"]
    - path: "src/app/(app)/dispatch/actions.ts"
      provides: "Server actions for dispatch CRUD from Command mode"
      exports: ["createDispatch", "updateDispatchStatus"]
    - path: "src/app/driver/dispatch/actions.ts"
      provides: "Driver-scoped server actions for dispatch interaction"
      exports: ["acceptDispatch", "rejectDispatch", "updateDriverNotes"]
  key_links:
    - from: "src/app/(app)/dispatch/actions.ts"
      to: "src/lib/dispatch-status.ts"
      via: "canDispatchTransition validation"
      pattern: "canDispatchTransition\\("
    - from: "src/app/(app)/dispatch/actions.ts"
      to: "supabase loads table"
      via: "dispatch-load status sync"
      pattern: "\\.from\\('loads'\\).*\\.update"
    - from: "src/app/driver/dispatch/actions.ts"
      to: "supabase loads table"
      via: "reject reverts load to booked"
      pattern: "status.*booked"
    - from: "src/hooks/use-realtime-dispatches.ts"
      to: "supabase dispatches table"
      via: "postgres_changes subscription"
      pattern: "table.*dispatches"
---

<objective>
Build the dispatch data layer: database migration, TypeScript types, dispatch-status utility, Zod schemas, Realtime hook, and all server actions for dispatch CRUD (Command mode) and driver dispatch interaction (accept/reject/notes).

Purpose: Establish the complete dispatch data foundation so Plan 02 (dispatch board UI) and Plan 03 (Driver PWA dispatch) can build UI against working server actions and types.
Output: Migration file, types, schemas, status utility, Realtime hook, server actions, and unit tests.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dispatch/03-CONTEXT.md
@.planning/phases/03-dispatch/03-RESEARCH.md

@src/types/database.ts
@src/lib/load-status.ts
@src/hooks/use-realtime-loads.ts
@src/components/ui/status-badge.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/database.ts:
```typescript
export type LoadStatus = 'booked' | 'dispatched' | 'in_transit' | 'at_pickup' | 'loaded' | 'at_delivery' | 'delivered' | 'invoiced' | 'paid' | 'canceled'
export type Load = { id: string; org_id: string; status: LoadStatus; driver_id: string | null; vehicle_id: string | null; /* ... */ }
export type Driver = { id: string; org_id: string; user_id: string | null; first_name: string; last_name: string; status: DriverStatus; current_vehicle_id: string | null; /* ... */ }
export type Vehicle = { id: string; org_id: string; unit_number: string; /* ... */ }
```

From src/lib/load-status.ts:
```typescript
export const VALID_TRANSITIONS: Record<LoadStatus, LoadStatus[]>
export function canTransition(from: LoadStatus, to: LoadStatus): boolean
export function getStatusColor(status: LoadStatus): string
export function getStatusLabel(status: LoadStatus): string
```

From src/hooks/use-realtime-loads.ts:
```typescript
export function useRealtimeLoads(orgId: string | null): void
// Pattern: useRef for client, channel with postgres_changes, router.refresh() on change
```

From src/components/ui/status-badge.tsx:
```typescript
type StatusVariant = 'driver' | 'vehicle' | 'load'
export function StatusBadge({ status, variant }: { status: string; variant?: StatusVariant }): JSX.Element
// colorMap Record<StatusVariant, Record<string, { dot, bg, text }>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration, types, dispatch-status utility, Zod schemas, Realtime hook, and tests</name>
  <files>
    supabase/migrations/00015_dispatches.sql,
    src/types/database.ts,
    src/lib/dispatch-status.ts,
    src/schemas/dispatch.ts,
    src/hooks/use-realtime-dispatches.ts,
    src/components/ui/status-badge.tsx,
    tests/dispatch/status.test.ts,
    tests/dispatch/create.test.ts,
    tests/dispatch/accept-reject.test.ts,
    tests/dispatch/notes.test.ts,
    tests/dispatch/availability.test.ts
  </files>
  <behavior>
    - canDispatchTransition('assigned', 'accepted') returns true
    - canDispatchTransition('assigned', 'completed') returns false
    - canDispatchTransition('rejected', anything) returns false
    - canDispatchTransition('completed', anything) returns false
    - All 8 dispatch statuses have labels and colors
    - createDispatchSchema validates load_id (uuid required), driver_id (uuid required), vehicle_id (uuid optional), dispatcher_notes (optional string)
    - driverNotesSchema validates dispatch_id (uuid required), driver_notes (string 1-1000 chars)
    - getDispatchStatusLabel('en_route_pickup') returns 'En Route Pickup'
  </behavior>
  <action>
    1. Create `supabase/migrations/00015_dispatches.sql`:
       - dispatches table per PRD-01 Section 5.2 with all columns: id (uuid PK), org_id (FK organizations), load_id (FK loads), driver_id (FK drivers), vehicle_id (FK vehicles nullable), status (text default 'assigned'), assigned_at, accepted_at, completed_at, estimated_pickup_arrival, estimated_delivery_arrival, driver_notes, dispatcher_notes, assigned_by (FK auth.users), created_at, updated_at
       - RLS: `org_id = (select auth.org_id())` per established pattern
       - Indexes on org_id, load_id, driver_id, status
       - Partial unique index: `CREATE UNIQUE INDEX idx_dispatches_active_load ON dispatches(load_id) WHERE status NOT IN ('completed', 'rejected')` to prevent duplicate active dispatches
       - `ALTER PUBLICATION supabase_realtime ADD TABLE dispatches` for Realtime

    2. Update `src/types/database.ts`:
       - Add `DispatchStatus` type: 'assigned' | 'accepted' | 'en_route_pickup' | 'at_pickup' | 'en_route_delivery' | 'at_delivery' | 'completed' | 'rejected'
       - Add `Dispatch` type matching the migration columns
       - Add `dispatches` entry to the Database type following the established pattern (Row, Insert using Omit for auto fields, Update as Partial)

    3. Create `src/lib/dispatch-status.ts` mirroring load-status.ts exactly:
       - DISPATCH_STATUSES array in order
       - VALID_DISPATCH_TRANSITIONS record: assigned->[accepted,rejected], accepted->[en_route_pickup], en_route_pickup->[at_pickup], at_pickup->[en_route_delivery], en_route_delivery->[at_delivery], at_delivery->[completed], completed->[], rejected->[]
       - canDispatchTransition(from, to) function
       - getDispatchStatusLabel(status) function
       - getDispatchStatusColor(status) function returning Tailwind classes

    4. Create `src/schemas/dispatch.ts`:
       - createDispatchSchema: load_id (z.string().uuid()), driver_id (z.string().uuid()), vehicle_id (z.string().uuid().optional()), estimated_pickup_arrival (z.string().optional()), estimated_delivery_arrival (z.string().optional()), dispatcher_notes (z.string().optional().default(''))
       - Export CreateDispatchInput = z.input<typeof createDispatchSchema>
       - driverNotesSchema: dispatch_id (z.string().uuid()), driver_notes (z.string().min(1).max(1000))

    5. Create `src/hooks/use-realtime-dispatches.ts` cloning use-realtime-loads.ts:
       - Same pattern: useRef for client, channel `org:${orgId}:dispatch`, postgres_changes on dispatches table, router.refresh() callback
       - Filter: `org_id=eq.${orgId}`

    6. Update `src/components/ui/status-badge.tsx`:
       - Add 'dispatch' to StatusVariant union type
       - Add dispatch entry to colorMap: assigned (yellow), accepted (blue), en_route_pickup (indigo), at_pickup (cyan), en_route_delivery (purple), at_delivery (amber), completed (green), rejected (red)

    7. Create test files in `tests/dispatch/`:
       - `status.test.ts`: Test all valid transitions, invalid transitions, terminal states, labels, colors
       - `create.test.ts`: Test createDispatchSchema validation (valid input, missing fields, invalid UUIDs)
       - `accept-reject.test.ts`: Placeholder tests for accept/reject flow logic (test the status transition validation that powers it)
       - `notes.test.ts`: Test driverNotesSchema validation (valid, empty, too long)
       - `availability.test.ts`: Test driver availability categorization logic (will be a pure function that partitions drivers by dispatch status)
  </action>
  <verify>
    <automated>npx vitest run tests/dispatch/ --reporter=verbose</automated>
  </verify>
  <done>
    Migration file exists with dispatches table, RLS, indexes, partial unique index, and Realtime publication. DispatchStatus and Dispatch types added to database.ts. dispatch-status.ts exports transition map and utility functions. Zod schemas validate dispatch creation and driver notes. useRealtimeDispatches hook cloned from loads pattern. StatusBadge extended with dispatch variant. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Server actions for dispatch CRUD and driver dispatch interaction</name>
  <files>
    src/app/(app)/dispatch/actions.ts,
    src/app/driver/dispatch/actions.ts
  </files>
  <action>
    1. Create `src/app/(app)/dispatch/actions.ts` with 'use server':
       - `createDispatch(formData: FormData)`: Parse with createDispatchSchema. Validate load is in 'booked' status. Check driver has no active dispatch (status NOT IN completed, rejected). Insert dispatch record with status='assigned', assigned_at=now(). Update load: set status='dispatched', driver_id, vehicle_id (use driver's current_vehicle_id if vehicle_id not provided). revalidatePath('/dispatch') and revalidatePath('/loads'). Return { error?: string }.
       - `updateDispatchStatus(dispatchId: string, newStatus: DispatchStatus)`: Fetch current dispatch. Validate transition with canDispatchTransition(). Update dispatch status. If newStatus is 'completed', set completed_at. revalidatePath('/dispatch'). Return { error?: string }.

    2. Create `src/app/driver/dispatch/actions.ts` with 'use server':
       - `acceptDispatch(dispatchId: string)`: Authenticate user. Look up driver by user_id. Verify dispatch belongs to driver and is in 'assigned' status. Update dispatch: status='accepted', accepted_at=now(). Load remains 'dispatched'. revalidatePath('/driver/dispatch') and revalidatePath('/dispatch'). Return { error?: string }.
       - `rejectDispatch(dispatchId: string)`: Authenticate user. Look up driver by user_id. Verify dispatch belongs to driver and is in 'assigned' status. Update dispatch: status='rejected'. Update load: status='booked', driver_id=null, vehicle_id=null. revalidatePath('/driver/dispatch') and revalidatePath('/dispatch') and revalidatePath('/loads'). Return { error?: string }.
       - `updateDriverNotes(formData: FormData)`: Parse with driverNotesSchema. Authenticate user. Look up driver by user_id. Verify dispatch belongs to driver. Update dispatch driver_notes field. revalidatePath('/driver/dispatch'). Return { error?: string }.

    Follow established patterns:
    - Use `createClient()` from `@/lib/supabase/server` (async server client)
    - Zod validation before database queries
    - Return `{ error: string }` on failure, `{}` on success
    - revalidatePath for affected routes
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    createDispatch creates dispatch record and syncs load status to 'dispatched'. updateDispatchStatus validates transitions. acceptDispatch sets accepted_at. rejectDispatch reverts load to 'booked' and clears assignment. updateDriverNotes updates driver_notes field. All actions type-check cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/dispatch/ --reporter=verbose` -- all dispatch tests pass
2. `npx tsc --noEmit` -- no type errors
3. Migration file contains dispatches table with RLS, indexes, partial unique index
4. All server actions follow established patterns (Zod validation, Supabase server client, revalidatePath)
</verification>

<success_criteria>
- Dispatches table migration ready for deployment with RLS and Realtime enabled
- DispatchStatus type and Dispatch type added to database.ts with Database table entry
- Dispatch status transitions validated by canDispatchTransition()
- Zod schemas validate dispatch creation and driver notes inputs
- useRealtimeDispatches hook subscribes to dispatch table changes
- StatusBadge supports dispatch variant with 8 status colors
- Server actions handle: create dispatch (with load sync), update status, accept, reject (with load revert), driver notes
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-dispatch/03-01-SUMMARY.md`
</output>
