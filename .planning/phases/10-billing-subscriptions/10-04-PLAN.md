---
phase: 10-billing-subscriptions
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified:
  - src/app/(app)/settings/billing/page.tsx
  - src/app/(app)/settings/billing/billing-content.tsx
  - src/app/(app)/settings/billing/plans/page.tsx
  - src/app/(app)/settings/billing/plans/plan-comparison.tsx
  - src/app/(app)/settings/layout.tsx
  - src/app/(app)/oo/billing/page.tsx
autonomous: true
requirements:
  - BILL-09
  - BILL-10

must_haves:
  truths:
    - "Billing page shows current plan name, status, and trial expiry countdown"
    - "Usage meters display vehicles, drivers, loads, AI queries with used/limit bars"
    - "Upgrade button links to Stripe checkout, Manage Billing opens Stripe portal"
    - "Invoice history table shows past billing invoices with PDF links"
    - "Plan comparison page shows all 4 tiers side-by-side with feature checkmarks"
    - "Annual/monthly toggle shows pricing with annual savings"
    - "Settings nav includes Billing link"
    - "Owner-Operator billing page shows simplified plan and usage view"
  artifacts:
    - path: "src/app/(app)/settings/billing/page.tsx"
      provides: "Server component loading billing data"
      exports: ["default"]
    - path: "src/app/(app)/settings/billing/billing-content.tsx"
      provides: "Client component with plan card, usage meters, payment info, invoice history"
      exports: ["BillingContent"]
    - path: "src/app/(app)/settings/billing/plans/page.tsx"
      provides: "Plan comparison page"
      exports: ["default"]
    - path: "src/app/(app)/settings/billing/plans/plan-comparison.tsx"
      provides: "Client component with plan grid, toggle, CTAs"
      exports: ["PlanComparison"]
  key_links:
    - from: "src/app/(app)/settings/billing/billing-content.tsx"
      to: "/api/billing/create-checkout"
      via: "fetch on upgrade button click"
      pattern: "fetch.*api/billing/create-checkout"
    - from: "src/app/(app)/settings/billing/billing-content.tsx"
      to: "/api/billing/portal"
      via: "fetch on manage billing button click"
      pattern: "fetch.*api/billing/portal"
    - from: "src/app/(app)/settings/layout.tsx"
      to: "/settings/billing"
      via: "nav link added to settingsNav array"
      pattern: "billing"
---

<objective>
Build the billing settings UI with plan overview, usage meters, invoice history, and plan comparison page. Add Owner-Operator billing view.

Purpose: Users need to see their plan, track usage against limits, upgrade/downgrade, view invoices, and compare plans. This is the self-service billing experience.
Output: Billing settings page, plan comparison page, OO billing page, updated settings nav.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-billing-subscriptions/10-02-SUMMARY.md
@.planning/phases/10-billing-subscriptions/10-03-SUMMARY.md
@src/app/(app)/settings/layout.tsx
@src/app/(app)/settings/notifications/page.tsx
@src/lib/billing/plans.ts
@src/lib/billing/enforce.ts
@src/types/database.ts
</context>

<interfaces>
From src/app/(app)/settings/layout.tsx:
```typescript
const settingsNav = [
  { href: '/settings/team', label: 'Team', icon: Users },
  { href: '/settings/notifications', label: 'Notifications', icon: Bell },
]
```

From src/lib/billing/plans.ts (Plan 01):
- PLAN_CONFIG: Record<BillingPlan, PlanConfig> with pricing, limits, features, modules
- PLANS: BillingPlan[]
- formatLimit(value: number): string

From src/lib/billing/enforce.ts (Plan 03):
- getUsageSummary(orgId): { resource: { current, limit, percentage } }

From API routes (Plan 02):
- GET /api/billing/status -> { account, limits, usage }
- POST /api/billing/create-checkout -> { url }
- POST /api/billing/portal -> { url }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Billing settings page with usage meters and invoice history</name>
  <files>src/app/(app)/settings/billing/page.tsx, src/app/(app)/settings/billing/billing-content.tsx, src/app/(app)/settings/layout.tsx, src/app/(app)/oo/billing/page.tsx</files>
  <action>
1. **Update src/app/(app)/settings/layout.tsx**:
   - Add billing to settingsNav: `{ href: '/settings/billing', label: 'Billing', icon: CreditCard }`
   - Import CreditCard from lucide-react

2. **Create src/app/(app)/settings/billing/page.tsx** (server component):
   - Authenticate user, get org_id
   - Query billing_accounts for org (via supabase)
   - Query usage via getUsageSummary(orgId) from enforce.ts
   - Query billing_invoices for org ordered by created_at desc, limit 20
   - Pass data to BillingContent client component
   - Metadata: title "Billing | Manifest"

3. **Create src/app/(app)/settings/billing/billing-content.tsx** (client component):
   - Props: account (BillingAccount), usage (summary object), invoices (BillingInvoice[])

   **Current Plan Card:**
   - Plan name (from PLAN_CONFIG), status badge (trialing/active/past_due/canceled)
   - If trialing: show "Trial ends in X days" countdown
   - If past_due: show warning with "Update payment method" link
   - Billing cycle (monthly/annual) and current rate
   - "Change Plan" link to /settings/billing/plans
   - "Manage Billing" button that POSTs to /api/billing/portal, then window.location = url

   **Usage Meters Section:**
   - For each resource (vehicles, drivers, loads, AI queries):
     - Label, "X of Y" text, progress bar (Tailwind bg-gray-200 with colored fill)
     - Color: green <60%, yellow 60-85%, red >85%
     - If limit is -1 (unlimited): show "X used" with no bar
   - Use the getUsageSummary data

   **Payment Method Card:**
   - If payment_method_last4 exists: show brand icon + "****1234"
   - If not: show "No payment method" with "Add payment method" button (opens portal)

   **Invoice History Table:**
   - Columns: Date, Amount, Status, PDF
   - Status badge (paid=green, open=blue, void=gray)
   - PDF link opens in new tab
   - "View all in Stripe" link at bottom (opens portal)

4. **Create src/app/(app)/oo/billing/page.tsx** (Owner-Operator billing):
   - Same data loading pattern as settings/billing/page.tsx
   - Simplified layout: plan card, usage meters, upgrade prompt if on free
   - "View Invoices" link
   - Reuse BillingContent component with `simplified` prop or create OOBillingContent

Follow existing page patterns: server component loads data, client component renders with interactivity. Use Tailwind classes consistent with existing settings pages.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Billing settings page shows plan card, usage meters with progress bars, payment method, invoice history table. Settings nav includes Billing. OO billing page exists with simplified view.</done>
</task>

<task type="auto">
  <name>Task 2: Plan comparison page with annual/monthly toggle</name>
  <files>src/app/(app)/settings/billing/plans/page.tsx, src/app/(app)/settings/billing/plans/plan-comparison.tsx</files>
  <action>
1. **Create src/app/(app)/settings/billing/plans/page.tsx** (server component):
   - Authenticate user, get org_id
   - Query billing_accounts for current plan
   - Pass currentPlan to PlanComparison component
   - Metadata: title "Plans | Manifest"

2. **Create src/app/(app)/settings/billing/plans/plan-comparison.tsx** (client component):
   - Props: currentPlan (BillingPlan)

   **Monthly/Annual Toggle:**
   - useState for billingCycle ('monthly' | 'annual')
   - Toggle switch at top: "Monthly" / "Annual (save 17%)"
   - When annual selected, show annual prices from PLAN_CONFIG

   **Plan Grid** (4 columns on desktop, stacked on mobile):
   For each plan in PLANS:
   - Plan name as heading
   - Price: "$X/mo" or "$X/yr" based on toggle. Free = "Free forever". Enterprise = "Custom pricing"
   - Description from PLAN_CONFIG
   - Feature list with checkmarks (green check) or X (gray) for each feature
   - Limits shown: "X vehicles", "X drivers", "X loads/mo", "X AI queries/mo"
   - Module access: Compliance, IFTA, CRM, AI, API, White Label
   - CTA button:
     - Current plan: "Current Plan" (disabled, outlined)
     - Downgrade: "Downgrade" (gray button)
     - Upgrade: "Upgrade" (primary button, filled)
     - Enterprise: "Contact Sales" (outline button, mailto:sales@glomatrix.com)
   - "Current" badge on active plan

   **Upgrade/Downgrade Action:**
   - On CTA click (not enterprise): POST to /api/billing/create-checkout with { plan, billingCycle }
   - Redirect to returned Stripe checkout URL
   - Show loading state on button while fetching

   **Responsive:** Grid cols: 1 on mobile, 2 on md, 4 on lg. Cards have border, rounded corners, shadow. Professional plan gets "Most Popular" badge and highlighted border (primary color).

   Use PLAN_CONFIG as single source of truth for all plan data. Do NOT hardcode plan details in the component.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Plan comparison page shows 4 tiers in grid with feature checkmarks, monthly/annual toggle with pricing, upgrade CTA redirects to Stripe checkout, current plan badged</done>
</task>

</tasks>

<verification>
- tsc compiles cleanly
- /settings/billing renders plan card, usage meters, payment, invoices
- /settings/billing/plans renders comparison grid with toggle
- Settings nav shows Billing link with CreditCard icon
- /oo/billing renders simplified billing view
</verification>

<success_criteria>
Users can view their current plan and usage, compare plans with pricing toggle, initiate upgrade via Stripe checkout, manage payment via Stripe portal, and view invoice history. Owner-operators have a simplified billing page.
</success_criteria>

<output>
After completion, create `.planning/phases/10-billing-subscriptions/10-04-SUMMARY.md`
</output>
