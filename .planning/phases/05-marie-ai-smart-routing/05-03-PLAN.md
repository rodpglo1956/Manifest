---
phase: 05-marie-ai-smart-routing
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/dispatch/driver-suggestions.tsx
  - src/components/dispatch/dispatch-assignment-form.tsx
  - src/app/(app)/dispatch/dispatch-board.tsx
  - tests/routing/suggestions.test.ts
autonomous: true
requirements:
  - ROUT-03
  - ROUT-04
  - ROUT-05

must_haves:
  truths:
    - "Dispatch UI shows a Suggested tab with ranked driver recommendations"
    - "Each suggestion shows driver name, overall score, and per-factor breakdown"
    - "Dispatcher can one-click assign a driver from the suggestion list"
    - "Dispatcher can switch to manual selection to override suggestions"
  artifacts:
    - path: "src/components/dispatch/driver-suggestions.tsx"
      provides: "Ranked suggestion list with score breakdown"
      exports: ["DriverSuggestions"]
    - path: "src/components/dispatch/dispatch-assignment-form.tsx"
      provides: "Enhanced form with Suggested/Manual tabs"
  key_links:
    - from: "src/components/dispatch/driver-suggestions.tsx"
      to: "/api/dispatch/suggest"
      via: "fetch POST to get suggestions"
      pattern: "fetch.*api/dispatch/suggest"
    - from: "src/components/dispatch/driver-suggestions.tsx"
      to: "src/app/(app)/dispatch/actions.ts"
      via: "createDispatch for one-click assign"
      pattern: "createDispatch"
---

<objective>
Integrate smart routing suggestions into the dispatch UI. Add a "Suggested" tab to the dispatch assignment form showing ranked driver recommendations with score breakdown and one-click assign.

Purpose: Dispatchers see AI-powered driver recommendations when assigning loads, increasing dispatch efficiency and driver-load fit.

Output: Enhanced dispatch assignment form with Suggested/Manual tabs and one-click assign from suggestions.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-marie-ai-smart-routing/05-CONTEXT.md
@.planning/phases/05-marie-ai-smart-routing/05-RESEARCH.md
@.planning/phases/05-marie-ai-smart-routing/05-01-SUMMARY.md

@src/components/dispatch/dispatch-assignment-form.tsx
@src/app/(app)/dispatch/actions.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/marie.ts (created in Plan 01):
```typescript
export type DriverSuggestion = {
  driver_id: string
  driver_name: string
  score: number        // 0-100
  factors: ScoringFactors
}
export type ScoringFactors = {
  proximity: number    // 0-1
  availability: number // 0-1
  equipment: number    // 0-1
  performance: number  // 0-1
  lane: number         // 0-1
}
```

API contract (from Plan 01):
```
POST /api/dispatch/suggest
Body: { load_id: string }
Response: { suggestions: DriverSuggestion[] }
```

Existing dispatch form props:
```typescript
interface DispatchAssignmentFormProps {
  load: Load
  drivers: Driver[]
  vehicles: Pick<Vehicle, 'id' | 'unit_number' | 'make' | 'model'>[]
  onClose: () => void
}
```

From src/app/(app)/dispatch/actions.ts:
```typescript
export async function createDispatch(formData: FormData): Promise<{ error?: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver suggestions component with score visualization</name>
  <files>
    src/components/dispatch/driver-suggestions.tsx,
    tests/routing/suggestions.test.ts
  </files>
  <action>
    1. Create src/components/dispatch/driver-suggestions.tsx ('use client'):
       - Props: { loadId: string; vehicles: Vehicle[]; onClose: () => void }
       - On mount, fetch POST /api/dispatch/suggest with { load_id: loadId }
       - Loading state: skeleton cards while fetching
       - Error state: "Could not load suggestions. Try manual assignment."
       - Empty state: "No available drivers found for this load."
       - For each suggestion, render a card showing:
         a. Driver name and overall score (e.g., "Score: 85/100") with color coding (green >70, yellow 40-70, red <40)
         b. Factor breakdown as horizontal bars or percentage labels:
            - Proximity: [score] (icon: MapPin)
            - Availability: [score] (icon: CheckCircle)
            - Equipment: [score] (icon: Truck)
            - Performance: [score] (icon: TrendingUp)
            - Lane: [score] (icon: Route)
         c. "Assign" button that calls createDispatch with the driver_id and loadId
       - One-click assign: constructs FormData with load_id, driver_id, calls createDispatch
       - Show isPending state on assign button
       - On successful assign, call onClose
       - Use useTransition for the assign action (project convention)

    2. Create tests/routing/suggestions.test.ts:
       - Test DriverSuggestions renders loading state initially
       - Test DriverSuggestions renders suggestion cards after fetch resolves
       - Test score color: >70 green, 40-70 yellow, <40 red
       - Test assign button calls createDispatch with correct driver_id
       - Mock fetch for /api/dispatch/suggest
  </action>
  <verify>
    <automated>npx vitest run tests/routing/suggestions.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Suggestion cards display driver name, score, and 5-factor breakdown
    - Score is color-coded by range
    - One-click assign calls createDispatch and closes form
    - Tests pass for rendering and interaction
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Suggested/Manual tabs to dispatch assignment form</name>
  <files>
    src/components/dispatch/dispatch-assignment-form.tsx,
    src/app/(app)/dispatch/dispatch-board.tsx
  </files>
  <action>
    1. Update src/components/dispatch/dispatch-assignment-form.tsx:
       - Add tab state: 'suggested' | 'manual' (default 'suggested')
       - Render two tab buttons above the form content: "Suggested" and "Manual"
       - "Suggested" tab label shows a sparkle/star icon to indicate AI
       - When 'suggested' tab active: render DriverSuggestions component (passing load.id, vehicles, onClose)
       - When 'manual' tab active: render existing manual assignment form (current form content)
       - ROUT-05: The manual tab IS the override -- switching to manual tab = overriding suggestions
       - Keep all existing manual form logic intact (driver dropdown, vehicle dropdown, notes, submit)
       - Tab styling: active tab has primary border-bottom, inactive is gray

    2. Review src/app/(app)/dispatch/dispatch-board.tsx:
       - Verify the dispatch board passes vehicles to the assignment form
       - If vehicles are not currently passed, add vehicles fetch to the dispatch board page and pass through
       - No other changes needed -- the assignment form is already rendered inline below loads panel

    Note: The "Override" button (ROUT-05) is implemented as the "Manual" tab. When dispatcher clicks Manual tab, they get the existing driver dropdown for manual selection. This is the override path.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/routing/ --reporter=verbose</automated>
  </verify>
  <done>
    - Dispatch assignment form has Suggested (default) and Manual tabs
    - Suggested tab shows ranked driver suggestions from smart routing API
    - Manual tab shows existing driver dropdown form (override path)
    - One-click assign from suggestions creates dispatch
    - Existing manual assignment flow unchanged
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/routing/ --reporter=verbose` -- all routing tests pass
- `npx tsc --noEmit` -- no type errors
- Dispatch assignment form shows tabs
- Suggestions load when Suggested tab is active
</verification>

<success_criteria>
- Dispatch assignment form has Suggested/Manual tabs
- Suggested tab fetches and displays ranked driver suggestions with score breakdown
- Each suggestion shows driver name, overall score (0-100), and 5 factor scores
- One-click assign from suggestion list creates dispatch successfully
- Manual tab preserves existing driver dropdown form for override
- No regression in existing dispatch assignment functionality
</success_criteria>

<output>
After completion, create `.planning/phases/05-marie-ai-smart-routing/05-03-SUMMARY.md`
</output>
