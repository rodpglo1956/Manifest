---
phase: 05-marie-ai-smart-routing
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/marie/marie-button.tsx
  - src/components/marie/marie-panel.tsx
  - src/components/marie/marie-chat.tsx
  - src/components/marie/marie-message.tsx
  - src/components/marie/use-marie.ts
  - src/app/(app)/layout.tsx
  - src/app/driver/layout.tsx
  - src/components/marie/marie-driver-chat.tsx
  - tests/marie/chat.test.ts
autonomous: true
requirements:
  - MARI-01
  - MARI-08
  - MARI-09
  - MARI-10

must_haves:
  truths:
    - "Marie floating button appears on every page in Command and Owner-Operator modes"
    - "Clicking Marie button opens a slide-out chat panel on the right side"
    - "User can type a question and receive a response from Marie"
    - "Marie responses with action markers render inline action buttons"
    - "Driver PWA has a simplified Marie chat for questions only"
    - "Marie button shows alert badge placeholder (count=0, wired in Phase 6)"
  artifacts:
    - path: "src/components/marie/marie-button.tsx"
      provides: "Floating Marie button with alert badge"
      exports: ["MarieButton"]
    - path: "src/components/marie/marie-panel.tsx"
      provides: "Slide-out chat panel container"
      exports: ["MariePanel"]
    - path: "src/components/marie/marie-chat.tsx"
      provides: "Chat thread with input"
      exports: ["MarieChat"]
    - path: "src/components/marie/marie-message.tsx"
      provides: "Message rendering with action button parsing"
      exports: ["MarieMessage"]
    - path: "src/components/marie/use-marie.ts"
      provides: "Client hook for Marie API calls"
      exports: ["useMarie"]
    - path: "src/components/marie/marie-driver-chat.tsx"
      provides: "Simplified driver PWA Marie chat"
      exports: ["MarieDriverChat"]
  key_links:
    - from: "src/components/marie/use-marie.ts"
      to: "/api/marie/query"
      via: "fetch POST"
      pattern: "fetch.*api/marie/query"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/marie/marie-button.tsx"
      via: "component import in layout"
      pattern: "MarieButton|MariePanel"
    - from: "src/app/driver/layout.tsx"
      to: "src/components/marie/marie-driver-chat.tsx"
      via: "component import in driver layout"
      pattern: "MarieDriverChat"
---

<objective>
Build the Marie AI chat UI: floating button, slide-out panel, chat thread with message rendering and inline action buttons, and simplified driver PWA chat. Integrate into app and driver layouts.

Purpose: Users can interact with Marie from any page. Command/Owner-Operator modes get full chat with action buttons. Driver PWA gets simplified question-only chat.

Output: Complete Marie chat UI integrated into all layouts.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-marie-ai-smart-routing/05-CONTEXT.md
@.planning/phases/05-marie-ai-smart-routing/05-RESEARCH.md
@.planning/phases/05-marie-ai-smart-routing/05-01-SUMMARY.md

@src/app/(app)/layout.tsx
@src/app/driver/layout.tsx
@src/types/marie.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/marie.ts (created in Plan 01):
```typescript
export type MarieMessage = { role: 'user' | 'assistant'; content: string }
export type MarieQueryRequest = { query: string }
export type MarieQueryResponse = { response: string; usage?: { input_tokens: number; output_tokens: number } }
export type ActionButton = { type: 'view_load' | 'dispatch_driver' | 'generate_invoice'; entityId: string; label: string }
```

API contract (from Plan 01):
```
POST /api/marie/query
Body: { query: string }
Response: { response: string, usage: { input_tokens, output_tokens } }
```

Action button format in Marie responses:
```
[ACTION:view_load:UUID:View Load #1234]
[ACTION:dispatch_driver:UUID:Dispatch Driver Name]
[ACTION:generate_invoice:UUID:Generate Invoice]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Marie chat components (button, panel, chat, message, hook) and layout integration</name>
  <files>
    src/components/marie/marie-button.tsx,
    src/components/marie/marie-panel.tsx,
    src/components/marie/marie-chat.tsx,
    src/components/marie/marie-message.tsx,
    src/components/marie/use-marie.ts,
    src/app/(app)/layout.tsx
  </files>
  <action>
    1. Create src/components/marie/use-marie.ts (client hook):
       - useState for messages: MarieMessage[]
       - useState for isLoading: boolean
       - sendQuery(query: string): appends user message, POST to /api/marie/query, appends assistant response
       - clearMessages(): resets messages array
       - Error handling: on fetch failure, append assistant message "Sorry, I ran into an issue. Try again in a moment."

    2. Create src/components/marie/marie-message.tsx ('use client'):
       - Parse action markers from text using regex: /\[ACTION:(\w+):([a-f0-9-]+):([^\]]+)\]/g
       - Render clean text (markers removed) + action buttons below
       - Action buttons: "View Load" navigates to /loads/[id], "Dispatch Driver" navigates to /dispatch, "Generate Invoice" calls /api/invoices or navigates
       - User messages: right-aligned, primary background
       - Assistant messages: left-aligned, gray background
       - Use Next.js useRouter for navigation from action buttons

    3. Create src/components/marie/marie-chat.tsx ('use client'):
       - Receives messages, isLoading, onSend from props
       - Renders message list (scrollable, auto-scroll to bottom on new message)
       - Text input at bottom with send button (Lucide Send icon)
       - Submit on Enter key (not Shift+Enter)
       - Show typing indicator when isLoading ("Marie is thinking...")
       - Empty state: "Ask Marie anything about your operations"

    4. Create src/components/marie/marie-panel.tsx ('use client'):
       - Slide-out panel: fixed right-0 top-0 bottom-0, w-[400px], z-50
       - Transition: translate-x transform with transition-transform duration-300
       - Header: "Marie" title with close (X) button
       - Body: MarieChat component
       - Backdrop overlay (semi-transparent) that closes panel on click
       - Uses useMarie hook internally

    5. Create src/components/marie/marie-button.tsx ('use client'):
       - Fixed position bottom-6 right-6, z-40
       - Circular button, bg-[#EC008C] (brand pink per CONTEXT.md), white icon
       - Use Lucide MessageCircle or Sparkles icon
       - Alert badge: small red circle with count, top-right corner
       - For now, alertCount=0 (placeholder for Phase 6 proactive_alerts)
       - onClick toggles MariePanel visibility
       - Renders MariePanel as sibling (panel manages its own open/close state via prop)

    6. Update src/app/(app)/layout.tsx:
       - Import and render MarieButton + MariePanel at layout level (after children)
       - This makes Marie available on every Command and Owner-Operator page
       - Use a client wrapper component if needed to manage open state between button and panel
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/marie/ --reporter=verbose</automated>
  </verify>
  <done>
    - Marie floating button (pink, bottom-right) appears on all (app) pages
    - Clicking button opens slide-out panel with chat interface
    - User can type query, see response from Marie API
    - Action markers in responses render as clickable buttons
    - Panel closes via X button or backdrop click
    - Alert badge renders (count=0 placeholder)
  </done>
</task>

<task type="auto">
  <name>Task 2: Driver PWA simplified Marie chat and chat test scaffolds</name>
  <files>
    src/components/marie/marie-driver-chat.tsx,
    src/app/driver/layout.tsx,
    tests/marie/chat.test.ts
  </files>
  <action>
    1. Create src/components/marie/marie-driver-chat.tsx ('use client'):
       - Simplified version of Marie chat for Driver PWA
       - Same floating button pattern but smaller, positioned for mobile
       - Chat panel: full-screen slide-up on mobile (not side panel)
       - Uses useMarie hook but displays a note: "Ask me about your loads and schedule"
       - No action buttons rendered (driver cannot execute actions)
       - Filter out action markers from responses before display (strip [ACTION:...] patterns)
       - Simpler styling appropriate for mobile PWA

    2. Update src/app/driver/layout.tsx:
       - Import and render MarieDriverChat at layout level
       - Makes simplified Marie available on all driver PWA pages

    3. Create tests/marie/chat.test.ts:
       - Test MarieMessage action parsing: text with [ACTION:view_load:uuid:Label] extracts { type, entityId, label } and clean text
       - Test MarieMessage with no actions: renders text only, no buttons
       - Test useMarie hook: sendQuery appends user + assistant messages (mock fetch)
       - Test driver chat strips action markers from displayed text
       - Mock fetch for /api/marie/query returning { response: "test response" }
  </action>
  <verify>
    <automated>npx vitest run tests/marie/chat.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - Driver PWA has Marie chat accessible from all pages
    - Driver Marie is question-only (no action buttons displayed)
    - Action markers stripped from driver-facing responses
    - Tests pass for message parsing and hook behavior
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/marie/ --reporter=verbose` -- all Marie tests pass
- `npx tsc --noEmit` -- no type errors
- Marie button visible in (app) layout
- Marie driver chat visible in driver layout
</verification>

<success_criteria>
- Marie floating button (#EC008C pink) appears on every (app) page (Command + Owner-Operator)
- Clicking button opens slide-out panel with chat interface on right side
- User can type a question and see Marie's response
- Responses with [ACTION:...] markers render as inline clickable buttons
- Alert badge shows on Marie button (count=0 placeholder)
- Driver PWA has simplified Marie with no action execution
- Panel closes via X or backdrop click
</success_criteria>

<output>
After completion, create `.planning/phases/05-marie-ai-smart-routing/05-02-SUMMARY.md`
</output>
