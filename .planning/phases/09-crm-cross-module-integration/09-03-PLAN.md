---
phase: 09-crm-cross-module-integration
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/app/(app)/crm/companies/page.tsx
  - src/app/(app)/crm/companies/[id]/page.tsx
  - src/components/crm/company-form.tsx
  - src/components/crm/contact-list.tsx
  - src/components/crm/company-activity-timeline.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [CRM-01, CRM-02]

must_haves:
  truths:
    - "User can create, view, edit companies with type-based tabs"
    - "Company detail shows contacts, activity timeline, and aggregate stats"
    - "Contacts can be added per company with primary contact designation"
    - "CRM section appears in sidebar navigation"
  artifacts:
    - path: "src/app/(app)/crm/companies/page.tsx"
      provides: "Company list with type tabs"
      min_lines: 80
    - path: "src/app/(app)/crm/companies/[id]/page.tsx"
      provides: "Company detail with contacts, lanes, activities"
      min_lines: 100
    - path: "src/components/crm/company-form.tsx"
      provides: "Company create/edit form with Zod validation"
      min_lines: 60
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Updated sidebar with CRM section"
      contains: "crm"
  key_links:
    - from: "src/app/(app)/crm/companies/page.tsx"
      to: "src/app/(app)/crm/actions.ts"
      via: "Server action imports"
      pattern: "getCompanies|createCompany"
    - from: "src/components/crm/company-form.tsx"
      to: "src/lib/crm/schemas.ts"
      via: "Zod schema for form validation"
      pattern: "companySchema"
---

<objective>
Build company CRUD UI with type-based tabs (Customers, Brokers, Vendors, Prospects), company detail page with contacts and activity timeline, and add CRM section to sidebar navigation.

Purpose: Primary CRM interface for managing business relationships.
Output: Company list page, detail page, form component, contact list, sidebar update.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-crm-cross-module-integration/09-01-SUMMARY.md
@src/components/layout/app-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Company list page with type tabs and form</name>
  <files>src/app/(app)/crm/companies/page.tsx, src/components/crm/company-form.tsx, src/components/layout/app-sidebar.tsx</files>
  <action>
**src/app/(app)/crm/companies/page.tsx:**
Server component page with client wrapper for interactivity (per project convention of URL-based filtering).

- Top bar: "Companies" title + "Add Company" button (opens inline form via URL param ?addCompany=true, per 07-03 convention)
- Tab navigation: All | Customers | Brokers | Vendors | Prospects -- using searchParams `?type=customer` etc.
- Each tab fetches companies filtered by type using getCompanies action
- Company list as table rows showing: name, type badge, MC/DOT, primary contact, total_revenue (formatted), total_loads, last_load_date (relative time), status badge
- Revenue badge: green for >$100k, yellow for >$10k, gray otherwise
- Click row navigates to /crm/companies/[id]
- Search input filters by name/MC/DOT (debounced URL param ?search=)
- Empty state: "No companies found. Add your first customer, broker, or vendor."

**src/components/crm/company-form.tsx:**
Client component using react-hook-form + zodResolver(companySchema).
- Fields: name, company_type (select), mc_number, dot_number, credit_score, days_to_pay, payment_terms (select: Net 15/30/45/60/90), factoring_company
- Primary contact section: name, email, phone
- Address section: line1, city, state (select US states), zip
- Optional: website, notes, status (select), tags (comma-separated input)
- Submit calls createCompany or updateCompany action
- Edit mode: pre-populates from existing company data
- Use z.input for form type (per project convention)

**src/components/layout/app-sidebar.tsx:**
Add CRM section to navItems array after Compliance, before Settings:
```
{
  href: '/crm',
  label: 'CRM',
  icon: Building2, // from lucide-react
  active: true,
  subItems: [
    { href: '/crm', label: 'Dashboard', icon: BarChart3 },
    { href: '/crm/companies', label: 'Companies', icon: Building2 },
    { href: '/crm/lanes', label: 'Lanes', icon: Route },
    { href: '/crm/activities', label: 'Activities', icon: MessageSquare },
  ],
}
```
Import Building2, Route, MessageSquare from lucide-react. Follow existing Fleet sub-nav pattern with collapsible group.
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Company list page renders with type tabs, search, add form. Sidebar shows CRM section with sub-navigation. Company form validates with Zod.</done>
</task>

<task type="auto">
  <name>Task 2: Company detail page with contacts and activity timeline</name>
  <files>src/app/(app)/crm/companies/[id]/page.tsx, src/components/crm/contact-list.tsx, src/components/crm/company-activity-timeline.tsx</files>
  <action>
**src/app/(app)/crm/companies/[id]/page.tsx:**
Server component that calls getCompanyDetail(id) and renders:

- Back link to /crm/companies
- Company header: name, type badge, status badge, edit button (toggles inline form via URL param)
- Info card: MC/DOT, credit score, days to pay, payment terms, factoring company, address, website
- Stats row: total_revenue (large number), total_loads, avg_rate_per_mile, last_load_date
- Tabbed sections (URL param ?tab=contacts|lanes|rates|activities):
  - **Contacts tab**: ContactList component with add contact inline form
  - **Lanes tab**: List of lanes associated via crm_lane_companies (link to /crm/lanes/[id])
  - **Rate Agreements tab**: Table of rate agreements (rate_type, rate_amount, effective_date, expiry_date, status badge). Expiring within 30 days highlighted yellow.
  - **Activities tab**: CompanyActivityTimeline component

**src/components/crm/contact-list.tsx:**
Client component showing contacts for a company.
- List of contacts: name, title, email (mailto link), phone (tel link), primary badge
- "Add Contact" button at top (toggles inline form, per project convention)
- Inline form uses contactSchema with zodResolver
- Set as Primary button on each contact (calls updateContact with is_primary=true)
- Edit icon on each contact toggles edit mode

**src/components/crm/company-activity-timeline.tsx:**
Client component showing activities for a company in chronological order.
- Each activity: type icon (phone for call, mail for email, sticky-note for note, calendar for meeting, etc.), subject, body preview, timestamp (relative), user name, outcome if completed
- Follow-up indicator: if follow_up_date exists and not completed, show "Follow-up due" badge with date. Red if overdue, yellow if today, gray if future.
- "Log Activity" button at top: inline form with activitySchema, activity_type select, subject, body textarea, follow_up_date date picker
- Reuse ActivityFeed visual pattern from dashboard (icon + description + timestamp layout) but extended with CRM-specific fields
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Company detail page shows all company info, contacts with CRUD, activity timeline with inline logging. Type-safe throughout.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- /crm/companies page renders with tabs and search
- /crm/companies/[id] page renders with contacts, lanes, rates, activities tabs
- Sidebar shows CRM section with sub-navigation
</verification>

<success_criteria>
Company management fully functional: users can create/edit companies, manage contacts with primary designation, view activity timelines, and navigate via sidebar.
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm-cross-module-integration/09-03-SUMMARY.md`
</output>
