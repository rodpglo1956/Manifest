---
phase: 09-crm-cross-module-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00025_crm_tables.sql
  - src/types/database.ts
  - src/lib/crm/schemas.ts
  - src/lib/crm/helpers.ts
autonomous: true
requirements: [CRM-01, CRM-02, CRM-03, CRM-05]

must_haves:
  truths:
    - "CRM database tables exist with RLS policies for org isolation"
    - "TypeScript types match all CRM table schemas"
    - "Zod validation schemas cover company, contact, lane, rate agreement, and activity forms"
  artifacts:
    - path: "supabase/migrations/00025_crm_tables.sql"
      provides: "6 CRM tables with RLS, triggers, indexes"
      contains: "crm_companies"
    - path: "src/types/database.ts"
      provides: "CRM TypeScript types and Database table definitions"
      contains: "CrmCompany"
    - path: "src/lib/crm/schemas.ts"
      provides: "Zod schemas for all CRM forms"
      exports: ["companySchema", "contactSchema", "laneSchema", "rateAgreementSchema", "activitySchema"]
    - path: "src/lib/crm/helpers.ts"
      provides: "CRM utility functions"
      exports: ["formatCompanyType", "getAgreementStatus", "isAgreementExpiring"]
  key_links:
    - from: "src/types/database.ts"
      to: "supabase/migrations/00025_crm_tables.sql"
      via: "Type definitions match table columns"
      pattern: "CrmCompany|CrmContact|CrmLane"
---

<objective>
Create CRM database foundation: 6 tables (crm_companies, crm_contacts, crm_lanes, crm_lane_companies, crm_rate_agreements, crm_activities) with RLS policies, TypeScript types, Zod validation schemas, and helper utilities.

Purpose: All CRM plans depend on this schema and type foundation.
Output: Migration file, updated database types, Zod schemas, helper functions.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CRM database migration with RLS</name>
  <files>supabase/migrations/00025_crm_tables.sql</files>
  <action>
Create migration 00025_crm_tables.sql with all 6 CRM tables per PRD-03 Section 4.2 schema:

1. **crm_companies** - name, company_type (customer/broker/vendor/partner/prospect), mc_number, dot_number, credit_score, days_to_pay, payment_terms, factoring_company, primary_contact_name/email/phone, address fields (line1, line2, city, state, zip), website, notes, status (active/inactive/blacklisted/prospect), tags TEXT[], aggregate fields (total_revenue NUMERIC(14,2) DEFAULT 0, total_loads INTEGER DEFAULT 0, avg_rate_per_mile NUMERIC(8,4), last_load_date DATE), org_id, timestamps

2. **crm_contacts** - org_id, company_id FK, first_name, last_name, title, email, phone, is_primary BOOLEAN DEFAULT false, notes, timestamps

3. **crm_lanes** - org_id, origin_city, origin_state, origin_zip, destination_city, destination_state, destination_zip, distance_miles INTEGER, avg_rate_per_mile NUMERIC(8,4), last_rate NUMERIC(10,2), last_run_date DATE, total_runs INTEGER DEFAULT 0, preferred_equipment TEXT[], notes, status (active/inactive/seasonal), timestamps

4. **crm_lane_companies** - lane_id FK, company_id FK, relationship (shipper/broker/receiver), contracted_rate NUMERIC(10,2), contract_start DATE, contract_end DATE, created_at

5. **crm_rate_agreements** - org_id, company_id FK, lane_id FK (nullable), rate_type (per_mile/flat_rate/percentage/hourly), rate_amount NUMERIC(10,4), effective_date DATE, expiry_date DATE, min_volume INTEGER, equipment_type TEXT, document_url TEXT, status (active/expired/pending/rejected), created_at

6. **crm_activities** - org_id, activity_type (call/email/note/meeting/rate_negotiation/load_booked/issue/follow_up/system), company_id FK (nullable), contact_id FK (nullable), lane_id FK (nullable), user_id UUID NOT NULL, subject TEXT, body TEXT, scheduled_at TIMESTAMPTZ, completed_at TIMESTAMPTZ, outcome TEXT, follow_up_date DATE, created_at

RLS policies for all 6 tables using project convention: `USING (org_id = (select auth.org_id()))` for direct org_id tables. For crm_lane_companies, use subquery through crm_lanes. Enable RLS on all tables. Add FOR ALL policies with USING and WITH CHECK.

Add indexes: crm_companies(org_id, company_type), crm_contacts(org_id, company_id), crm_lanes(org_id), crm_rate_agreements(org_id, company_id, status), crm_activities(org_id, company_id, follow_up_date).

Add updated_at trigger for crm_companies, crm_contacts, crm_lanes (reuse existing trigger function from prior migrations).
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && grep -c "CREATE TABLE" supabase/migrations/00025_crm_tables.sql | grep -q "6" && grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/00025_crm_tables.sql | grep -q "6" && echo "PASS"</automated>
  </verify>
  <done>6 CRM tables created with RLS policies, indexes, and triggers. Migration file ready for supabase db push.</done>
</task>

<task type="auto">
  <name>Task 2: CRM TypeScript types, Zod schemas, and helpers</name>
  <files>src/types/database.ts, src/lib/crm/schemas.ts, src/lib/crm/helpers.ts</files>
  <action>
**src/types/database.ts** - Add CRM types after existing Fleet types:

Add union types:
- `CrmCompanyType = 'customer' | 'broker' | 'vendor' | 'partner' | 'prospect'`
- `CrmCompanyStatus = 'active' | 'inactive' | 'blacklisted' | 'prospect'`
- `CrmLaneStatus = 'active' | 'inactive' | 'seasonal'`
- `CrmActivityType = 'call' | 'email' | 'note' | 'meeting' | 'rate_negotiation' | 'load_booked' | 'issue' | 'follow_up' | 'system'`
- `CrmRateType = 'per_mile' | 'flat_rate' | 'percentage' | 'hourly'`
- `CrmAgreementStatus = 'active' | 'expired' | 'pending' | 'rejected'`
- `CrmLaneRelationship = 'shipper' | 'broker' | 'receiver'`

Add Row types: CrmCompany, CrmContact, CrmLane, CrmLaneCompany, CrmRateAgreement, CrmActivity -- matching migration columns exactly.

Add Database table entries for all 6 tables with Row/Insert/Update types following project convention (Omit id/created_at/updated_at for Insert, make nullable fields optional; Partial for Update).

**src/lib/crm/schemas.ts** - Zod validation schemas:
- `companySchema` - name required (3-100 chars), company_type enum, optional: mc_number, dot_number, credit_score, days_to_pay (positive int), payment_terms, factoring_company, primary_contact fields, address fields, website (url or empty), notes, status enum, tags array. Use z.input for CompanyInput type (per project convention with zodResolver).
- `contactSchema` - first_name required, last_name required, company_id required UUID, optional: title, email (email format or empty), phone, is_primary boolean, notes
- `laneSchema` - origin_city, origin_state required, destination_city, destination_state required, optional: origin_zip, destination_zip, distance_miles (positive int), preferred_equipment array, notes, status enum
- `rateAgreementSchema` - company_id required UUID, rate_type enum, rate_amount required (positive number), effective_date required (date string), optional: lane_id UUID, expiry_date, min_volume (positive int), equipment_type, document_url, status enum
- `activitySchema` - activity_type enum required, subject required, optional: company_id UUID, contact_id UUID, lane_id UUID, body, scheduled_at datetime, outcome, follow_up_date date

**src/lib/crm/helpers.ts** - Utility functions:
- `formatCompanyType(type: CrmCompanyType): string` - title case display
- `formatActivityType(type: CrmActivityType): string` - human-readable labels
- `getAgreementStatus(agreement: { expiry_date: string | null; status: string }): string` - returns 'expiring_soon' if within 30 days, else status
- `isAgreementExpiring(expiryDate: string | null, daysThreshold?: number): boolean` - true if within threshold (default 30)
- `formatCurrency(amount: number): string` - USD format
- `formatRatePerMile(rate: number | null): string` - "$X.XX/mi" format
  </action>
  <verify>
    <automated>cd /Users/rodpatterson/Manifest && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>CRM types exported from database.ts, Zod schemas validate all CRM forms, helper functions format CRM data. TypeScript compiles with no errors.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- Migration file has 6 CREATE TABLE statements
- All 6 tables have RLS enabled with org_isolation policies
- Zod schemas export with z.input types for form compatibility
</verification>

<success_criteria>
CRM database foundation ready: migration deployable, types importable, schemas usable in forms, helpers available for UI formatting.
</success_criteria>

<output>
After completion, create `.planning/phases/09-crm-cross-module-integration/09-01-SUMMARY.md`
</output>
