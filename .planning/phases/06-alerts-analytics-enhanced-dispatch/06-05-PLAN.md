---
phase: 06-alerts-analytics-enhanced-dispatch
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(app)/dispatch/dispatch-board.tsx
  - src/app/(app)/dispatch/dispatch-map.tsx
  - src/app/(app)/dispatch/dispatch-timeline.tsx
  - src/app/(app)/dispatch/conflict-warning.tsx
  - src/app/(app)/dispatch/page.tsx
  - src/app/(app)/dispatch/actions.ts
  - tests/dispatch/map.test.ts
  - tests/dispatch/timeline.test.ts
autonomous: true
requirements:
  - EDSP-01
  - EDSP-02
  - EDSP-03
  - EDSP-04

must_haves:
  truths:
    - "Dispatch board has tab navigation: List, Map, Timeline"
    - "Map view shows unassigned loads as pins and available drivers as different-colored pins"
    - "Timeline view shows driver schedules with current/next loads and gaps"
    - "Conflict warning appears when assigning driver with overlapping load"
    - "Smart routing suggestions remain accessible in dispatch UI"
  artifacts:
    - path: "src/app/(app)/dispatch/dispatch-map.tsx"
      provides: "MapLibre map with load and driver pins"
      min_lines: 50
    - path: "src/app/(app)/dispatch/dispatch-timeline.tsx"
      provides: "Gantt-style timeline of driver schedules"
      min_lines: 50
    - path: "src/app/(app)/dispatch/conflict-warning.tsx"
      provides: "Inline conflict detection warning"
      exports: ["ConflictWarning"]
  key_links:
    - from: "src/app/(app)/dispatch/dispatch-board.tsx"
      to: "src/app/(app)/dispatch/dispatch-map.tsx"
      via: "dynamic import with ssr:false"
      pattern: "dynamic.*dispatch-map.*ssr.*false"
    - from: "src/app/(app)/dispatch/dispatch-board.tsx"
      to: "src/app/(app)/dispatch/dispatch-timeline.tsx"
      via: "conditional render based on active tab"
      pattern: "activeView.*timeline"
    - from: "src/app/(app)/dispatch/conflict-warning.tsx"
      to: "src/lib/dispatch/conflict-check.ts"
      via: "checkDispatchConflict call"
      pattern: "checkDispatchConflict"
---

<objective>
Enhanced dispatch board with three view tabs (List, Map, Timeline), conflict detection warning on assignment, and continued smart routing integration from Phase 5.

Purpose: Map view lets dispatchers visually match drivers to loads by proximity. Timeline view shows scheduling gaps. Conflict detection prevents double-booking.
Output: Map component with MapLibre, timeline component, conflict warning, tabbed dispatch board.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-alerts-analytics-enhanced-dispatch/06-CONTEXT.md
@.planning/phases/06-alerts-analytics-enhanced-dispatch/06-RESEARCH.md
@.planning/phases/06-alerts-analytics-enhanced-dispatch/06-01-SUMMARY.md
@src/app/(app)/dispatch/dispatch-board.tsx
@src/app/(app)/dispatch/page.tsx
@src/components/dispatch/dispatch-assignment-form.tsx

<interfaces>
From src/app/(app)/dispatch/dispatch-board.tsx:
```typescript
interface DispatchBoardProps {
  unassignedLoads: Load[]
  drivers: Driver[]
  activeDispatches: ActiveDispatch[]
  busyDriverIds: string[]
  vehicles: Pick<Vehicle, 'id' | 'unit_number' | 'make' | 'model'>[]
  orgId: string | null
}
```

From src/components/dispatch/active-dispatches-list.tsx:
```typescript
export type ActiveDispatch = {
  id: string; load_id: string; driver_id: string; vehicle_id: string | null;
  status: DispatchStatus; assigned_at: string; estimated_pickup_arrival: string | null;
  estimated_delivery_arrival: string | null; load: Load; driver: Driver;
}
```

From src/lib/geo/city-coords.ts (Plan 01):
```typescript
export function getCityCoords(city: string | null, state: string | null): { lat: number; lng: number } | null
```

From src/lib/dispatch/conflict-check.ts (Plan 01):
```typescript
export function checkDateOverlap(range1, range2): boolean
export function checkDispatchConflict(supabase, driverId, newLoadId): Promise<{ hasConflict: boolean; conflictingLoads: string[] }>
```

From src/components/dispatch/driver-suggestions.tsx (Phase 5):
```typescript
// Smart routing suggestions already exist in dispatch assignment form
// Suggested/Manual tabs already in DispatchAssignmentForm
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch map view with MapLibre and driver/load pins</name>
  <files>src/app/(app)/dispatch/dispatch-map.tsx, src/app/(app)/dispatch/dispatch-board.tsx, tests/dispatch/map.test.ts</files>
  <action>
    **src/app/(app)/dispatch/dispatch-map.tsx:**
    - 'use client' component, DEFAULT export (required for dynamic import)
    - Import { Map, Marker, NavigationControl, Popup } from 'react-map-gl/maplibre'
    - Import 'maplibre-gl/dist/maplibre-gl.css'
    - Import getCityCoords from '@/lib/geo/city-coords'
    - Props: { loads: Load[], drivers: Driver[], onSelectLoad?: (loadId: string) => void }
    - Map config: mapStyle = 'https://tiles.openfreemap.org/styles/positron', initial viewport centered on US (lat: 39.8, lng: -98.5, zoom: 4)
    - Load pins: use getCityCoords(load.pickup_city, load.pickup_state) for position. Pin color: #EC008C (brand pink). Show load number on hover via Popup.
    - Driver pins: use getCityCoords based on driver's last delivery location -- look up from the driver's last completed dispatch's load delivery_city/state. If no last delivery, use home_terminal or skip. Pin color: #3B82F6 (blue). Show driver name on hover.
    - Loads without coordinates are listed in a "Loads without location" sidebar panel below the map
    - Drivers without location show as "Location unknown" in a small legend
    - Clicking a load pin calls onSelectLoad(load.id)
    - Height: 500px, rounded corners, shadow

    **src/app/(app)/dispatch/dispatch-board.tsx:**
    - Add tab navigation: List | Map | Timeline (use simple button group with active state)
    - State: activeView = 'list' | 'map' | 'timeline'
    - Import dispatch-map dynamically: const DispatchMap = dynamic(() => import('./dispatch-map'), { ssr: false, loading: () => <div className="h-[500px] bg-gray-100 animate-pulse rounded-lg" /> })
    - When activeView === 'map': render DispatchMap with loads + drivers props
    - When activeView === 'list': render existing two-column layout (unchanged)
    - When activeView === 'timeline': render DispatchTimeline (Task 2)
    - Tab buttons styled with brand colors, active tab gets solid background
    - Keep assignment form visible regardless of view (load selection works from any view)

    **tests/dispatch/map.test.ts:**
    - Test that getCityCoords is called for each load with pickup_city/state
    - Test that loads without coordinates are filtered to "no location" list
    - Test component renders without errors (mock react-map-gl)
  </action>
  <verify>
    <automated>npx vitest run tests/dispatch/map.test.ts --reporter=verbose 2>/dev/null; echo "Build:"; npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Map view shows load pins (pink) and driver pins (blue) on US map, loads without coordinates listed separately, clicking load pin selects it for assignment</done>
</task>

<task type="auto">
  <name>Task 2: Timeline/Gantt view and conflict detection warning</name>
  <files>src/app/(app)/dispatch/dispatch-timeline.tsx, src/app/(app)/dispatch/conflict-warning.tsx, src/app/(app)/dispatch/dispatch-board.tsx, src/app/(app)/dispatch/page.tsx, tests/dispatch/timeline.test.ts</files>
  <action>
    **src/app/(app)/dispatch/dispatch-timeline.tsx:**
    - 'use client' component
    - Props: { drivers: Driver[], activeDispatches: ActiveDispatch[], onSelectDriver?: (driverId: string) => void }
    - Custom timeline component (NOT a heavy Gantt library, per research recommendation)
    - Layout: vertical list of drivers on the left (names), horizontal time axis (next 7 days) across the top
    - Each driver row shows horizontal bars for their active dispatches:
      - Bar position: pickup_date to delivery_date mapped to time axis
      - Bar color: by dispatch status (assigned=yellow, accepted=blue, en_route=green, at_pickup/at_delivery=orange)
      - Bar label: load number (truncated if narrow)
      - Hover tooltip: load details (pickup city -> delivery city, dates)
    - Gaps between loads shown as empty space (available time)
    - Current time marker as vertical red line
    - Scrollable horizontally if needed
    - Drivers with no active dispatches show "Available" in green text
    - Clicking a driver row calls onSelectDriver if provided
    - Clean, readable design (per user request: "should look like a scheduling tool -- clear, readable, not cramped")
    - Use min-height per row for readability, proper spacing

    **src/app/(app)/dispatch/conflict-warning.tsx:**
    - 'use client' component
    - Props: { driverId: string, loadId: string }
    - On mount (useEffect), call a server action or API to check for conflicts
    - Use checkDispatchConflict from '@/lib/dispatch/conflict-check' via a server action wrapper
    - If conflict detected: show yellow/amber warning banner with AlertTriangle icon
    - Message: "Warning: Driver has overlapping loads ({conflicting load numbers}). Assigning may cause scheduling conflicts."
    - Banner appears inline in the assignment form, between driver selection and submit button
    - If no conflict: render nothing

    **src/app/(app)/dispatch/dispatch-board.tsx:**
    - Add DispatchTimeline to the timeline tab view
    - Pass drivers and activeDispatches to timeline
    - Wire onSelectDriver to auto-scroll/highlight that driver in assignment

    **src/app/(app)/dispatch/page.tsx:**
    - Ensure page passes enough data for timeline view (activeDispatches must include load data for date ranges)
    - May need to enrich activeDispatches query with load pickup_date, delivery_date if not already included

    **Wire ConflictWarning into dispatch-assignment-form.tsx:**
    - Import ConflictWarning component
    - When a driver is selected in the assignment form AND a load is selected, render ConflictWarning between the driver dropdown and submit button
    - Pass selectedDriverId and selectedLoadId

    **EDSP-04 (Smart routing integration):**
    - Already built in Phase 5 (Suggested/Manual tabs in DispatchAssignmentForm)
    - No new work needed -- verify the existing suggestion panel still works with the new tabbed layout
    - The assignment form with smart routing tabs should be visible alongside all three views (list/map/timeline)

    **tests/dispatch/timeline.test.ts:**
    - Test timeline renders driver rows from props
    - Test drivers with no dispatches show "Available"
    - Test dispatch bars render with correct status colors
    - Test ConflictWarning shows warning when hasConflict is true
    - Test ConflictWarning renders nothing when no conflict
  </action>
  <verify>
    <automated>npx vitest run tests/dispatch/timeline.test.ts --reporter=verbose 2>/dev/null; echo "Build:"; npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Timeline view shows driver schedules with colored bars for dispatches and gaps, conflict warning appears when assigning overlapping driver, all 3 dispatch views accessible via tabs, smart routing still works in assignment form</done>
</task>

</tasks>

<verification>
- Dispatch board has 3 tabs: List, Map, Timeline
- Map view renders with MapLibre and shows pins (requires browser)
- Timeline view shows driver schedules as horizontal bars
- Conflict warning appears when assigning driver with overlapping loads
- Smart routing suggestion panel still works in assignment form
- TypeScript compiles: `npx tsc --noEmit`
- Tests pass: `npx vitest run tests/dispatch/ --reporter=verbose`
</verification>

<success_criteria>
- Map view shows load pins (pink) and driver pins (blue) on US map
- Map dynamically imported with ssr:false (no SSR crashes)
- Timeline shows 7-day view with driver schedule bars and gaps
- Current time marker visible on timeline
- Conflict detection warns on overlapping assignment (yellow banner)
- Tab navigation (List/Map/Timeline) preserves assignment form visibility
- Smart routing suggestions (Phase 5) accessible from all views
- Clean, readable timeline design
</success_criteria>

<output>
After completion, create `.planning/phases/06-alerts-analytics-enhanced-dispatch/06-05-SUMMARY.md`
</output>
