---
phase: 04-invoicing-dashboard
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/(app)/invoices/actions.ts
  - src/app/(app)/invoices/status-actions.ts
  - src/app/(app)/invoices/page.tsx
  - src/app/(app)/invoices/invoices-view.tsx
  - src/app/(app)/invoices/new/page.tsx
  - src/app/(app)/invoices/[id]/page.tsx
  - src/app/(app)/invoices/[id]/edit/page.tsx
  - src/components/invoices/invoice-form.tsx
  - src/components/invoices/invoice-list.tsx
  - src/components/invoices/invoice-filters.tsx
  - src/components/invoices/invoice-detail.tsx
  - src/app/(app)/loads/[id]/page.tsx
  - src/components/layout/sidebar.tsx
autonomous: true
requirements: [INV-01, INV-03, INV-04, INV-07]

must_haves:
  truths:
    - "User can create an invoice from a delivered load with auto-populated broker/rate data"
    - "User can edit invoice fields (bill-to, amounts, dates, notes)"
    - "User can mark invoice as sent, paid, or void with valid transitions"
    - "Invoice list shows all invoices with filters by status, date range, broker/customer"
    - "Invoice detail page shows full invoice info with status actions"
    - "Load detail page has 'Create Invoice' button when status is delivered"
    - "Sidebar includes /invoices link"
  artifacts:
    - path: "src/app/(app)/invoices/actions.ts"
      provides: "createInvoiceFromLoad, updateInvoice server actions"
      exports: ["createInvoiceFromLoad", "updateInvoice"]
    - path: "src/app/(app)/invoices/status-actions.ts"
      provides: "markInvoiceSent, markInvoicePaid, markInvoiceVoid server actions"
      exports: ["markInvoiceSent", "markInvoicePaid", "markInvoiceVoid"]
    - path: "src/app/(app)/invoices/page.tsx"
      provides: "Invoice list page (server component)"
    - path: "src/components/invoices/invoice-form.tsx"
      provides: "Invoice create/edit form with react-hook-form + Zod"
    - path: "src/components/invoices/invoice-list.tsx"
      provides: "Invoice list table component"
  key_links:
    - from: "src/app/(app)/invoices/actions.ts"
      to: "supabase invoices table"
      via: "createClient + insert/update"
      pattern: "from.*invoices.*insert|update"
    - from: "src/app/(app)/invoices/actions.ts"
      to: "src/app/(app)/loads/"
      via: "Updates load status to 'invoiced' on invoice creation"
      pattern: "loads.*update.*invoiced"
    - from: "src/components/invoices/invoice-form.tsx"
      to: "src/schemas/invoice.ts"
      via: "zodResolver(invoiceSchema)"
      pattern: "zodResolver.*invoiceSchema"
    - from: "src/app/(app)/loads/[id]/page.tsx"
      to: "createInvoiceFromLoad"
      via: "Create Invoice button for delivered loads"
      pattern: "createInvoiceFromLoad"
---

<objective>
Invoice CRUD UI: create from delivered load, edit, list with filters, detail view, and status transition actions.

Purpose: Users need to create, view, edit, and manage invoice statuses through a complete UI workflow.
Output: Invoice pages, components, server actions, sidebar navigation update, and load detail integration.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-invoicing-dashboard/04-CONTEXT.md
@.planning/phases/04-invoicing-dashboard/04-RESEARCH.md
@.planning/phases/04-invoicing-dashboard/04-01-SUMMARY.md

@src/app/(app)/loads/status-actions.ts
@src/app/(app)/loads/[id]/page.tsx
@src/components/layout/sidebar.tsx

<interfaces>
<!-- From Plan 01 outputs (will exist when this plan runs) -->

From src/types/database.ts:
```typescript
export type InvoiceStatus = 'draft' | 'sent' | 'paid' | 'overdue' | 'void'
export type Invoice = {
  id: string; org_id: string; load_id: string; invoice_number: string;
  bill_to_company: string; bill_to_email: string | null; bill_to_address: string | null;
  amount: number; fuel_surcharge: number; accessorials: number; total: number;
  status: InvoiceStatus; issued_date: string | null; due_date: string | null;
  paid_date: string | null; paid_amount: number | null; payment_method: string | null;
  notes: string | null; pdf_url: string | null; created_at: string; updated_at: string;
}
```

From src/lib/invoice-status.ts:
```typescript
export function canTransitionInvoice(from: InvoiceStatus, to: InvoiceStatus): boolean
export function getInvoiceStatusLabel(status: InvoiceStatus): string
```

From src/schemas/invoice.ts:
```typescript
export const invoiceSchema: ZodObject<...>
export type InvoiceInput = z.input<typeof invoiceSchema>
```

From src/hooks/use-realtime-invoices.ts:
```typescript
export function useRealtimeInvoices(orgId: string | null): void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoice server actions and form component</name>
  <files>src/app/(app)/invoices/actions.ts, src/app/(app)/invoices/status-actions.ts, src/components/invoices/invoice-form.tsx</files>
  <action>
    1. Create `src/app/(app)/invoices/actions.ts` with 'use server':
       - `createInvoiceFromLoad(loadId: string)`: Query load by id where status='delivered', auto-populate invoice from load data (bill_to_company from broker_name, bill_to_email from broker_email, amount from rate_amount, fuel_surcharge, accessorials from accessorial_charges, total from total_charges), set issued_date to today, due_date to today+30 days, status='draft'. Insert into invoices table (invoice_number auto-generated by trigger). Then update load status to 'invoiced' using Supabase update. revalidatePath('/loads'), revalidatePath('/invoices'). Return { id } or { error }.
       - `updateInvoice(id: string, formData: FormData)`: Parse form data with invoiceSchema, update invoice record. Recompute total = amount + fuel_surcharge + accessorials. revalidatePath('/invoices'). Return { error? }.

    2. Create `src/app/(app)/invoices/status-actions.ts` with 'use server' (mirroring loads/status-actions.ts):
       - `markInvoiceSent(invoiceId: string)`: Validate transition with canTransitionInvoice, update status to 'sent'. revalidatePath.
       - `markInvoicePaid(invoiceId: string, paidDate: string, paidAmount: number, paymentMethod: string)`: Validate transition, update status to 'paid' with paid_date, paid_amount, payment_method. Also update linked load status to 'paid'. revalidatePath.
       - `markInvoiceVoid(invoiceId: string)`: Validate transition, update status to 'void'. revalidatePath.

    3. Create `src/components/invoices/invoice-form.tsx`:
       - Client component using react-hook-form + zodResolver(invoiceSchema)
       - Props: initialData (optional Invoice for edit mode), onSubmit handler, isLoading
       - Fields: bill_to_company (text), bill_to_email (text), bill_to_address (textarea), amount (number), fuel_surcharge (number), accessorials (number), total (computed display, not editable -- auto-calculated from amount + fuel_surcharge + accessorials), issued_date (date input), due_date (date input), notes (textarea), payment_method (select for edit mode only)
       - Use z.input<typeof invoiceSchema> for form types per project convention
       - Display invoice_number in JetBrains Mono font (read-only) when in edit mode
       - Show total as formatted currency using Intl.NumberFormat
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>createInvoiceFromLoad auto-populates from load data and transitions load to invoiced. Status actions validate transitions. Invoice form renders with Zod validation and auto-calculated total.</done>
</task>

<task type="auto">
  <name>Task 2: Invoice pages (list, create, detail, edit) and sidebar update</name>
  <files>src/app/(app)/invoices/page.tsx, src/app/(app)/invoices/invoices-view.tsx, src/app/(app)/invoices/new/page.tsx, src/app/(app)/invoices/[id]/page.tsx, src/app/(app)/invoices/[id]/edit/page.tsx, src/components/invoices/invoice-list.tsx, src/components/invoices/invoice-filters.tsx, src/components/invoices/invoice-detail.tsx, src/components/layout/sidebar.tsx</files>
  <action>
    1. Create `src/components/invoices/invoice-filters.tsx`:
       - Client component with filter controls: status dropdown (all, draft, sent, paid, overdue, void), date range (from/to date inputs), broker/customer text search
       - Uses URL searchParams pattern (matching loads-view.tsx filter pattern)
       - Updates URL on filter change via router.push with searchParams

    2. Create `src/components/invoices/invoice-list.tsx`:
       - Client component rendering table of invoices
       - Columns: invoice_number (JetBrains Mono), bill_to_company, total (formatted currency), status (StatusBadge variant='invoice'), issued_date, due_date
       - Row click navigates to /invoices/[id]
       - Empty state with "No invoices found" message

    3. Create `src/app/(app)/invoices/invoices-view.tsx`:
       - Client wrapper component (matching loads-view pattern)
       - Includes InvoiceFilters + InvoiceList + useRealtimeInvoices hook
       - Passes filtered data to InvoiceList

    4. Create `src/app/(app)/invoices/page.tsx`:
       - Server component that queries invoices with searchParams filters
       - Applies status, date range, and broker/customer text filters
       - Passes data to InvoicesView
       - "New Invoice" button links to /invoices/new

    5. Create `src/app/(app)/invoices/new/page.tsx`:
       - Server component with loadId from searchParams
       - If loadId provided: query load, pass data to InvoiceForm as pre-populated initialData
       - If no loadId: show dropdown/selector of delivered loads without invoices, then redirect with loadId
       - Form submits via createInvoiceFromLoad action, then redirects to invoice detail

    6. Create `src/components/invoices/invoice-detail.tsx`:
       - Client component showing full invoice info
       - Sections: header (invoice_number in JetBrains Mono, StatusBadge), bill-to info, line items (amount, fuel surcharge, accessorials, total), dates (issued, due, paid), notes
       - Status action buttons based on current status: Draft->["Mark Sent","Void"], Sent->["Mark Paid","Void"], Overdue->["Mark Paid","Void"]. Paid/Void show no action buttons.
       - "Mark Paid" opens inline form for paid_date, paid_amount, payment_method
       - "Download PDF" link to /api/invoices/[id]/pdf (Plan 03 implements the route)
       - "Edit" button links to /invoices/[id]/edit

    7. Create `src/app/(app)/invoices/[id]/page.tsx`:
       - Server component querying invoice by id with joined load data
       - Passes to InvoiceDetail component

    8. Create `src/app/(app)/invoices/[id]/edit/page.tsx`:
       - Server component querying invoice by id
       - Renders InvoiceForm in edit mode with updateInvoice action

    9. Update `src/components/layout/sidebar.tsx`:
       - Add /invoices link with FileText (or Receipt) icon from lucide-react
       - Place after dispatch link, before any settings link

    10. Update load detail page to add "Create Invoice" button:
        - In `src/app/(app)/loads/[id]/page.tsx`, add a "Create Invoice" button/link that appears only when load.status === 'delivered'
        - Links to /invoices/new?loadId={load.id}
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>Invoice list page shows all invoices with working filters. Create invoice page auto-populates from delivered load. Detail page shows full info with status action buttons. Edit page allows field updates. Sidebar has invoices link. Load detail has "Create Invoice" button for delivered loads.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- TypeScript compiles without errors
- `npx vitest run` -- full test suite passes
- Invoice list page renders at /invoices
- Create invoice from delivered load auto-populates data
- Status transitions work: draft -> sent -> paid
- Sidebar shows invoices link
- Load detail shows "Create Invoice" for delivered loads
</verification>

<success_criteria>
- Complete invoice CRUD: create from delivered load, edit, list with filters, detail view
- Status actions: mark sent, mark paid (with payment details), mark void
- Load status transitions to 'invoiced' on invoice creation, 'paid' on invoice payment
- Invoice numbers display in JetBrains Mono
- Amounts formatted as currency
- Sidebar navigation includes /invoices
- Load detail page has "Create Invoice" button for delivered loads
</success_criteria>

<output>
After completion, create `.planning/phases/04-invoicing-dashboard/04-02-SUMMARY.md`
</output>
