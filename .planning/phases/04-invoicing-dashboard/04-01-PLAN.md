---
phase: 04-invoicing-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00016_invoices.sql
  - src/types/database.ts
  - src/lib/invoice-status.ts
  - src/schemas/invoice.ts
  - src/components/ui/status-badge.tsx
  - src/hooks/use-realtime-invoices.ts
  - next.config.ts
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05]

must_haves:
  truths:
    - "Invoices table exists with all PRD-01 Section 6.2 columns and RLS"
    - "Invoice number auto-generates on INSERT via database trigger"
    - "Overdue invoices are auto-detected daily via pg_cron"
    - "Invoice status transitions are validated (draft->sent->paid, void from any non-terminal)"
    - "Invoice Zod schema validates form input"
    - "StatusBadge renders invoice status variants"
    - "Realtime hook refreshes on invoice changes"
  artifacts:
    - path: "supabase/migrations/00016_invoices.sql"
      provides: "Invoices table, invoice_number_sequences, trigger, pg_cron job, RLS, storage bucket"
      contains: "create table invoices"
    - path: "src/types/database.ts"
      provides: "Invoice and InvoiceStatus types, Database type extension"
      contains: "InvoiceStatus"
    - path: "src/lib/invoice-status.ts"
      provides: "Invoice status transition logic"
      exports: ["InvoiceStatus", "INVOICE_STATUSES", "VALID_INVOICE_TRANSITIONS", "canTransitionInvoice"]
    - path: "src/schemas/invoice.ts"
      provides: "Zod validation for invoice form"
      exports: ["invoiceSchema", "InvoiceInput"]
    - path: "src/components/ui/status-badge.tsx"
      provides: "Invoice status color variants"
      contains: "invoice"
    - path: "src/hooks/use-realtime-invoices.ts"
      provides: "Realtime subscription for invoices table"
      exports: ["useRealtimeInvoices"]
    - path: "next.config.ts"
      provides: "serverExternalPackages config for @react-pdf/renderer"
      contains: "serverExternalPackages"
  key_links:
    - from: "src/lib/invoice-status.ts"
      to: "src/types/database.ts"
      via: "import InvoiceStatus type"
      pattern: "import.*InvoiceStatus.*from.*database"
    - from: "src/hooks/use-realtime-invoices.ts"
      to: "supabase client"
      via: "postgres_changes on invoices table"
      pattern: "table.*invoices"
---

<objective>
Invoice foundation: database schema, TypeScript types, status logic, validation, and supporting infrastructure.

Purpose: Every subsequent plan (invoice UI, PDF generation, dashboards) depends on the invoices table, types, and status logic existing first.
Output: Migration file, extended types, invoice-status module, Zod schema, StatusBadge extension, Realtime hook, next.config update.
</objective>

<execution_context>
@/Users/rodpatterson/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rodpatterson/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-invoicing-dashboard/04-RESEARCH.md

@src/types/database.ts
@src/lib/load-status.ts
@src/hooks/use-realtime-loads.ts
@src/components/ui/status-badge.tsx
@supabase/migrations/00011_load_number_trigger.sql

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/database.ts (to extend):
```typescript
export type LoadStatus = 'booked' | 'dispatched' | 'in_transit' | 'at_pickup' | 'loaded' | 'at_delivery' | 'delivered' | 'invoiced' | 'paid' | 'canceled'
export type Database = { public: { Tables: { ... } } }
```

From src/lib/load-status.ts (pattern to mirror):
```typescript
export const VALID_TRANSITIONS: Record<LoadStatus, LoadStatus[]> = { ... }
export function canTransition(from: LoadStatus, to: LoadStatus): boolean
```

From src/hooks/use-realtime-loads.ts (pattern to mirror):
```typescript
export function useRealtimeLoads(orgId: string | null)
// Uses useRef for Supabase client, subscribes to postgres_changes
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration, TypeScript types, invoice-status module, and Zod schema</name>
  <files>supabase/migrations/00016_invoices.sql, src/types/database.ts, src/lib/invoice-status.ts, src/schemas/invoice.ts, tests/invoices/schema.test.ts, tests/invoices/status.test.ts</files>
  <behavior>
    - invoiceSchema validates: bill_to_company required (min 1 char), bill_to_email optional or valid email, amount/fuel_surcharge/accessorials/total coerce to non-negative numbers, issued_date and due_date required strings, notes and payment_method optional default ''
    - invoiceSchema rejects: empty bill_to_company, negative amount, missing issued_date, missing due_date
    - canTransitionInvoice('draft', 'sent') returns true
    - canTransitionInvoice('draft', 'paid') returns false
    - canTransitionInvoice('sent', 'paid') returns true
    - canTransitionInvoice('sent', 'void') returns true
    - canTransitionInvoice('overdue', 'paid') returns true
    - canTransitionInvoice('overdue', 'void') returns true
    - canTransitionInvoice('paid', anything) returns false
    - canTransitionInvoice('void', anything) returns false
    - INVOICE_STATUSES contains exactly ['draft', 'sent', 'paid', 'overdue', 'void']
  </behavior>
  <action>
    1. Create `supabase/migrations/00016_invoices.sql`:
       - `invoices` table per PRD-01 Section 6.2: id (uuid pk), org_id (fk organizations), load_id (fk loads), invoice_number (text not null), bill_to_company (text not null), bill_to_email (text), bill_to_address (text), amount (numeric 10,2 not null), fuel_surcharge (numeric 10,2 default 0), accessorials (numeric 10,2 default 0), total (numeric 10,2 not null), status (text not null default 'draft'), issued_date (date), due_date (date), paid_date (date), paid_amount (numeric 10,2), payment_method (text), notes (text), pdf_url (text), created_at/updated_at (timestamptz default now())
       - Enable RLS: `create policy "org_invoices" on invoices for all using (org_id = (select auth.org_id()))` matching existing pattern
       - Index on org_id, status, load_id
       - `invoice_number_sequences` table: org_id (uuid fk), year_month (text), last_number (integer default 0), primary key (org_id, year_month)
       - Enable RLS on invoice_number_sequences (same org_id pattern)
       - `generate_invoice_number()` trigger function modeled on 00011_load_number_trigger.sql: INV-YYYYMM-NNNN format, uses UPSERT on invoice_number_sequences for atomic increment
       - `trg_generate_invoice_number` BEFORE INSERT trigger on invoices
       - pg_cron extension + schedule: `cron.schedule('overdue-invoice-scanner', '0 8 * * *', $$ UPDATE public.invoices SET status = 'overdue', updated_at = now() WHERE status = 'sent' AND due_date < CURRENT_DATE; $$)`
       - Create 'invoice-documents' storage bucket with same RLS pattern as load-documents (org_id folder scoping)
       - Grant SELECT on invoice_number_sequences to authenticated role

    2. Extend `src/types/database.ts`:
       - Add `InvoiceStatus = 'draft' | 'sent' | 'paid' | 'overdue' | 'void'`
       - Add `PaymentMethod = 'check' | 'ach' | 'wire' | 'credit_card' | 'other' | ''`
       - Add `Invoice` type matching all table columns
       - Add `InvoiceNumberSequence` type
       - Add `invoices` and `invoice_number_sequences` entries to `Database.public.Tables`

    3. Create `src/lib/invoice-status.ts` mirroring load-status.ts:
       - Export `InvoiceStatus` type (re-export from database.ts)
       - Export `INVOICE_STATUSES` array: ['draft', 'sent', 'paid', 'overdue', 'void']
       - Export `VALID_INVOICE_TRANSITIONS` record: draft->['sent','void'], sent->['paid','void'], overdue->['paid','void'], paid->[], void->[]
       - Export `canTransitionInvoice(from, to)` function
       - Export `getInvoiceStatusLabel(status)` function
       - Note: 'overdue' is set by pg_cron, not user action -- but users can still transition FROM overdue to paid/void

    4. Create `src/schemas/invoice.ts`:
       - Zod schema per RESEARCH code example with z.input type export
       - Use z.coerce.number() for numeric fields (matching load wizard pattern)
       - Export `invoiceSchema` and `InvoiceInput` (using z.input per project convention)
  </action>
  <verify>
    <automated>npx vitest run tests/invoices/schema.test.ts tests/invoices/status.test.ts --reporter=verbose</automated>
  </verify>
  <done>Migration file creates invoices table with RLS, trigger, pg_cron job, and storage bucket. TypeScript types extend database.ts. invoice-status.ts exports transition logic. Zod schema validates invoice input. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge extension, Realtime hook, and next.config update</name>
  <files>src/components/ui/status-badge.tsx, src/hooks/use-realtime-invoices.ts, next.config.ts</files>
  <action>
    1. Update `src/components/ui/status-badge.tsx`:
       - Add 'invoice' variant to colorMap: draft (gray), sent (blue), paid (green), overdue (red), void (gray-400)
       - Colors per RESEARCH: draft={dot:'bg-gray-500', bg:'bg-gray-50', text:'text-gray-700'}, sent={dot:'bg-blue-500', bg:'bg-blue-50', text:'text-blue-700'}, paid={dot:'bg-green-500', bg:'bg-green-50', text:'text-green-700'}, overdue={dot:'bg-red-500', bg:'bg-red-50', text:'text-red-700'}, void={dot:'bg-gray-400', bg:'bg-gray-100', text:'text-gray-500'}

    2. Create `src/hooks/use-realtime-invoices.ts`:
       - Mirror use-realtime-loads.ts exactly
       - Channel name: `org:${orgId}:invoices`
       - Subscribe to postgres_changes on invoices table filtered by org_id
       - Use useRef for Supabase client (prevent infinite re-subscription per project decision)
       - Call router.refresh() on any change

    3. Update `next.config.ts`:
       - Add `serverExternalPackages: ['@react-pdf/renderer']` to config
       - Required for PDF generation route handler (Plan 03 depends on this)
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>StatusBadge renders invoice status variants with correct colors. Realtime hook exports useRealtimeInvoices. next.config.ts includes serverExternalPackages. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/invoices/ --reporter=verbose` -- all invoice tests pass
- `npx vitest run` -- full suite green (no regressions)
- Migration file is syntactically valid SQL
- TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- invoices table, invoice_number_sequences, trigger, pg_cron job, storage bucket defined in migration
- InvoiceStatus type and Invoice type added to database.ts
- canTransitionInvoice validates all status transitions correctly
- invoiceSchema validates and rejects input as specified
- StatusBadge supports 'invoice' variant with 5 status colors
- useRealtimeInvoices hook mirrors useRealtimeLoads pattern
- next.config.ts has serverExternalPackages for @react-pdf/renderer
</success_criteria>

<output>
After completion, create `.planning/phases/04-invoicing-dashboard/04-01-SUMMARY.md`
</output>
